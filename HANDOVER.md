# 生电终结者 · 项目交接文档（HANDOVER）

> 给接手的人（或新对话里的 AI）。**新对话开局标准动作**：作者贴仓库地址+一次性 PAT → clone →
> 读本文件 + DEVLOG.md 末尾几节 → 直接接活。不用翻聊天记录。
> 仓库：https://github.com/qiaodaxian233/SDZJZ · Fabric 1.21.1 · Yarn · 纯 Java · 前置仅 Fabric API。

## ⭐ 开发守则（置顶必守）

1. 不猜接口，先查文档；拿不准的照仓库现有用法抄。
2. 不糊里糊涂干活，先把边界问清楚。
3. 不臆想业务，先跟人类对齐并留痕。
4. 不造新接口，先复用已有。
5. 不跳过验证。
6. 不动架构红线。
7. 不装懂，坦白不会。
8. 不盲改，谨慎重构。

**协作风格（作者明确要求，见其 doubaox/对话记忆.md）**：
- 用户反馈是第一信号，不是"待评估输入"。说了就照做，别拿"原则/安全/最佳实践"包装拖延。
- 有担忧先把活干完，末尾一句话提醒即可，**不重复、不展开**。
- 多文件交付走 **git push**，不甩一堆 attachment。
- 别装看过资料；抓不全就说抓不全。

## ⭐⭐ 工作流铁律（血泪换的，m99 之前丢过整轮工作）

1. **做一步推一步**：每完成一个独立改动就 commit + push，绝不在沙箱里攒。沙箱随时重置。
2. PAT 由作者在对话里现贴，**绝不写进任何提交文件**；git 身份沿用仓库既有提交者。
3. 沙箱编不了 Fabric/Mojang 依赖 → 新 API 一律标「待编译验证」，作者本地 IDEA+JDK21 编译，报错贴回逐个修。
   沙箱可 `apt-get update && apt-get install openjdk-21-jdk-headless` 装 javac 做**纯语法冒烟检查**
   （**m523 起 xplat 文件上挂白名单前先 `python3 docs/tools_gen_api_check.py --candidate <文件>` 过第 20 闸，再写 build.gradle**——m522b 血案是先挂再红）
   （**m523b 起第 20 闸第三类判据「抽象必自声」**：xplat 白名单文件 `extends MC类 implements MC接口` 时，接口抽象方法两代父类是否兜底要对 yarn 1.20.1 数一遍——1.21 父类顺手给的方法 1.20.1 可能没有，冒烟对这类错永远是盲的）
   （**m508 起冒烟一律跑 `python3 docs/tools_smoke.py`**：两代全量排序清单 + 改动文件逐文件单编——javac 无 MC jar 时类体归因与源文件顺序有关，`find` 无序清单下 78 个文件（全部主线屏）曾整类不归因，单编才完整；日志在 /tmp/smoke/）
   （grep "expected|illegal|reached end|not a statement|unclosed"，其余报错都是缺 MC 依赖，正常）。
   **m510 起：每刀推完必须读 CI**——沙箱拿不到 artifact，读法=拉 `ci-mojmap-errors`(主线 javac)/`ci-retro-errors`(1.20.1 编译+GameTest)/
   `ci-gametest-report`(主线 GameTest) 三个分支的日志（raw 域可读；run 状态走 `api.github.com/.../actions/runs` 带 PAT 头防限流）。
   CI 红=判官不存在、构建不存在，**红着不许写下一刀**（m291b 规矩 m473~m509 间实际没人执行，主线断了 28 刀没人发现）。
4. **注册六件套逐项计数断言**（m92b 教训）：新物品 = MachineDef + ModItems(reg+创造栏两处) +
   SuperBenchRecipes/data 配方 + 中英 lang + 模型 json + 贴图 png，改完逐项 grep 验数，JSON 过 json.load。
5. 每里程碑写 DEVLOG（现象→根因→修法→教训），提交信息带 mNN 编号。
6. 升级/封顶类公式改动必问："到顶之后玩家再投入会怎样"——静默无效比数值弱更伤（m99 教训）。
7. **版本号一笔一跳**（m317 作者拍板）：每笔里程碑提交前把 gradle.properties 的
   mod_version 改成 **0.1.<里程碑号>**（热修字母尾号不抬数字段）；CI "版本号对表"闸忘跳即红。
8. **两代际两 JDK**（m407 作者实机报修）：根构建（MC 1.21.1 / Gradle 8.10 / Loom 1.7.4）**必须跑在 JDK 21**，
   `versions/26.2` 子构建（Gradle 9.5.1）**必须跑在 JDK 25**。作者机器为了 26.2 装了 25 并成了默认 JAVA_HOME，
   根构建的守护进程随之用 25 起，连 build.gradle 都编不过（`Unsupported class file major version 69`，69=Java 25）。
   仓库已用 `gradle/gradle-daemon-jvm.properties`（`toolchainVersion=21`）把根构建的守护进程钉死在 21；
   **以后动构建/换 Gradle 版本，先问这条**。临时兜底：`gradlew.bat -Dorg.gradle.java.home="<jdk21路径>" build`。

## 核心调度公平性：已收官（m302~m309，实测验证通过）——评审复评 9.4

评审③复评结论（2026-08-07 作者转达）：定性 **anti-starvation scheduler**（防饥饿，非严格
fair-share）正确且够用；**明确不要现在重写 Round Robin**（默认预算极高平时不触顶，89 行状态
简单胜过中央运行队列）；工作量欠账不丢+burst 上限的处理获认可；"下一步应该测，而不是继续
凭感觉重构"。压测矩阵：1/10/50/100 核 × 64/256/512 节点，cap 压到 100 满载跑 5~10 分钟，
判据=无长期零吞吐核心且最低最高吞吐只差几倍。工具链：/sdzjz profile sched（m304）+
GameTest 七八号用例（m305/m309）+ 一键压测 /sdzjz bench（m306~m308）。
**实测终局（2026-08-08 第五份报告）**：100核×64节点+产线核心（101>cap100 最坏形），
零吞吐=0、分布 1.0×、预算账分毫不差——防饥饿+拍龄有界轮转在真实 BE tick 序成立，销账。
过程中抓获并修复：铺场鸡生蛋死锁(m307)/看门狗占空比噪声(m308)/k>cap 恒饿真 bug(m309)。

外部评审文档③唯一方向性建议。m302 把 maxRecipesPerNetworkTick 真接线成全服共享预算
（对表发现原"全服预算"实为 m270 单核心预算，NetworkTick 键此前未接线）+ 饥饿名单保底
（没吃到的核心下 tick 持保底 1 周期先食权，幽灵核心一拍自然过期），默认 1M 不束缚=行为零变化。
**远期候选（要更强公平再立项）**：方案②轮转相位（hash(pos)+serverTick 相位段轮"优先日"，
按需比例公平）+ dirty core 队列（存储/图变更才入队，空闲核心零 CPU）。

## 类型迁移六项扫描（m366b 教训制度化，顾问⑥轮原话）

任何 X→primitive/value 改型必查六项：**定义点 / 全消费点(grep 字段名逐一过目) / 容器类型(Set/Map/List 泛型) / Comparator 与 Hashing / 序列化(NBT/网络包) / 打印与还原**。m366b 漏的就是容器消费点（seen/silent 集）。

## Phase 1.5 边界三规（m367 机器化，tools_common_gate 挂 CI 必跑）

- ①Common 名册（docs/common_manifest.txt）零 MC/Fabric/Mojang 字面（剥注释后抓 import+内联 FQN）；
- ②Object 句柄只许形参/返回/record 透传，名册内禁存字段（platform/ 除外）；纯值元组 Object[] 不在此列；
- ③Platform 只做 bootstrap/registry，服务字段硬顶 4——新 SPI 各归各模块入口，防"Platform 神类"（顾问原话：Minecraft 没了别让 Platform 变成新的 Minecraft）。

## NBT 读写铁律（m353 立/m357 入档，外部审计⑤轮原话：读→view，写→copy，不要为省 GC 把写路也改 view）

- **读**：NodeTags.viewOf（NbtComponent.getNbt 零拷贝内部实包）——绝对只读，DEFAULT 空件全局共享一份，写它=全服中毒。
- **写**：NodeTags.nbtOf 拷贝 → 改 → s.set(CUSTOM_DATA, NbtComponent.of(n)) 三段缺一不可（缺 set=丢写，垃圾桶 tc 死数三百个里程碑没人发现的教训）。

## 多版本代际架构（m361 立项，顾问方案全盘采纳）

- **目标**：Common(业务)/Legacy(1.21.x)/Modern(26.x) 代际结构，双端锚点 1.21.1+26.2；五阶段 P0 地雷图(m361 已交)→P1 建 Common→P2 Legacy 参考实现(行为逐位不变)→P3 26.2→P4 补 1.21.4/5/8/11→P5 发布流水线。
- **地雷图**：docs/PLATFORM_MAP.md（工具 docs/tools_platform_scan.py 可复跑）。A=9文件/843行可直迁；B=93文件/15501行需 SPI；D=20 client；E=6 mixin。RecipeAccess 最便宜(57用点/7文件)先动，SCBE(3517行/732用点)压轴。
- **不要做**：每版本一套源码/长期分支/Common里if(version)/Mixin全版本共用/为兼容重写Planner/巨型Platform接口/26.2 API反向污染Common。
- **积压重定位**：bigStacks/portableVault 升格为 SPI 模块（BigStackService/VaultScreenPlatform，§7/§8），随 Phase 1 落位不再单刀。

## 当前状态（m526 SB5：**1.20.1 超大工作台五判官进 `RetroBenchTests`（1→6，累计 73），S 线六刀收官**——①多重集全表唯一+蓝图逐格==BOM ②配方 id 对本世代注册表·已知缺口白名单**双向对表**（新缺口红/缺口补上白名单没滚也红）③端到端铺料→出机器→PICKUP（doClick→tryRemove→onTake 服务端权威扣料）→网格空 ④填料钮+空宿主两钮一件不少+换台回收+越界，每步守恒 ⑤quickMove 三向。**判官②量出 1.20.1 配方可达 3/126**（data_panel/storage_core/data_cable）：`bom/bomPacked/addSmall` 三建表口都自动并入 `sdzjz:core_module`（本世代未注册）→109 条机器配方一条合不出；另 **10 种 1.21 原版新料被 12 条 BOM 引用**（crafter×9/微风·试炼农场专料/heavy_core）本世代查不到；44 条刷怪类需抓物笼（已知）。处置**进作业表阻塞区第一条待拍板**（推荐 A：注册 core_module，一刀级，解锁 ~53 条）。两代冒烟零真错、21 闸全绿（0.1.526）；零协议零存档零配置零资源、主线零改动。**下一刀：读 m526 CI（判官②第一次实机对表，红=账没对上看清单再滚）→ 等拍板：拍 A 开 SB6（RetroItems+core_module 六件套+白名单滚一行+缺口表 21→20），不拍回 A12；期间可做 D5 余四件头注。开工先跑 21 闸。** 上笔 m525 SB4：**主线 `SuperBenchScreen` 392 行整段下沉 xplat `client/SuperBenchView`（两代共用绘制体+交互体）**——机械替换六类（字体/leftPos-topPos/屏幕尺寸/面板尺寸常量/槽位/发包-加控件走 Host 七口），世代口三处走已有口（`gfxTex`/`gfxItem`/`SuperBenchScreenHandler.host()` 十口 private→public），BG 惰性化（m483 律），归一 diff 只剩这几处其余逐句同原文；主线屏 392→83 行壳、1.20.1 最小壳退役→91 行同形壳（三句世代差：renderBackground 单参/mouseScrolled 三参/EditBox 手动 tick）；白名单 40→42（+PinyinInitials）、1.20.1 +gui 贴图、第 19 闸资源表+删符号登记；**宿主前提头注第一份模板（D5）**；Host 七口对 yarn 两代 Screen 族核零撞名。两代冒烟零真错、自家 11 符号 0、21 闸全绿（0.1.525）；零协议零存档零配置，主线像素应逐位不变；1.20.1 可见：超大工作台屏与主线同形（浏览器/搜索/BOM/总览卡/两钮，空宿主明说不可用）。**下一刀：读 m525 CI → SB5 1.20.1 超大工作台判官（配方全表唯一/端到端合成/quickMove 服务端权威/空宿主压缩钮不吞件）进 `RetroBenchTests`，S 线收官。开工先跑 21 闸。** 上笔 m524 SB3：**超大工作台 1.20.1 方块壳 `SuperBenchBlock120`（RetroBlocks 内部类，主线 55 行原文，世代差三处=无 codec / `use` 六参 / BE 类）+ `SuperBench120` BE 壳 + 三注册 id 同名同源 `sdzjz:super_bench`（方块 `copy(CRAFTING_TABLE)` / BE / 普通 `MenuType(SuperBenchScreenHandler::new, VANILLA_SET)`）→ 注册后紧跟 `installType`（m523 安装口）+ 创造栏 + 资源五件逐字节同主线 + lang 两键**；最小屏壳 `SuperBenchScreen120`（只画底+槽位，头注"不许长功能"，SB4 换共用绘制体）+ `MenuScreens.register`；判官 +1 `RetroBenchTests.super_bench_block_be_and_menu_installed`（注册三件 + 两参构造钉 installType 装了没，累计 68，S 线判官集起点）。两代冒烟零真错（retro 95→98）、自家 5 新符号 0、21 闸全绿（0.1.524）；零协议零存档零配置，主线零改动；1.20.1 可见：创造栏多超大工作台、右键开只有网格+结果槽+背包的屏。评审两条提醒入账（闸别无限补进取活规则；白名单同步点 D4 / 共用件宿主前提 D5 登记）。**下一刀：读 m524 CI（1.20.1 job+判官必须绿）→ SB4 `SuperBenchScreen` 绘制体下沉共用件（先量 API 面：贴图 id→Gfx.tex / EditBox 同签名 / 发包留各代）→ 主线退壳 → 1.20.1 最小壳换共用件 + 拷 `super_bench_gui.png`（第 19 闸预期红一次）→ SB5。开工先跑 21 闸。** 上笔 m523b 热修：**m523 CI 1.20.1 job 红——`CraftGridInventory` 靠父类 `SimpleContainer.getItems()` 满足 `CraftingContainer.getItems()` 只对 1.21.1 成立（yarn 两代对表：1.20.1 `SimpleInventory` 无 method_54454），`does not override abstract method`**→ 本类自声明 `@Override NonNullList<ItemStack> getItems()`（返回型钉 NonNullList 才能同时覆写 1.21 父类；`withSize` 两代同名逐格装 `getItem(i)`，主线结果逐位不变，全库无人持有该列表）；**第 20 闸加第三类判据「抽象必自声」**（白名单 xplat 文件 implements 表列 MC 接口时表列方法必须本文件声明；自证=重放 m523 原文当场红→复绿）；**第七类冒烟盲区**记档（缺 MC jar 解析不了父类，这类错根本不报——补法只能闸表/桩重放）；临时桩类重放 m523 原文×1.20.1 桩=红且报错逐字同 CI、修后两代桩全绿；两代冒烟零真错、21 闸全绿、版本仍 0.1.523（热修不抬数字段）；对照表 +1 行、白名单注释、作业表 5b 补句。**下一刀：读 m523b CI（1.20.1 job 必须绿）→ SB3 `SuperBenchBlock120` 世代壳 + BE + 注册（注册后 installType）+ 创造栏 + 资源六件套 → SB4 屏；开工先跑 21 闸。** 上笔 m523 SB2b：**超大工作台 handler 四口重做→`SuperBenchScreenHandler`/`CraftGridInventory` 重挂 1.20.1 白名单（38→40）**——菜单类型安装口 `installType/reqType`（主线 ModScreenHandlers 静态段装，1.20.1 随 SB3）+ 物品宿主口 `Host` 十口 default（主线 `LegacySuperBenchHost` 原句照搬；1.20.1 RetroBootstrap 空宿主=全关，压缩区两钮明说不可用不静默吞件）+ `asCraftInput` 覆写删除（1.21.1 源码核过接口 default 逐位同义）；第 20 闸加 `--candidate` 候选模式「先闸后挂」（m522b 血案重放红 5 处→绿）；主线判官 +1 `super_bench_host_pack_roundtrip`（累计 67，钉宿主口装了没）。两代冒烟零真错、21 闸全绿（0.1.523），主线行为零变化。**评审四条同刀入账**：CanvasScreen120 ≈464 行主线搬入段登记 A12 减负（下沉 CanvasWireLayer/CanvasWireDrag，排 SB5 后；过渡期取活检查单盯锚点 m164b/m519/m193）、SB 线每刀开工先跑 21 闸、上挂先候选过闸、m521 普查稿每刀顺手更新。**下一刀：读 m523 CI（1.20.1 job 必须绿）→ SB3 `SuperBenchBlock120` 世代壳 + BE + MenuType/BlockEntityType 注册（注册后 installType）+ 创造栏 + 资源六件套拷入 → SB4 屏。** 上笔 m522b 热修：**作者 1.20.1 构建红（`CraftingInput` 1.21 新类 / handler 引 src 的 `ModScreenHandlers`）→ SuperBenchScreenHandler/CraftGridInventory 退出白名单（40→38），`SuperBenchRecipes` 与 ItemData 三口留**；第 20 闸补两类判据（1.21 新类名表 + 白名单 xplat 文件引用的自家类必须在 1.20.1 源集可见），修码前重放红 5 处（作者报 2 + 还没走到的 `ModItems`/`CompressedPackItem`/`CaptureCageItem` 3）→ 修后绿；两代冒烟零真错、21 闸全绿。**作者请重构建 1.20.1 验证。下一刀：读 m522b CI → SB2b handler 四口（菜单类型安装口/压缩包宿主口/捕获笼宿主口/删 asCraftInput 覆写）→ 重新上挂 → SB3。** 上笔 m522：**SB2——超大工作台 `SuperBenchRecipes`/`SuperBenchScreenHandler`/`CraftGridInventory` 三文件上挂 1.20.1 白名单（37→40）；`ItemData` 加服务端可用三口 itemById/clearCustomName/id（第十五个世代口）；11 处三类换口 + 第 20 闸抓到手扫漏掉的第四类 `isSameItemSameComponents`×5→`StackKey.same`**。两代冒烟零真错、21 闸全绿（0.1.522），主线行为零变化（六处同义转发），1.20.1 只编译进去玩家还开不到。**下一刀：读 m522 CI → SB3 方块壳/BE/MenuType 注册/创造栏/资源六件套 → SB4 屏 → SB5 判官。** 上笔 m521：**零代码笔——主线↔1.20.1 全量缺口普查（`docs/主线对比1201_全量缺口与超大工作台普查_m521.md`）+ 超大工作台移植分片 SB2~SB5**。作者纠正「我说的是超大工作台」：1.20.1 没有超大工作台（造机器的入口），是当前最大玩法缺口，作业表新开 S 线排 C4 前。缺口表机械对比得：方块 8→4、屏 7→2、机器 107 台 ✅、非机器物品缺 21、升级系统 ✗、包 24→13、命令/JEI/mixin 为零、A/B 线 ✅。超大工作台三文件（Recipes/ScreenHandler/CraftGridInventory）可整文件上挂白名单（11 处专属三类，缺一个服务端 id 解析口），屏绘制体下沉、方块世代壳。21 闸全绿（0.1.521）。**下一刀：读 m521 CI → SB2（服务端 id 解析口 → 三文件换口进白名单 → 第 20 闸自证）。** 上笔 m520：**C2a——1.20.1 配方域补合成/余料两域（`RetroRecipeAccess` = `LegacyRecipeAccess` 原文，世代差三处：List<CraftingRecipe> 无 Holder / `getId()` / `new ResourceLocation`）；契约 `RecipeDomainAssertions.runAll` 拆 `runCraftSmelt`+`runBrewEnch`（断言正文不动），1.20.1 判官 `recipe_domain_contract_craft_smelt` 接上（累计 66）**。两代冒烟零真错、21 闸全绿（0.1.520）；动了 1.20.1 运行期配方查询（craftingPlans 从恒空变真数据，目前只判官在问），主线零改动。**下一刀：读 m520 CI → C4a 自动合成机目标选择 UI（`ct` 键，先逐句对主线拾取器入口与 payload）→ C4b 自动合成机 tick 分支（主线整段搬）。** 上笔 m519：**作者点名——1.20.1 连线改拖线（主线口径：出口柱/进口柱/存储卡供料口按下拖到目标松手），点击式「连线模式」+顶栏钮退役；A8 升级槽核实本世代无机制不搬，A 线清空**。主线 mouseClicked 三段起手 / mouseDragged 首句 / mouseReleased 三支落靶 / 三条预览线整段搬进 `CanvasScreen120`，API 面 1.21 专属为零；真世代差三处各收一口：存储卡 24×24 供料口 `x+0.75w` 抓取 6×8、存储边走 `linkStorageTo`（三态循环包按快照连发 1~2 次到目标态，不改协议）、机器边 `linkNodes`（cut 客户端判）。验证=两代冒烟零真错、自家 12 符号 0、括号配平、九参/十一参手数、21 闸全绿（0.1.519）。零协议零存档零配置，lang 两键删除，纯客户端，主线零改动。实机脚本八条见 DEVLOG m519。**下一刀：读 m519 CI → 作者第二问「合成」：自动合成机 1.20.1 未落地——C2 拆 C2a（合成/余料两域 RetroRecipeAccess，不依赖附魔定价拍板）→ C4 目标选择 UI + 自动合成机 tick。** 上笔 m518：**A9——1.20.1 悬停聚焦 m164b（指着哪张卡只有它的线全亮，其余压暗三成；两处分流 lit 接口兑现）**。主线 `renderBg` m164b 块（`hovN/hovEnd/hovAny` + 存储线 `lit` + 机器线 `lit2` + 非归并线 `mix(termInk,wire,0.30)` 压暗式）整段搬进 1.20.1，API 面 1.21 专属为零；归一后与原文的差只有存储端一支（主线 m265 总线放置卡 `snx/sny+bw/bh` 五行→本世代 `storageAt(mouseX,mouseY)` 一句），节点命中 `wnx(i)/wny(i)` 拖动幽灵同律、卡高 `NH`（主线 `NH+26` 升级格行本世代无）；m511 共用件 `WireBundler` 里归并线与徽章的 lit 这次才真被喂到 false。验证=两代冒烟零真错、自家 8 符号 0、六式两代各 1、21 闸全绿（0.1.518）。零协议零存档零配置零资源，纯客户端，主线零改动。实机脚本六条见 DEVLOG m518。**下一刀：读 m518 CI → A8 升级槽（先核本世代有无升级系统，m464 记档：无→不搬销项）→ A 线清空后按取活规则转 D-判官合一或等 C2 拍板。** 上笔 m517：**A10——1.20.1 节点卡层进世界矩阵（卡随 zoom 缩，5% 档不再互叠）；存储卡线口回 24 屏幕实占（m458 起 zoom≠1 悬空）**。同一份 `NodeCardRenderer` 主线在 translate+scale 下调、本世代在屏幕坐标层调——"共用件读到的环境两代一样吗"第四例（矩阵/线色/作用域/坐标层）；节点卡循环挪进机器线层那份 `pushWorld`（帧序不变），坐标改世界值，裁剪换算成屏幕坐标判整卡出屏，首端高亮描边同进矩阵，死 `ring` 取色拔除。**存储卡两代都不随 zoom（刻意）**：主线放置卡=总线卡 `bw()/bh()` 只随总线倍率；本世代 24×24 屏幕实占同律，但线口 m458 起按 `24*zoom` 算与实际画的卡不一致→两处改回 24f，与 `storageAt` 一致。验证=两代冒烟零真错、自家 6 符号 0、21 闸全绿（0.1.517）。零协议零存档零配置零资源，纯客户端，主线零改动。实机脚本五条见 DEVLOG m517。**下一刀：读 m517 CI → A9 悬停聚焦 m164b（主线 hovAny/hovN/hovEnd + 分流 lit + mix 压暗；本世代 m511 起 lit 恒真处即接口）→ A8 升级槽（先核本世代有无升级系统）。** 上笔 m516：**A11——画布底/网格/暗角层两代共用（xplat `client/CanvasBackdrop`）；m515 面板背景四行在 1.20.1 生效**。先核资源：m220 装饰底图 1.20.1 没有→拷入（m489 律）。主线 renderBg 开头底色/装饰底图/32px 双色网格/暗角原文整段搬，归一 diff=0（3/3+14/14）；世代差只剩布局参数（网格起始 y 主线 34/本世代 16，暗角区 (0,23)~(workRight,botTop)/(0,16)~(width-侧栏,height)）与主线专有的顶/底终端浅带夹在底色与网格之间→ 拆两口 `fillAndDecor`/`gridAndVignette`，主线在两口之间照旧画浅带，帧序逐位不变。1.20.1 `Palette.BACKDROP` 定色 + 24×zoom 随缩放仿写网格退役→两句共用件调用；`FRAME`/`GRID_STEP` 两常量删除。验证=两代冒烟零真错、自家 5 符号 0、十参/三参/九参逐处手数、21 闸全绿（0.1.516）。零协议零存档零配置，资源 +1 贴图，纯客户端。1.20.1 可见变化：底色随主题/配置、装饰底图、32px 定距网格（平移不跳档缩放不变距）、四缘暗角、面板背景四行与主题预设底色即时生效。实机脚本四条见 DEVLOG m516。**顺手教训：javadoc 里 `*/` 提前闭注释；断言词别出现在同一刀新写的注释里——两次脚本假摔都在写盘前拦住。****下一刀：读 m516 CI → A10 1.20.1 卡片层随缩放（节点卡/存储卡进世界矩阵，命中框同口径，renderItem 在矩阵下先例=NodeCardRenderer 142 行）→ A9 → A8。** 上笔 m515：**A7c——画布设置面板 m199 + 帮助卡 m219 两代共用（xplat `client/CanvasSettingsPanel` / `CanvasHelpCard`）；1.20.1 顶栏添「设置」「帮助」**。先量 API 面：控件全是 `EditBox`（两代同签名，m192 重命名框在树先例）+ fill/drawString + SciSkin + common 配置，1.21 专属为零→整段搬不分片。主线 13 处散落（字段/常量/init 四框/open/close/几何与公共表/渲染 83 行/点击 76 行/六处事件派发/render/removed）切片生成共用件 327 行，机械替换只 `this.font`/`this.width/height` 两类，宿主给字体/屏幕尺寸/开窗前清场；主线留 open/close/renderHelp 三壳 + 六句派发（2919→2645 行），第 19 闸登记 27 符号。1.20.1：字段/init/新增 removed 落盘兜底/顶栏两钮（x=56/100 宽 40，连线钮同工艺）/六处派发主线同句同序/render 压最上层/lang 两键。**面板背景四行在 1.20.1 暂不生效**（底/网格层仍 Palette 定色，A11 登记下一刀搬），其余六行两代同效。验证=两代冒烟零真错、自家 18 符号 0、21 闸全绿（0.1.515）。零协议零存档零配置新增，纯客户端。实机脚本四条见 DEVLOG m515。**教训：先量 API 面再定搬法——看着最该分片的 300 行量下来专属为零，分片是留给真世代差的。****下一刀：读 m515 CI → A11 画布底/网格/暗角层随主题+配置（先核 m220 装饰底图贴图在 1.20.1 资源目录）→ A10 → A9。** 上笔 m514：**A7b——整理布局 autoLayout 两代共用（xplat `client/CanvasLayout` 落位几何，发包留各代）；1.20.1 画布菜单补「整理布局」**。主线 autoLayout 发的是逐节点 `NodeMovePayload`/逐端点 `StorageNodeMovePayload` 普通移动包（非专用布局包）→ 世代口=包名，本世代 `NodeMove/StorageNodeMove` 在；几何（m149 竖排/m221 间距配置/存储右列 760·72）整段搬成 `CanvasLayout.machinePos/storagePos`（无 MC jar 单编 0 错），主线两循环算术换调用、发包/`holdHome` 预测/停靠卡过滤原句不动；1.20.1 `autoLayout` 主线骨架（端点列表按序、`containsKey` 过滤同主线未放置跳过），菜单顺序同主线。验证=两代冒烟零真错、自家 6 符号 0、两包四参逐字段、21 闸全绿（0.1.514）。零协议零存档零配置纯客户端。实机脚本三条见 DEVLOG m514。**下一刀：读 m514 CI → A7c 设置面板 m199 / 帮助卡 m219（先量 EditBox/滑块 1.21 专属 API 面）→ A10。** 上笔 m513：**A7a——1.20.1 节点菜单 / 存储连线菜单条目（主线 `openNodeMenu` 与存储端点右键菜单原文，只装本世代有的操作）+ `connectedComponent` 下沉共用；右键一键摘回退役**。A7 拆三段：A7a 菜单条目（本刀）/ A7b 整理布局 / A7c 设置面板+帮助卡；拾取器一族本世代无机制不搬（m489 律）。**世代口是机制面**：四操作+存储三态连线+分组过滤后节点菜单剩断开全部连线 / 组合所选 / 组合相连 / 取出机器（垫底红显）/ 取消，存储菜单剩断开全部连线 / 取消（无总线停靠不装收回总线）；主线 addMenu 原文逐条粘（文案/图标/style 全同）。`clearLinksOfMachine/Storage`：机器线 `NodeLink(cut)` 逐对；存储边走本世代三态循环包按当前态连发 1~2 次到断（不新造包）。三张菜单贴图拷进 1.20.1 资源目录，第 19 闸 mt 对表 8→11。对照表补两行。验证=两代冒烟零真错、自家 11 符号 0、五种包构造逐字段手数、21 闸全绿（0.1.513）。零协议零存档零配置，纯客户端。实机脚本七条见 DEVLOG m513（1.20.1 可见变化：右键节点弹主线同款菜单，取出机器要过菜单；右键存储卡有菜单）。**下一刀：读 m513 CI → A7b 整理布局 autoLayout（先看主线发逐节点 NodeMove 还是专用包）→ A7c → A10。** 上笔 m512：**A6——缩放平滑动效 m186 两代共用（xplat `client/ZoomAnim`），世代差为零；顺手补上 1.20.1 侧栏滚轮那一支（m459 记档说修了、代码里从没有过）**。主线 m186 块（状态六件+m185 `clampZoom`+`setViewInstant/zoomToward/tickZoomAnim`+三处 stop 两句+`removed()` settle 一句）整段搬成 `ZoomAnim`，视图状态走 `Host`（读三口+`view(panX,panY,zoom)` 一次落写；主线 pan 直存直取，1.20.1 由 `viewX/viewY`+float zoom 换算 `panX=-viewX*zoom`），归一后与原文的差只有 Host 读写四句；**ZoomAnim 无 MC jar 单编 0 条报错**（全库第一个零 MC 符号的 client 共用件）。主线四私有方法退役为同签名转发壳，七处调用点零改动（2957→2933 行）。1.20.1：`mouseScrolled` 改主线原文结构（侧栏滚动→modal 吞→地图区不缩放→顶栏以下 `za.zoomToward(1.1/0.9)`），`renderBg` 首句 `za.tick()`，「重置视角」换主线原文 `setViewInstant(0,0,1.0)`，小地图跳转/平移补 `za.stop()`；zoom 存 float 刻意不动（`Math.round(float)` 等消费点，m366b 六项扫描前不迁）。**三处对齐主线的手感变化（可叫停）**：滚轮因子 1.15→1.1/0.9、范围 0.35~2.5→配置 5%~800%、瞬时→指数缓动（`canvasSmoothZoom=false` 回旧行为）。**顺手两件**：侧栏滚轮滚清单（m459 修③记档具体但入口那支从没写过，deepen 到 93368df 亦无，背包超一屏机器永远够不着且悬停侧栏在缩放画布）、地图区不缩放（m490 上挂漏带）。**新登记 A10：1.20.1 卡片层不随缩放**（节点卡/存储卡在屏幕层 `sx/sy` 直画，范围放开到 5% 后极端档位卡互叠，主线卡在世界矩阵下——A 线下一件该搬的）。对照表补一口两行。验证=tools_smoke 两代全量+三改动文件单编零真错、自家 14 个新/删符号 0、归一 diff=Host 四句、`zoomToward/setViewInstant/view/super.mouseScrolled` 三参逐处手数、21 闸全绿（0.1.512）。零协议零存档改动，配置零新增（三键早在 common），纯客户端。实机脚本五条见 DEVLOG m512（1.20.1 可见变化：缩放缓动+指哪缩哪、范围 5%~800%、侧栏滚轮滚清单画布不缩放、小地图/顶栏区滚轮不缩放）。**教训：记档说"修了"和代码里有是两件事——m459 修③字段/钳回/绘制都落了，唯独入口那一支没写，三十多刀没人发现是因为验证脚本要"背包塞 20+ 种机器"谁也没真塞过；凡 DEVLOG 写"已修/已接"的交互，实机脚本要有一条能从入口打到它。****下一刀：读 m512 CI → A7 设置面板/帮助/节点菜单条目/拾取器；A10 排其后。** 上笔 m511：**A4——连线归并徽章 m193 两代共用（xplat `client/WireBundler`），世代差为零；顺手扶正 1.20.1 机器线层三处半搬**。m510b 推后读 CI：badge passing、三个错误分支最新回推 sha 均早于 m510b → **三 job 全绿，m472 以来第一次**；按作业表取 A4。主线 m193 四块（存储线/机器线循环内分流计数 + `seBundles`/`mmBundles` 两段绘制）与助手区（`GROUP_ENC`/`mmAnchorGeom`/`drawBundleBadge`）整段搬成 `WireBundler`（分流两口 `takeStorageEdge/takeConnection` 留在两代各自循环里调、绘制两口 `drawStorageBundles`（屏幕层 pxScale=1f）/`drawMachineBundles`（**调用方须已在世界矩阵下**，共用件不 push 否则主线双重变换）），六段归一后与原文 diff=0；**唯一参数化的真世代差在调用点=存储端卡片接口位置**（主线总线放置卡 x+14/x+bw-14 vs 1.20.1 24×24×zoom 存储节点卡 0.25w/0.75w，卡底 +2）→ `StoragePorts` 口一口回四个屏幕坐标，选缘与走线共用。主线屏三助手删除+第 19 闸登记（3021→2957 行）。1.20.1 照主线装配 gm/gRect/nGid、两循环各加一行分流（无 m164b 悬停聚焦，lit 恒真）。**顺手四件（各是一处可单独回退）**：①1.20.1 机器线层包进 `GroupFrameRenderer.pushWorld`（放开 public）——m488 起屏幕坐标层传 zoom 是半搬，线宽随放大变细、脉冲间距不随缩放，与主线相反；②三处线色 Palette ON/GOLD/ACCENT→`SciSkin.wireOut()/wireIn()`（m198 配置色，产出线由绿变紫、供料线由金变柔绿，机器线色不变）；③`render()` 整帧 `scopeCanvas(true)`（主线 m214）——m483 起共用件在 1.20.1 一直读终端配色；④死 `line()`+`Axis` import 拔除。对照表补两口五行。验证=tools_smoke 两代全量+四改动文件单编零真错、自家 15 个新/删符号 0、diff=0、`drawWire` 十一参逐处手数、21 闸全绿（0.1.511）。纯客户端渲染零配置零协议零存档改动。**PAT 401（api 与 push 均拒）→ 先本地提交 + format-patch 交付；作者当轮补了新 PAT，m511 已推 main，CI 八 job 全绿（m511b 记档）**。实机脚本七条见 DEVLOG m511（1.20.1 可见变化：跨组线归并+×N、机器线粗细随缩放到封顶、产出线紫/供料线柔绿、整个画布屏切画布配色）。**教训："世代差为零"要连坐标系与取色域一起查——三处差异都不在代码 diff 里，在调用层（pxScale 传给谁/颜色从哪读/静态作用域谁开）。****下一刀：A6 缩放平滑动效 m186（m511 CI 已读全绿）；新登记 A9 悬停聚焦 m164b（1.20.1 无）。** 上笔 m510：**修构建失败**修构建失败——主线编译错潜伏 28 刀（m481 起）+ 1.20.1 判官九条恒失败（m473 起），CI 红灯三十多刀没人读；立第 21 闸**。m509 推完轮询 CI 三 job 红，回溯 60 次 run：**CI 最后一次全绿是 m472**——1.20.1 GameTest 自 m473 红、主线编译自 m481 红，此后次次红着推；沙箱拿不到 artifact 但 CI 早把错误清单回推到 `ci-mojmap-errors`/`ci-retro-errors`/`ci-gametest-report` 三个分支（raw 域可读），本刀从那里读出来的。主线唯一编译错=`probeChainWants` 给七参 `chainWants0` 只传五参（m481 立探针时按 1.20.1 五参口径写的），修=补 `probeOutT()`（m179/m355 同款两趟算法，计划新鲜直用否则现算）+ 备忘表现配；**为什么全部冒烟都抓不到：`(Level) level` 类型不可解析时 javac 不做重载适用性判定，这类错根本不报（第六类盲区，前五类是"报了没人看"）**→ **第 21 闸 `tools_arity_check.py`**（单声明方法裸调用实参数对表，227 文件/2561 处零误报，尺子修三轮、血案重放红/复绿自证，挂 CI）。1.20.1 九条判官**全是判官写错不是业务错**：六条同根因（`addNode` 存副本，判官加节点后改本地引用 tag=改孤儿栈→加 `add(c,s)` 助手取回图里那份）；exact_pull 前提随 m486 真移植失效（主线 chainEndsInTrash 尊重过滤器名单→放行剑 id）；distributor 拿本就被收的骷髅头验"没人收"→换烈焰棒；tick_lamps 熔炉族期望 m494 漏更新→红灯；stack_key 用钻石剑验"无 tag"→1.20.1 构造器自带 `{Damage:0}`→换石头。**教训：读 CI 是每刀收尾固定动作（fetch 三分支）；判官写完 CI 没绿之前不算存在。** 验证=两代冒烟零真错、21 闸全绿（0.1.510），主线业务方法体零改动。**m510 CI 首跑：主线编译绿（m481 以来第一次）、主线 GameTest 全绿（m481 以来第一次真跑）、1.20.1 九条转绿八条；m510b 热修最后一条（判官把搬仓去处指回源仓，泵与清运同拍拍末恒 100→另建 dst 仓+守恒断言）。下一刀：确认 CI 三 job 全绿（m472 以来第一次）→ 回 A4。** 上笔 m509：**A5c 补刀——画布右键菜单机制两代共用（xplat `client/CanvasMenu`），m508 那份 1.20.1"最小菜单件"退役换共用件 + 第 19 闸并入菜单贴图路径普查**。来路如实记：本会话按作业表取 A5c 时 HEAD=m507，先把菜单机制搬成共用件准备第二刀落交互，推送被拒——另一会话已并行把 A5c 推成 m508（交互面照主线逐句落地，菜单写了最小件记"工艺随 A7"）；rebase 到其上改成补刀：主线 m148 3A 菜单机制整段搬（状态十一件+三常量+openMenu/clearMenu/addMenu×4/mt+renderMenu+点选分派块），机械替换只四类（workRight/height→形参、this.font→形参、NODEFRM/CYAN/TXT→SciSkin 定义、mt 的 fromNamespaceAndPath→新增 `SciSkin.gfxTex` 世代口），**四段归一后与原文 diff=0**；主线屏机制退役为同签名转发壳（几十处 addMenu 装配点零改动，3164→3038 行）；1.20.1 最小菜单件约 50 行整段删除换同名同签名转发壳（A7 装节点菜单条目可直接粘主线原文），组菜单/画布菜单改主线原文形态（m313 贴图图标/物品行图标/危险项/取消组首），顺带补三处主线 modal 口径（菜单开着吞滚轮/拖动、悬停详情不画），两张 group_*.png 拷进 1.20.1 资源目录。第 19 闸并入「菜单贴图路径普查」（按世代扫屏里 `mt("…")` 逐张对资源目录，两路自证红/复绿），另登记两屏 25 个已删符号。验证=tools_smoke 两代全量+四改动文件单编零真错、自家新符号 0、diff=0、20 闸全绿（0.1.509），纯 UI 零协议零存档改动。**协作教训：多会话并行取活，作业表「下一刀」不是锁——push 前必须再 fetch 对表**（这次靠 git 非快进拒绝兜住）。**下一刀：A4——连线归并徽章 m193（作业表原文不变）。** 上笔 m508：**A5c——1.20.1 分组客户端交互落地，分组分片收官 + 冒烟第五类盲区：javac 类体归因看文件顺序**。按作业表取活。交互面照主线 `StructureCoreScreen` m192 事件段逐句对着写进 `CanvasScreen120`（状态字段原名、mouseClicked 三段/mouseDragged 两段/mouseReleased 两段/keyPressed/charTyped/六助手全部主线原文，`be()`→`g`、`ClientNet`→`ClientNet120`+m506 两包，1.20.1 API 逐项 yarn 核过不猜）；两处本世代填空=最小右键菜单件（列表形状照主线，工艺随 A7 换）与画布菜单「重置视角」。共用件再进两件：选中描边/框选矩形搬进 `GroupFrameRenderer.drawSelection/drawSelectBox`，三件绘制共用 `pushWorld`；主线调用点挪到 popPose 后像素同位。1.20.1 `wnx/wny` 拖动覆盖统一（单卡幽灵+组成员快照+增量），顺带修机器线拖卡不跟。**第五类验证盲区**：把主线改动的 `gv` 故意改坏冒烟报 0——javac 无 MC jar 时**类体归因取决于源文件顺序**，`find` 无序清单下 `NodeUpgrades.java` 排在主线屏前面，主线屏整类 3100 行零 Attr 错，78 个文件（全部主线屏、大半物品/包类）少归因（5493 vs 排序后 7633）；should-stop/compilePolicy 开关全无效；1.20.1 清单一直排过序所以没中招；**单文件单编归因完整**（对拍 572 vs 77，探针自证红/绿）。固化 `docs/tools_smoke.py`（两代全量排序清单 + git 改动文件逐文件单编，内部走 m491 筛选器），作业表第 6 步/HANDOVER 守则改指向它；主线 189 文件逐文件全扫补历史盲区（结果见 DEVLOG m508）。**教训：源文件顺序也是量程的一部分；尺子量到 0 先问它有没有量到这里。**验证=tools_smoke.py 两代全量+三文件单编零真错、自家符号 0、20 闸全绿（0.1.508），零配置零协议零存档改动（纯客户端 UI + 两包首次发出）。实机脚本见 DEVLOG m508（1.20.1 分组第一次完整可玩：Shift 选/框选→G 建组→拖标题带→右键重命名/解散→摘成员自动解散）。**下一刀：A4——连线归并徽章 m193（先逐句对主线 626~760 归并段与 1736~ 锚点助手，切前看区间）。** 上笔 m507：**A5b——画布分组组框渲染两代共用，1.20.1 第一次画出组框（只读不写）+ 顺手修 1.20.1 命中框 24×24 残留**。逐句对主线 m192 组框段与 groupMembers/groupRect，**世代差为零**（fill/hGrad/drawString+算术，translate(DDD) yarn 核过在位），与 m505 普查稿「客户端不可共用」相反——那句只对 A5c 交互面成立；照 m484/m488/m490 路数整段搬成 xplat `client/GroupFrameRenderer`（GPAD/GBAND/groupMembers/groupRect/drawFrames），五个读口收进 View（坐标记法照 MinimapRenderer.View），三段归一后 diff=0。唯一参数化的真世代差=View.cardHeight（主线 NH+26 含升级格行 / 1.20.1 无升级系统=NH）。主线渲染段换共享表装配+一行 drawFrames（nGid 留主线喂 m193 归并），两几何助手改转发三处调用点零改动，3177→3164 行。1.20.1 `CanvasScreen120` 加 groupView、renderBg 最底层插组框（受总开关），groupNames 读快照像（m506 下沉后随 CanvasSnapshot 原样到）。**顺手修 1.20.1 `nodeAt` 命中框 24×24 残留**（卡 m484 起已是 100×52，只有左上角能悬停/拖/右键）→ NW×NH 乘 zoom 整卡命中。判官零新增（client 类服务端判官不可载，全库零先例）。验证=两代冒烟零真错、自家符号 0、diff=0、20 闸全绿（0.1.507），纯 UI 零协议零存档改动。实机：主线组框应与之前逐位一致；**1.20.1 组框要等 A5c 建出组才看得见**，本刀 1.20.1 唯一可见变化=整张卡任意位置可悬停/拖/右键。**下一刀：A5c——1.20.1 分组客户端交互（框选/G 键/组菜单/拖组/重命名小窗），接 m506 两包，这半才是真不可共用。** 上笔 m506：**A5a——画布分组服务端下沉，六方法两代共用 + 1.20.1 写通路 + 顺手修第 19 闸两处坏尺子**。按作业表取活。逐句对主线 SCBE 六方法（createGroup/dissolveGroup/renameGroup/moveGroup/setNodeGroupTag/sweepGroups）与 clampCanvas，零 1.21 专属 API，**整段搬进 xplat `node/CanvasGraphState`**（数据本就住那儿），机械替换只三类（`g.` 前缀去掉 / `setChanged+syncToClient`→构造器注入的 `onChange.run()`（m485 手法，无参构造=空回调保客户端像零改动）/ `sweepGroups` 放开 public），**归一后与原文 diff=0**；SCBE 四公开方法退役为一行转发、两私有助手删除、detachNode 改调 `g.sweepGroups()`、clampCanvas 留垫片（3906→3847 行）。1.20.1 写通路：`NodePayloads120` 追加 NodeGroup（一包三义）/NodeGroupMove 两包（有界红线走 Net120 两口），`StructureCoreMenu120` 接收器按主线 m191 接收器原文顺序（总开关→尺寸熔断→前验→分派→推快照）+ 可测操作核 `groupOp`，`RetroBootstrap` 注册，`StructureCore120` 的 g 传 `this::setChanged` 并在 detachNode 补 `g.sweepGroups()`。零新数据结构零读协议（groupNames 与栈上 gp 随现有快照通道到客户端）。第四张跨代契约 `CanvasGroupAssertions`（八条判定，压「同一份代码×两套栈实现」），判官 +5（主线 2 / 1.20.1 3，含两包往返与解码期拒收 4097 条/200 字）。**顺手修第 19 闸两处坏尺子**：①登记表 dict 重复键静默覆盖（m485 那批已删符号自 m504 起没在查）→合并+重复键自检；②声明正则太松把行首裸调用当声明→**语句级裸调用已删方法恒不红，正是 m504 exactIdxAppended() 潜伏的形状**→改「类型 名字」成对+认 class/interface/enum/record；三步自证含 m504 血案重放当场红。**教训：闸的自证要用它声称能防的那次血案重放**。验证=两代全量冒烟零真错、自家符号 0、20 闸全绿（0.1.506）、零配置零资源改动，**动了两代 detachNode 与主线全部分组操作路径（存档 gp 键/groups 表，两代同名同布局双向兼容）**。实机脚本见 DEVLOG m506（主线建组/拖组/重命名/解散/摘成员自动解散/关配置拒收；1.20.1 只需确认画布与存档照旧——客户端 UI 随 A5b）。阻塞区新增一条世代差待拍板：1.20.1 单节点拖动钳 ±1e5 vs 组位移 ±1e6。**下一刀：A5b——1.20.1 CanvasScreen120 分组客户端读+组框渲染（只读不写）。** 上笔 m505：**A4/A5 小普查（零代码笔）——厘清依赖方向，发现服务端分组业务可真移植**。B 线业务代码 m504 收官后转 A 线，作业表里 A4（归并徽章 m193）与 A5（分组框选 m192）互相把对方列成依赖——主线代码给了答案：m193 归并的锚点算法依赖 m192 算出的"分组共享表"，**A5 先于 A4，作业表原表述是笔误**，已订正。**意外发现**：服务端分组业务是又一次 B 线故事——`CanvasGraphState.groupNames`（m477）与 `NodeTags.nodeGroup`（m472）早就共用，本世代已有同一个 `g` 字段；真正没共用的六个业务方法（createGroup 等，约75行）逐句读下来零 MC 1.21 专属 API，与 `StorageLedger` 的 `Runnable onChange` 解耦手法同一个形状。同步机制两代不同（主线观众推送 vs 本世代客户端拉取）但那是执行面差不是数据差，两代现有快照通道原样能带分组数据，**服务端真正缺的只是"客户端发起写请求"这一条通路**（新网络包+接收器）。**客户端才是真正的大头且不可共用**：Screen/GuiGraphics API 两代差异大，主线 m192/m193 代码高度耦合 Screen 类自己的状态字段，且大多"夹在"更大的通用方法里（`connectedComponent` 等就恰好夹在分组辅助方法区间但不是分组专属——m487 血案同款教训，行号盲切前必须先看区间里有什么）。粗量两代客户端代码 300~450 行，本世代要新写。产出 `docs/画布分组归并1201_A4A5小普查_m505.md`：依赖订正+真移植可行性判断+区块行号粗量+四刀分片（A5a服务端下沉→A5b客户端读+组框渲染→A5c客户端交互→A4归并徽章）+各刀验收口径。作业表 A5 拆成 A5a/A5b/A5c 三行。验证=零代码改动，20 闸全绿。**下一刀：A5a——分组服务端下沉（六方法进共用件+网络包1.20.1对位+接收器）。** 上笔 m504：**B5——FTA 事务包装两代共用，B 线业务代码整体收官**。逐句对起点：`StorageCore120.java` 账目"剩 309 行"，但普通账/精确账/类型闸/修订号 m485 早就共用，真正双写只有 `FabricLedger`/`FabricLedger120`（Fabric Transfer API 的 insert/extract/iterator/View）——业务逐位相同，差异只有分流判据（世代口 `ItemData.has`）与 id 物化防御写法（本世代 `tryParse` 更安全）。**意外挖出潜伏的真编译错**：主线 `FabricLedger` 里 `exactIndexOf`/`exactIdxAppended`/`exactIdxRemoved` 六处调用漏了 `ledger.` 前缀——m485 账本下沉时这三个方法整段搬进了 `StorageLedger`（后两个还降成 private），调用点没跟着改，孤立编译（`-Xmaxerrs` 绕开截断）实锤 `cannot find symbol`。**两层安全网都没接住**：冒烟筛选器（m491）故意把"自家符号找不到"让位给第 19 闸，但第 19 闸当年没登记这个文件，潜伏了十几刀（git log 确认非本会话引入）；本世代因改走 `markIndexDirty()` 全量重建侥幸绕开（O(1)→O(n) 的性能退化，逻辑结果等价，判官测试从不关心索引怎么维护）。**返工一次**：第一版把整个 `FabricLedger` 搬进了 xplat，被 `layer_gate`（第13道分层硬闸）当场拦下——**xplat 见 MC、不见加载器**，FTA 的 Storage/ItemVariant/TransactionContext 本身就是加载器类型，哪怕两代都是 Fabric loader 也不例外（`Xfer.java` m434 早是先例：零 FTA 符号的 Impl 接口+Object 句柄）。重新设计：`StorageLedger` 新增 `ftaInsert`/`ftaExtract`/`ftaAmount` 三个**不出现任何 FTA 类型**的业务核对方法（`ItemStack`+`List<Runnable>` undo+`Runnable beforeMutate` 回调跨边界，`beforeMutate` 对应原 `updateSnapshots(tx)`，必须在"确定真的要改"那一刻精确调用）；两代 `FabricLedger`退回真正的薄壳（FTA生命周期钩子+类型转换+iterator物化，一行转发业务判断给 `ledger.fta*`）。**教训**：判断"能不能下沉"不能只看代码长得像不像，还要看依赖的类型属于哪一层——业务逻辑与它依赖的具体接口类型要分开看，后者若是加载器类型，哪怕写在同一个方法里也不能下沉。两代壳收尾：主线 442→371 行（薄壳，业务判断挪去共用件）；本世代 308→238 行。**双写闸账目：业务双写 309→0 行**——**B 线（数据面板B3/数据线B4/存储核心B5）三大块业务代码层面全部真移植完成**，只剩 4 条判官同名用例本身留给 D 阶段合一（不影响业务账目）。开工时（m477）账上 2226 行业务双写，现在归零。顺手：`hopperDockAccepts` 同一批漏前缀的第七处裸调用补上；本世代一段死代码残留注释清除；第 19 闸补登记两文件删除的符号防未来回归。验证=孤立编译精确复核（六处裸调用全消失）、两代全量冒烟零真错、分层硬闸绿（139 xplat 文件零加载器符号）、第19闸四项全绿、双写闸0行、20 闸全绿，**动了两代存储核心 FTA 直连路径（第三方管道模组存取）**，事务判断逻辑未变只是搬了地方。实机脚本=①管道模组存取普通/精确件 ②高频小额存取性能不劣于合一前（主线其实是变相修复）③事务回滚普通/精确账目应还原 ④嵌套事务内提交外回滚精确账目整体还原索引懒重建仍命中 ⑤天量长整试探性insert不溢出 ⑥主线漏斗贴底存入禁抽取 ⑦本世代旧档读入逐条恢复非法条目静默丢弃。**下一步：B 线业务代码已收官，剩 4 条判官同名用例的 D 阶段合一（另立里程碑，非阻塞）；主线 Gradle 构建从 m481 起没真构建过这条阻塞仍在——这次挖出的 FabricLedger 真编译错就是实证，强烈建议作者尽快跑一次真实构建。** 上笔 m503：**B4b——数据线方块层下沉，`CableEnd120` 退役，B 线数据面板/数据线两大块全部收官**。逐句对两代 `DataCableBlock`：属性表/碰撞箱几何/三态判定核心（`endFor` 判定树）逐位相同，真差异只有CODEC/方法可见性（版本 API 差，override 没法下沉）与主线专属 m233/m229（B4a 同款默认关钩子接住）——又一次没有真差异住在业务里（B 线第七刀）。`CableEnd`/`CableEnd120` 两枚举本就该是一份（序列化名逐字同值，零版本 API），`CableEnd120` **整个文件退役**，1.20.1 直接用 xplat 的 `CableEnd`。新共用件 `CableEndCore`（xplat/block）收下属性表/几何数据/`shapeFor`/`classify` 三态判定核心；`classify` 的断开/出售钩子跟 B4a 的 `ExtractPort.Host` 同一副面孔（第三次用同一套"主线专属功能默认关"手法）。**"自家网络方块名单"刻意没跟着下沉**：第 16 闸靠抓 `endFor` 方法体里的 `前缀.常量` token 文本防漏抄（m469 血案），若把名单判断包进新方法闸会抓到 0 条直接报错——两代 `endFor` 因此保留"局部变量判布尔+一行转发 classify"的极简形态，名单字面一个字没挪窝，**本刀特意验证过第16闸改动前后抓取内容逐位不变，不是靠文本巧合虚绿**。两代壳收尾：主线 `DataCableBlock` 153→118 行（NORTH 等保留为转发字段，外部引用点零改动）；`DataCableBlock120` 202→161 行并降为世代壳。**账目：业务双写 512→309 行**——**B 线数据面板与数据线两大块全部真移植完成，只剩 `StorageCore120` 剩余 309 行**。验证=两代全量冒烟零真错、第16闸单独复核判据真实有效、第19闸补登记两方块类删除的十三个符号、20 闸全绿、零配置零资源改动，**动了两代数据线方块状态判定与渲染取值路径**。实机脚本=①摆放邻接自家三大方块应各伸插头、缆对缆连续无接头盒 ②贴容器/第三方FTA存储伸插头 ③转弯外观碰撞箱视觉一致 ④链接器断开某面后该面缩回、恢复后伸回；贴转化桌伸插头 ⑤旧档自愈：陈旧NONE态进档首拍自动补插头 ⑥本世代三种交互（循环三态/持物白名单/潜行清空）反馈文案不变。**下一刀：B4 收尾唯一剩余项——`StorageCore120` 剩余 309 行下沉（对位 block/StorageCoreBlockEntity 账本核心）。** 上笔 m502：**B4a——数据线抽取口业务下沉，`DataCable120` 降为世代壳，`RetroXfer` 兑现五口门面**。先逐句对再问「差异住在哪一层」（m501 教训第一次当工序跑）：两代数据线 BE 的业务（m225 逐模板抽/全部模式游标轮转/m231 回收拍/m228 六面视图/m218b 核心 40t 缓存）**逐位相同**，真差异三类——①transfer 触点（主线 Xfer 门面 vs 本世代内联 FTA）②主线专属功能（m229 转化桌/m230 升级/m233 按面断开）③附加数据判定（组件 vs tag），分别住在**加载器层/功能层/世代口**，B 线第六刀差异六比零没有一个真住在业务里。①的解法=**作废 m440 旧结论**：「本世代不设 Xfer 门面」是仿写路线的取舍，m434 接口化注释本来就预言"1.20.1 格照此同款"——新建 `RetroXfer`（FabricXfer 原文照搬）在 RetroBootstrap 安装。**教训（m483 同型第二犯，立规矩）：今后凡引用 m4xx 排刀稿结论，先问一句「它预设的路线还在吗」。**②=带默认值的宿主口（ExtractPort.Host 四钩子默认关，主线 PortHost 兑现、sellOverflow=原 insertInto 卖桌尾段原文）；③=ItemData.has + StackKey.same（m502 新增静态出口，本世代实现即原 isSameItemSameTags）。共用件 `ExtractPort`（xplat/storage 约 240 行，零 fabric import）收下实例态七件与八个方法全部主线原文。白捡：本世代得 extractAll 的 tryParse 防御（坏档怪 id 不再炸拍，m443 账本同款）；主线 NBT 键逐字节不动、本世代键同名同布局——**存档双向兼容零迁移**。两代壳收尾：主线 BE 323→211 行、本世代 288→143 行并降世代壳。**账目：业务双写 801→512 行**；B 线只剩 B4b 数据线方块（203 行）与 StorageCore120 剩余 309 行。判官零新增：本世代五条数据线判官原样跑在共用代码上（判官钉不变量的红利第三次兑现）。验证=两代全量冒烟零真错、20 闸全绿、第 19 闸新登记两代数据线删除符号清单、零配置零资源改动，**动了两代数据线抽取/回收路径（碰账本、邻接机器与 EMC）**。实机脚本=①贴箱开抽按游标轮转进箱 ②白名单持物右键只抽该件 ③回收模式从 Create 传送带收货、tag 件入精确账本 ④目标满余量回账本绝不落地 ⑤主线专属三件照旧（贴桌溢出卖 EMC/升级槽/按面断开）⑥两代旧档开关/方向/白名单原样恢复。**下一刀：B4b `DataCableBlock120` 下沉（203 行，对位 xplat/block/DataCableBlock 153 行；endFor 名单有第 16 闸对表看着）。** 上笔 m501：**B3 收官——取物回账下沉，`DataPanel120` 降为世代壳**。m500 记档时把 `serverTake` 的逐句对结论写成「两代**不变量完全相同**，差的是循环结构与申报策略」，**刻意没写「不是照抄能解决的」**（m473/m475 栽过那跤）；本刀真去搬，验证了那份克制是对的。**差异的正确切法**=两代的差不在「取」而在「申报」：主线客户端发**档位下标**（1/8/16/32/64/2组/4组/8组/填满，服务端按 maxStack 算 want），本世代客户端发**数量**（服务端按「一请求至多九栈」钳位）——**协议形状差不是业务差**；于是共用件 `PanelAggregator.takeInto` 只吃**算好的 want**，档位算法留主线调用点、九栈钳位留本世代调用点（作业表阻塞区那条 A/B 走 A：不动玩法手感、不动协议）。搬进来的是主线 m82/m100 原文（分块→跨核心累计取→`Inventory.add`→余量 `deposit` 原路回账本（m130 自动分流回精确账本，不混堆不变裸）→仍非空则 `player.drop` 双保险→见底即停），m112 客户端保险丝随循环一起搬（改判 `player.level().isClientSide`，它才是这条链的权威侧）。**本世代白捡两处加固**（按堆叠上限分块取／余量回不进账本时 drop 双保险），**主线白捡一处**（分块的 maxStack 从 `Item.getDefaultMaxStackSize()` 改成 `ItemStack.getMaxStackSize()`——m163c「动态堆叠上限走栈不走 Item」在此处一直没落实）。**`DataPanel120` 在双写闸里降为世代壳**：只剩 BE 开屏工厂（`ExtendedScreenHandlerFactory` 两代签名不同）、虚拟列表菜单 `PanelMenu120`（主线是 54 展示槽+合成网格 handler）、展示栈物化、申报量钳位、payload 处理——都是协议侧该各写的。**账目：业务双写 978→801 行（B3 整刀 991→801）**；B 线只剩 `StorageCore120` 剩余 309 行与 B4 数据线两件（492 行）。判官 +1 `panel_take_spans_multiple_cores`（两台核心各 40/24，一次取 64 应取满且两边扣净）——**它钉的是不变量不是形状**，合一前后两种形状都该绿；钉形状的判官会逼着你把已合一的代码拆回去喂它（m486 第一批对表闸退役同理）。**教训（把 m495 往前推一格）**：m495 说「不要从行为描述推代码差」，本刀补后半句——**逐句对完发现差异之后，还要再问一句「这个差异住在哪一层」**；`serverTake` 的差异看着在业务层（循环结构都不一样），实际住在**协议层**；判据很简单：**把差异挪到调用点之后，剩下的还对得上原文吗**——对得上就说明差异本来不属于这段业务。B 线五刀下来，差异分别住在「兜底口」「灯表」「参数」「协议」，**没有一次真的住在业务逻辑里**。验证=两代冒烟（按 m500 新口径的全量源集）经筛选器复查均无必须看的真错、第 19 闸四项全绿、20 闸全绿、零配置零资源改动，**动了两代面板取物路径（碰账本与玩家背包）**。实机脚本=①九个档位逐个点，到手数与账本扣减逐个对得上②**背包只留一格空**点「填满背包」：只进得去那一格的量且**账本余量分毫不差**（绝不落地绝不消失）③跨核心取：两台各放半组，取一整组应一次取满两台都扣 ④精确件取附魔书保附魔、背包满时余量回仓仍是精确条目⑤本世代客户端申报天量（改包）应被钳在九栈内 ⑥主线 shift 从展示区取物（本刀未动）照旧。**下一刀：B4 `DataCable120` + `DataCableBlock120` 下沉（492 行），先逐句对。** 上笔 m500：**B3 前半——面板聚合两代共用 + 顺手修掉一条主线真编译错**。按作业表取 B3，开工先逐句对：本世代 60 行 `snapshot` 是把主线**两步**（BE 出全量快照 `masterEntries` → handler 出分页 `repage` 前半）压扁成一个方法的仿写件，循环体/排序意图/服务端钳位口径本就同构；**真差异三处全是「本世代少了主线的加固」**（①精确条目跨核心不并账——两台核心里各一本同款附魔书会分两行 ②排序尾键缺两级=同量同 id 时序不定会抖 ③大小写归一**这处反而本世代对**：`Locale.ROOT` vs 默认 locale，土耳其 locale 下 `"I"` 小写成 `ı` 会打断 id 匹配），**一处世代取舍都没有**。落地=新建 `xplat/storage/PanelAggregator`（`entriesOf` 聚合+并账+m83 存量降序 / `page` 过滤+钳滚动+开窗 / 四口聚合读写门面，主线原文整段搬含注释刀号），两代各删各的，三处加固自动推广并配判官一条。**核心列表直接吃 `StorageLedgerProbe` 不另开接口**——写到一半发现两代存储核心早就实现了 m480 那个探针，**两代零 implements 改动**，这是 m480 的口第二次被白捡（第一次 m498 拉料 bank）。**第十三个世代口** `StackKey.Kind.dataOrder`（`MASTER_ORDER` 尾键用 `getComponentsPatch()`，m487 血案同款符号）。**刻意不搬**：展示栈物化（协议形状差）与三套缓存（只有主线有观众态，m480「不为接口好看搬东西」同律）。**顺手抓到一条主线真编译错（本刀最该看的部分）**：冒烟筛选器报 9 条必须看——`SdzjzGameTests` 在 `com.sdzjz.gametest` 包里直接 `be.g.machineNodes`，而 `StructureCoreBlockEntity.g` 是**包私有**，**主线根构建会红**；先跑基线对照组确认不是本刀捅的（m481 那条 `route_domain_contract` 判官带进来的），按取活规则第 1 条插队修掉（与邻居 `prof` 同口径放开，不为判官造新访问器）。**教训：尺子的量程也是尺子的一部分，而「源集」就是冒烟的量程**——前几刀我报「无必须看的真错」量的是更窄的源集（改哪代跑哪代），于是这条错在 m481~m499 十九刀里没人看见；m471 撞过一次（javac 只报前 100 条），**同一把尺子换了量程就是另一把尺子**，冒烟源集自此两代都按各自 build.gradle 真实源集全量跑。这条也说明主线根构建至少从 m481 起就没被真正构建过（近期反馈都在 1.20.1 侧），D2 那条欠账今天兑现了一次。**账目：业务双写 991→978 行，数字小要说实话**——真删的业务双写约 55 行，但闸按整文件行数计账，`DataPanel120` 剩下的是 BE 开屏三方法/`PanelMenu120`/payload 处理这些真·世代壳加一块 `serverTake`；**B3b 收完 `serverTake` 这个文件才能整体降世代壳、190 行的账才归零**。`serverTake` 逐句对结论=两代**不变量完全相同**（取多少给多少、塞不下原路回账本绝不落地），差的是循环结构与申报策略（主线按 maxStack 分块+按钮档位 vs 本世代按核心遍历+九栈钳位），**按 m495 教训不写「不是照抄能解决的」**，B3b 再对一次；申报策略若要统一属产品口径，已问作者。验证=两代冒烟经筛选器复查均无必须看的真错（主线由 9 条→0）、第 19 闸四项全绿、20 闸全绿、零配置零资源改动，**动了两代面板展示路径与主线取物读账路径**。实机脚本=①两代面板排序与之前一致（存量降序/同量按 id 稳定/不抖）②**1.20.1 重点**：两台核心各放一本同款附魔书应显示**一行合计**（合一前分两行），异款另起一行③搜 `cobble` 大小写混打应同时命中裸件与 tag 件 ④滚到底再滚行号被服务端钳住 ⑤shift 入仓照旧（精确件不混堆不变裸）⑥主线批量取出/清空回仓/m289 摘要照旧。**下一刀：B3b——`serverTake` 取物回账下沉，收完把 `DataPanel120` 降为世代壳。** 上笔 m499：**B2 收官——逻辑节点六分支下沉，StructureCore120 降为世代壳**。m496 逐句对过六分支，当时结论是「同构，唯一差异是主线 distributeEven 为 void 而本世代要拿剩余量回灌」；m495/m496 把分发与均分下沉、**统一成都返回剩余量**之后（主线兜底恒回 0），这段就再没有拦路的差异了，本刀下沉。六分支逐句照原文（5t 清运节拍/m350 整锅转存再清/m150 垃圾桶吞光并累计已吞/**m153 抽取节点残量留缓存=背压**（不亮黄，与其余五族不同，原文刻意）/开关与传感器关闸的持料待命词条）。**灯表刻意留在世代口**：两代口径本就不同（本世代 m473 加了「持料待命」黄灯因为它没有输出缓存、货真会停在缓存里；主线有输出缓存不需要这条）——**这是真实世代差不该硬统一**，收进 Host.lampAfterDrain。**StructureCore120 从 991 行降到 837 行并在双写闸里正式降级为「世代壳」**（路由脑 m486/分发 m495/均分 m496/拉料 m498/六分支 m499 全部走共用件，本文件只剩 BE 注册·存档签名·tick 编排骨架·宿主面·机器生产分支）；**账目：业务双写 1829→991 行**。**B 线主体至此收官**（开工时 m477 账上 2226 行，现 991，其中 309 是 StorageCore120 剩余、其余是数据线与面板 B3/B4）。**一条贯穿 B 线的观察**：m473/m475 我曾断言执行面「不是照抄能解决的」，之后 m495/m496/m498/m499 连着四刀逐句对，**每一次差异都只是参数化能收的东西**——四次之后可以下结论：**「两代行为有差异」几乎从不意味着「代码没法共用」**，差异通常只住在很少的几行里，把它们收进口，剩下的就能整段共用；这条对 B3/B4 同样适用。验证=两代冒烟经筛选器复查均无必须看的真错、第 19 闸四项全绿、20 闸全绿、零配置零资源改动，**动了两代逻辑节点清运热路径**。实机脚本=①六族逐个过（分配器均分/过滤器放行拦下/垃圾桶吞光涨已吞/抽取开关两态/传感器关闸持料）②**抽取节点残量应留缓存且不亮黄**（背压，与其余五族不同）③下游全满时其余五族亮「持料待命」黄灯且缓存数字可见④退出再进在途量不变。**下一刀：B3 DataPanel120 下沉（190 行），继续逐句对。** 上笔 m498：**B2 收尾——拉料循环下沉 + 第二批对表闸退役**。照纪律逐句对完主线 m92 拉料段与本世代 pullSupply：**差异六处全是参数化能收的、无一是「逻辑不同」**（泵速倍数/精确账本源/出线目标/饱和加法类名/合成机需求缓存/m218c 移相），全收进 SupplyPuller.Host，其余逐字相同——**包括 m157 那段「猪人塔/幽匿线产物消失」的血案注释**。**白捡了 m480 的东西**：精确账本 bank 直接用 StorageLedgerProbe（建它时是为跨代行为契约，这里发现两代存储核心都已实现、exactTemplates/withdrawExact 现成）——**为 A 目的建的抽象在 B 处白捡**，真移植攒基础设施的复利。**第二批对表闸退役**：下沉完第 16 闸当场红报「拉料五族清单漏抄 5 条」，因为 m475 立那条时两代各写一份靠尺子追平，现在两代跑同一份代码**「漏抄」按构造不存在**；摘除并记档。**规律浮出来了：每当一块业务真移植完成，为它设的对表项就同时失去对象——对表闸的萎缩速度，正好是真移植的进度条**；剩余对表项只剩「已建方块名单」这类真·两代各写的东西。账目=业务双写 **1964→1902 行**；B 线主体（图状态/身份键/账本/路由脑/分发/均分/拉料）至此全部下沉，StructureCore120 剩下的是 tick 编排骨架与世代壳。验证=两代冒烟经筛选器复查均无必须看的真错、第 19 闸四项全绿、20 闸全绿、零配置零资源改动，**动了两代拉料热路径**。实机脚本=①仓→金线→过滤器(白名单沙)→耗料机：沙被拉走名单外不动②抽取节点三态（默认关不抽/开了没去处仍不抽 m157/接产出仓搬仓全抽）③仓里带附魔剑+链尾垃圾桶：剑被抽走销毁，链尾换别的机器则一件不动（授权闸）④泵挡位调高缓存上限应随速率放宽（m163a）不再卡 4096。**下一刀：B2 真收尾——tick 编排骨架（预算/节拍/邻接派生位/灯表）逐句对，然后把 StructureCore120 在双写闸里降级为世代壳。** 上笔 m497：**修 m494 的构建失败 + import 完整性普查（第四类盲区）**。作者回报 1.20.1 构建失败 3 个错误=「找不到符号: 变量 Items」，m494 那两条熔炉族判官用了 Items.RAW_IRON/Items.DIRT 而 RetroTickTests 没 import Items，补上。**为什么又漏了**：这是**第四类验证盲区**且形状与前三次不同——m487 漏的是「**已删**符号还有没有人用」→第 19 闸悬空引用；m488 漏的是「切段漏带闭括号」→括号配平并入；m491 漏的是「**类型错误**这一整类」→冒烟报告筛选器；**本刀漏的是「新写的代码用了本文件没 import 的类型」**——它在 javac 输出里是 cannot find symbol，而 m491 定的筛选器判据**把这一类一律归噪音**（缺 MC jar 时上千条，不归噪音工具没法用），**于是这个真错正好落在两把尺子中间的缝里**：筛选器认为它是噪音、第 19 闸只管已删符号，没人管它。补法=第 19 闸并入**import 完整性普查**，判据刻意做窄以求零误报（只认一张常用 MC 类型白名单，用了 Xxx. 或 new Xxx( 却既没 import 又不是全限定名又不在同包又不是本文件自定义即红）；**自证方式是重放本次故障**（拿掉 Items 的 import 当场红并指到行号，补回复绿）。**第 19 闸现在管四件事**（悬空引用 m487/括号配平 m488/共用件资源 m489/import 完整性 m497），加上 m491 筛选器共五件，**共同点始终是同一个：都发生在「我的验证」与「作者的构建」之间那道缝里**。**值得记的是这次的形状变化**：前三次是「我的清单里没有这一类」，这次是**「两把尺子各自认为对方会管」**；工具多了之后漏洞不再只出现在工具内部，**也会出现在工具的交界处**——所以每加一把尺子都要问一句：**它和已有的尺子之间有没有缝？** 验证=两代冒烟经筛选器复查均无必须看的真错、20 闸全绿、零配置零资源改动。 上笔 m496：**B2 续——均分分发下沉，同一个形状的第二次兑现**。照作业表开工纪律办（m495 立的「先逐句对，不要从行为差推代码差」），这刀对的是**逻辑节点六分支**与**均分分发**：逐句对完六分支确认两代差异**仍然只是那一处**（主线 distributeEven 是 void——有输出缓存兜底永远送得出去所以 moved 无条件为真；本世代返回剩余量要回灌+亮黄灯），**循环体、5t 清运节拍、m350 整锅转存再清、灯表语义全部同构**；于是 distributeEven0 也按 m495 办法下沉=**均分主体共用**（m357 scratch 两遍法、share/extra 余数轮转、m270 类型上限拒收份额转兜底、BUF_CAP 封顶）**兜底走 Tail 口**；两代 evenOk scratch 同刀删除（移进共用件），删完跑第 19 闸确认无悬空引用。**这是 m495 那条教训的第二次兑现**：连着两块被我先前判为「执行面取舍不同、没法共用」的东西，真去逐句对之后**都只差最后三行**；**「行为描述不同」和「代码没法共用」之间没有必然关系**——前者说产品语义、后者看代码结构，中间隔着一层不能直接跨。验证=两代冒烟经筛选器复查均无必须看的真错、第 19 闸绿、20 闸全绿、零配置零资源改动，**动了两代均分分发路径（分配器节点热路径）**。实机脚本=①分配器接两台下游产物应两边各一半、**奇数时前一台多一件**（余数轮转）②其中一台下游类型已满时它的份额应转给兜底（主线进输出缓存/存储、1.20.1 落产出仓或黄灯持料）**不许堵在分配器缓存里**③拆掉全部下游余量应落产出仓或输出缓存。**下一刀：B2 剩余（tick 编排骨架 + 拉料循环），继续逐句对。** 上笔 m495：**B2 前置——产物分发下沉，「执行面取舍不同」原来只差三行**。按作业表取 B2，表里写着前置是「先统一两处世代取舍」；而 m473/m475 两次记档都断言「执行面两代取舍确实不同，**不是照抄能解决的**」——这刀真去逐句对了一遍，**发现那个断言下得太早**。**两代的 distribute0/routeOut 循环体逐字相同**（m150 两轮垫底、m270 类型上限跳过、BUF_CAP 封顶投喂、垃圾桶族第二轮才喂），**唯一分岔是最后三行的兜底**（主线 depositFor→depositOrBuffer→addOutput 输出缓存+断网喷射；1.20.1 depositTail 落 kind0 产出仓、拒收原样回吐给调用方亮黄灯持料）。**所以「统一世代取舍」的正确形状不是逼两代行为一致**（主线有输出缓存本世代没有，硬统一等于给某代加它不该有的东西）**而是把差异收窄到一个口**：新建 ProductRouter（共用两轮垫底与投喂）+Tail 世代口（兜底各实现各的）——**第十二个世代口**。顺带把 isTrashLike 收进共用件（m150 垃圾桶+m378 虚空处理器都是「最后去处」第一轮一律跳过），本世代那份重写删除、判定改走 defOf==Machines.VOID_PROCESSOR（m472 身份口），删完跑第 19 闸确认无悬空引用。**教训：「不是照抄能解决的」这种断言，下之前要先逐句对一遍**——m473/m475 我写那句时是**从行为描述推的**（主线有输出缓存所以执行面没法共用），听起来成立，但真去看代码不同的只有三行、相同的有二十行；**从行为差推代码差，会把可共用的部分一起判死**；这条对 B2 剩下的部分同样适用，下一刀先逐句对不预设结论。验证=两代冒烟经筛选器复查均无必须看的真错、第 19 闸绿、20 闸全绿、零配置零资源改动，**动了两代的产物分发路径（tick 热路径）**。实机脚本=①机器→下游机器出线：产物先喂下游缓存喂满后落产出仓②下游接垃圾桶+一台耗料机：**沙子应全进耗料机、垃圾桶饿着**（两轮垫底）③下游全满且无产出仓：主线进输出缓存、1.20.1 黄灯持料且缓存数字可见④两代产量对账。**下一刀：B2 主体（tick 编排/六分支/拉料/灯表），逐句对完再决定哪些能共用。** 上笔 m494：**C2-⑤d2 熔炉族 tick 落地——万能熔炉在 1.20.1 可玩**。**按作业表取活**（第一次完全自主取活没等作者指方向）：A 线主体已收尾、C 线除本项外全被「附魔定价 A/B」阻塞、B2 需先办前置，故取 C1。搬法照流程=主线熔炉族分支（734~789，56 行）整段搬进本世代 tick，两条料路逐句照抄（**有入线吃自己的在途缓存+遗留池**走 m350 转存 scratch 防虚账、**无入线吃 kind1 定向供料线**），capacity 算式/m149 白名单闸/SmeltPlanner.resultOf 判定/「无可烧材料」红灯词条全部原样。**本世代必须显式接线才取料**（主线原注照搬）=不做全局网络兜底，**防止把玩家存着的原木/圆石/粗矿悄悄全烧了**。**三处世代取舍显式记档**=①无并发/数量升级（m464），running/countLv 恒 1/0，capacity 只留「一组×周期数×熔炉族倍数（工程款×108）」②无输出缓存/断网喷射，去处满=原样回灌自己缓存+黄灯持料（m473 同律）③**主线此处 xpPool += 0.1*done（熔炼经验入核心池），本世代 xpPool 未建随 ⑤e 到序**（m476 普查早标了 ⑤d/⑤e 这处交叉）。**顺手拆掉一颗会崩服的雷**：熔炉族每拍都问 SmeltPlanner.resultOf→Platform.recipes()，而本世代**从没注册过配方域**，未注册时那口**直接抛异常**——不装就是「玩家一放万能熔炉就崩」；故建 RetroRecipeAccess 并**实装熔炼一域**（其余三域安静返回没有，随 ⑤d1 到序）；熔炼域世代差是**查对照表**得的（1.20.1 getAllRecipesFor 直接返 List<SmeltingRecipe>、id 走 getId()；候选三元组 {配方id,产物id,产量} 形状与 Legacy 逐位一致——pickStable 稳定选序靠它，错一位两代就选出不同配方）。**路由两面零改动**：accepts/chainWants 的熔炉族分支在 m486 下沉路由脑时已随主线原文一起搬来，配方域一装即生效——真移植的又一份红利：**功能到序时不用再改路由**。判官两条累计六十五（供料线路径端到端出铁锭且同仓泥土一件不动；不接线不取料红灯+m149 白名单只烧勾选的）。验证=两代冒烟经筛选器复查均无必须看的真错、20 闸全绿、零资源改动；**本刀装了配方域（新增运行期注册）、动了 tick 路径，实机重点验**。实机脚本=①仓里塞粗铁+泥土，金线供料+绿线产出仓→稳定出铁锭且泥土一件不少②不接线时红灯「未接存储/供料线」且仓里什么都不动③白名单只勾金矿→粗铁不再被烧④工程款熔炉产量应是基础款 ×108。**下一刀：B2 tick 执行面下沉，先办前置「统一两处世代取舍」。** 上笔 m493：**建作业表 + 节点悬停详情下沉（A 线主体收尾）**。作者点名「你能不能设计一个作业表啊，不要每次都让我让你怎么做」——合理且指出了我一直没解决的问题：**每刀结束都在等作者指方向**。**docs/作业表.md** 六节=①取活规则（修构建失败>作者当轮点名>当前分片收尾>无阻塞主线活>需拍板的）外加一张「**我自己能决定/必须问作者**」对照（技术实现·加闸改闸·重构命名自己定；玩法语义·跨代行为取舍·高风险主线手术时机必须问）②**每刀标准流程**（把 m483~m492 全部教训固化成 10 条 checklist：搬前先查世代 API 对照表、整段搬不重写、**连调用点一起搬**、切段别按行号盲切、注释里刀号一起搬；验证用筛选器不手写 grep、20 闸一条命令、版本与三份文档同刀更新、汇报说清实机预期与是否动存档路径）③下一刀④**待办四条线**（A 画布视觉/B 真移植剩余/C 功能补齐/D 欠验与债务）每项带依赖与验收⑤**阻塞区**（等拍板的我不猜）⑥已完成归档。建完立刻按表取活做了 **A3 节点悬停详情**：主线 m85 段整段搬成 NodeTooltip（机器名×数量/状态（核心停机优先）/**周期与基础产量**/**产出表前三项**/消耗输入提示），照流程先查对照表命中两处世代差（instanceof MachineItem→defOf、ResourceLocation.parse→Gfx.id）——**这次是查表不是猜**；1.20.1 原来只有「机器名+一行原因」，现在与主线同形并保留本世代特有的阻塞原因整句。**A 线主体收尾**（卡面工艺 m483→节点卡 m484→连线缎带 m488/m492→面板屏 m489→小地图 m490→悬停详情 m493），剩 A4~A8 是增强项，**按取活规则第 4 条下一刀走 C1（⑤d2 熔炉族 tick，无阻塞、一落地万能熔炉可玩）**。验证=两代冒烟经筛选器复查均无必须看的真错、20 闸全绿、零配置零资源改动、**不碰存档与运行时路径**（纯 UI）。**教训：「等人给活」本身就是一种成本**——前面十几刀我每次收尾都把决策抛回给作者，而其中绝大多数（先搬哪件·怎么分片·要不要加闸）本来就该我自己定，作者真正需要介入的只有两类：拍板语义取舍、反馈实机结果；**把这条边界写下来比每次临时判断可靠**。 上笔 m492：**连线锚点照主线原文重写——m488 我又是「自己推」而不是「搬」**。作者贴图：缎带工艺生效了（有光晕有弧度）但线落在卡片顶边外侧不在端口柱上。**根因**=m488 搬了 wirePath 的**画法**（129 行原文照搬那部分是对的）却**自己推了两处调用点的锚点算法**（写成「机器右缘→存储卡中心」），而主线原文这两处各有讲究我全漏了：①**m184 选缘看几何**（收料口在机器右侧就走右缘出线，在左侧走左缘——不是恒定右缘；我写死右缘，下游在左时线会绕背后大圈，正是主线当年修掉的毛病）②**存储卡的接口在卡底不在卡中心**（产出接卡底左收料口、供料从卡底右供料口下发，切线是**垂直**的 0,-1 / 0,1；我用了卡中心+水平切线所以线插到卡片外面去了）③**m352 柱心分高**（产出走出口柱心 NH/2-7、供料走进口柱心 NH/2+7，还要看 nodeDualSidePorts 配置；我两条都用同一个 y）④**pxScale**（主线画存储线传 1f 即屏幕坐标层调用，节点线才传 zoom；我两处都传 zoom）；节点↔节点那条同理漏了 fwd 选缘。修法=两处调用点全部照主线原文重写（连注释里的刀号一起搬）。**这是同一个错误的第三次**：m483 自己编状态灯点坐标→m484 整段搬 drawNode 纠正；m488 自己推锚点→本刀纠正。**「搬了函数体就以为搬完了」是个陷阱——调用点的参数怎么算，往往才是那些刀号（m184/m352/m197）真正住的地方**；以后搬一个绘制件**必须连它在主线的全部调用点一起看**，不能只搬定义。验证=两代冒烟经 m491 筛选器复查均无必须看的真错（**新工具第一次在日常流程里替掉手写 grep**）、20 闸全绿、零配置零资源改动。实机预期=绿线（产出）从机器近侧缘水平出去垂直向上接存储卡底部左侧，金线（供料）从卡底部右侧垂直下来水平接机器近侧缘，节点间紫线按上下游左右关系选缘，线不再插到卡片外面。**下一步：节点悬停详情浮层（UI 最后一件），然后回业务线。** 上笔 m491：**修 m488 的构建失败 + 冒烟报告筛选器——我的 grep 漏了整整一类错误**。作者回报 1.20.1 构建失败 2 个错误=int mcx = (m == dragIndex ? dragCx : nodeCx(m))，dragCx/dragCy 是 double 而 nodeCx() 返回 int，三元提升成 double 再赋给 int；m488 改连线锚点时把原来包在 sx(...) 里的表达式拆出来直接赋 int 就炸了，改回 double。**但真正的问题是这两条错误本来就在我的冒烟输出里**——实测把错误放回去重跑，javac 明明白白报了 incompatible types，就躺在 1515 条 cannot find symbol 噪音里；**我的 grep 名单只有两类**（语法错、自家新符号 cannot find symbol），**类型错误这一整类从来没筛过**；手写 grep 的问题就在这——**筛的是我想得到的类别，漏的是我想不到的**。**docs/tools_smoke_filter.py（冒烟报告筛选器）**：把 javac 输出切块，按「缺依赖三类一律记噪音、**其余一律进必须看**」分流；判据刻意**简单可靠优先**（试过按 symbol: 明细行判 MC 类型，底噪仍 476 条高到没人看；而自家符号找不到本就有第 19 闸专管）；另登记三种已知底噪（缺 MC jar 时 record 紧凑构造器/方法引用/静态导入解析不了参数类型），新出现的一律进必须看；**双向验证**=含错日志精确报 2 条并指到行号，修好后干净；新工具第一次跑就翻出一条我从没看见过的（SdzjzCommands 静态导入，核过是底噪）。**这是第三次因为「验证方式」而不是「代码」出问题**=m487 没筛删除的符号→第 19 闸、m488 漏带闭括号→括号配平并入第 19 闸、m491 漏筛类型错→本工具；三次形状一模一样——**我的验证是「按我记得的清单去查」，漏掉的永远是不在清单上的那类**；这次改的不是清单而是**把清单换成白名单式分流**：不认识的错误默认进「必须看」而不是默认被忽略。验证=两代冒烟经筛选器复查均无必须看的真错、20 闸全绿、零配置零资源改动。**教训：grep 是给已知问题用的，不是给验证用的**——验证要的是把未知暴露出来，而 grep 天然只能匹配已知模式，用它做验证等于每次只检查上次栽过的跟头；第 19 闸（静态扫描）+本筛选器（动态输出分流）两件合起来才勉强补上「我的验证」与「作者的构建」之间那道缝。 上笔 m490：**世代 API 对照表 + 第 20 闸 + 小地图整段搬**。**作者点的「仓库里应该 1.20.1 的 API」是这几刀失误的总根子**——我一直在**猜** 1.20.1 有没有某个 API，猜对没事猜错就是 m487 那种构建失败；仓库里确实有映射资源（MAPPING_MEMBERS.tsv 3772 行）但那是 **Yarn→Mojmap**，**不是 1.20.1↔1.21 的世代差表**，正好是我缺的那份，而世代差知识全散在各文件注释里没有一处能查。**建了 docs/世代API对照表.md**=①已建的**十一个世代口**索引（先查这里，多半已经有人替你收过）②API 逐项对照（1.21 专属→1.20.1 对位→该走哪个口→哪一刀撞的），分物品与数据/资源与注册表/渲染/方块实体与存档/附魔药水五组③**非 API 类世代差**（资源文件、NBT 键命名空间、JDK 版本、执行面取舍）——同样会咬人且更隐蔽；**每行都是踩过或核过的**。**第 20 闸 tools_gen_api_check**：扫共用层（辖区=1.20.1 白名单里的 xplat 文件+common 全层，**白名单从 build.gradle 现读不手抄**）有没有 1.21 专属符号，命中即红并给出对位与该走的口；**自证=重放 m487 血案**（把 getComponentsPatch 放回 StorageLedger 当场红，还原复绿）——**这把闸能把那次构建失败挡在推送之前**。顺手搬完**第五件：画布小地图**（主线 renderMinimap/mapGeom/mapJump 整段下沉成 MinimapRenderer：投影+边框+深底+顶部青条+空画布提示+节点按分类配色小矩形+**当前视口白框**+点击跳转；**1.20.1 原来完全没有小地图**，这刀从零补上；**世代差为零**，主线 panX/panY 与本世代 viewX/viewY 只是同一件事的两种记法，换算在 View 口做一次）。验证=两代冒烟真语法错 0、自家符号 symbol 级报错各 0、**20 闸全绿**、零配置零资源改动。**教训：「我不知道这代有没有这个 API」应该是一次查表而不是一次赌**（前面几刀我把它赌成了构建失败两次）；更该记的是**知识不汇总就等于没有**——世代差的答案早就散在几十条注释里（m443 写过 tag、m452b 写过 ResourceLocation、m477 写过 parse），散着就查不到于是同一类问题反复踩；**建表+建闸一次，查表此后每次**。**下一步：节点悬停详情浮层（UI 最后一件），然后回业务线（tick 执行面 / ⑤d 规划器族）。** 上笔 m489：**画布视觉第四件——数据面板屏工艺整段搬 + 搜索框挪位 + 补共用件资源**。搬的东西=主线 DataPanelScreen.renderBg 形制（全屏暗底+termPanel 窗体大卡+紫方块图标带受光角+标题分隔细线+分区卡片+**聚焦紫描边**四边 1px+termSlot 主线槽位+**真实比例滚动条**轨道 termBaseDeep 加轨内顶阴影加紫滑块），1.20.1 原来是清一色 ctx.fill 平涂（我当初写的 cell() 两行一格）；**本世代菜单只有背包 36 槽**（无经验库/合成终端/回收）那三块不搬——**搬工艺不搬本世代没有的功能区**，到序时随各族补。**顺带解决 m474 记下的地盘冲突**：搜索框原钉在标题栏右上角 96..187，而 IPN/REI/JEI 默认也往容器右上角放按钮（作者早先截图里三个排序按钮就压在搜索框上），这次挪到**网格上方独占一行**（主线也是这个位置）躲开整个生态的默认落点。**撞到新类型漏点并补闸**：SciSkin.termSlot/termBtn 要 blit textures/gui/{slot,button}.png——**主线有，1.20.1 资源目录里根本没有**；这类「共用件挂上去了但它依赖的资源这代没有」**编译器管不着**（运行期资源）**纯语法冒烟也管不着**，只有开屏才看得见（槽位画成紫黑格）；拷两张贴图过去并把「共用件资源普查」并进第 19 闸（自证：挪走 slot.png 当场红，放回复绿）。**第 19 闸现在管三件事**=悬空引用（m487）/括号配平（m488）/共用件资源（m489），三件的共同点是**都发生在「我的验证」与「作者的构建·开屏」之间那道缝里**——每被咬一次就往闸里加一条，比每次写「下次注意」有用。验证=两代冒烟真语法错 0、自家符号 symbol 级报错各 0、19 闸全绿、零配置改动、**新增资源两张**（1.20.1 侧 slot/button.png 从主线原样拷）。实机预期=数据面板从「深蓝方格+平涂」变成主线那套终端质感（窗体大卡/紫刻标题/卡片分区/槽位立体感/滚动条按真实比例），搜索框在网格正上方独占一行与整理类模组按钮不再重叠。**下一步：画布小地图、节点悬停详情浮层，继续整段搬。** 上笔 m488：**画布视觉第三件——连线缎带整段搬 + 锚点按新卡归位**。作者问「是代码搬不过去吗？跟原版的源码差太多了」并贴新截图；答=能搬不是搬不过去，截图里节点卡已经是主线那张（顶条/机器名/进出口柱与「进」「出」字标/状态灯点/×1 都到位，m484 生效），**差的是还没搬的部分，最刺眼的就是那条线**——主线是 m136 谱系的缎带，1.20.1 还是我当初自己写的一根等宽直线段。搬的东西=主线 drawWire/drawWireFree/wirePath/pulseAt/ribbon/portDot/quad/mulRgb/towardWhite **129 行整段**下沉成 xplat/client/WireRenderer：三次贝塞尔走线（控制柄按距离自适应，近不打结远不拉直）→采样法线与弧长累计→**三层缎带**（右下偏移暗投影把线从网格上抬起来/宽羽化软光晕/亮度沿线坡升的亮核）→**彗星脉冲**（等距脉冲沿弧长顺流 110px/s 间距 88px，头缘陡尾缘缓，并排线按端点坐标错相）→**端口发光圆点**（12 段三角扇+渐隐外环，线的起讫落在接点上而不是凭空断在卡边）→m197 线宽随缩放封顶；**唯一世代差**=发顶点那四行，走 m483 已建的 Gfx 口（新增 quadVC）。**顺带修了 m484 留下的错位**：1.20.1 连线锚点还按旧 24×24 卡算（+12 取中心）而卡早已 100×52，改成主线同款端口锚点=**出口柱（上游右缘中上）→进口柱（下游左缘中下）**切线水平；截图里线从卡左上角斜插进来就是这个错位。**同一个坑踩了第三次这次固化进闸**：按行号切段搬代码时漏带闭括号（m487 StorageCore120、本刀 WireRenderer 各一次），它在纯语法冒烟里报成一堆 illegal start of expression 而**括号一数就知道**；把括号配平普查并进第 19 闸（全库扫，剥注释与字符串后数花括号）。**m487 我写「按行号切段是危险动作」，写完当刀又踩——光写教训不装闸，教训就只是教训。** 验证=两代冒烟真语法错 0、三个改动文件括号配平、19 闸全绿、零配置零资源改动，**仍不敢说验证充分，等作者 Gradle 反馈**。实机预期=连线应从等宽直线段变成有投影/有光晕/有流向脉冲的缎带，两端落在卡片端口柱上（不再斜插进卡角），产出线绿供料线金节点连线紫，与 1.21.1 同色同工艺。**下一步：数据面板屏工艺（termPanel/termSlot/termBtn 在共用 SciSkin 里现成）→小地图→悬停详情浮层，继续整段搬。** 上笔 m487：**修 m485/m486 的构建失败 + 第 19 闸（悬空引用）**。作者回报 1.20.1 Gradle 构建失败 15 个错误，全是我那两刀捅的，两类根因：**①整段搬时没检查搬入物里有没有世代专属 API**——StorageLedger.deposit 里那句 stack.getComponentsPatch().isEmpty() 是组件世代专属（1.20.1 没有），我检查了「新符号有没有报错」却没检查「搬进共用层的原文里有没有 1.21 独有调用」；修法=改走 ItemData.has(stack)（那门面 m437/m451 本来就是为「有没有附加数据」建的，两代各判各的）；**整段搬之后必须扫一遍搬入物的 API 面**。**②按行号切段连带删掉了不属于被搬内容的东西**——删 1.20.1 那 181 行账本时 46~226 区间里还夹着**构造器**、connectedCores、loadedCoreAt、acceptsPlainType，于是方块实体没了构造器、数据线/面板/菜单四个文件全找不到符号；从 git show ccb4317: 取回补上；**按行号切段是危险动作，切之前要先看区间里有什么不能只看首尾**。**纯语法冒烟为什么没抓到（最该记的）**：javac 无 MC jar 时 cannot find symbol 上千条全是缺 net.minecraft.* 的噪音，我一直用「自家新符号定向 grep」筛——**那只筛了新增的符号，没筛删除的符号还有没有人在用**，exactTpl/xpBank/connectedCores 这些不在 grep 名单里于是躺在噪音里直到 Gradle 才炸。**第 19 闸 tools_dead_ref_check（悬空引用，挂 CI）**：对做过下沉的文件列出本刀删掉的符号名，该名已无声明却仍被引用即红；**修尺子修了两轮**（①正则只认 名./名(/名[ 漏了裸标识符用法，自证没红当场按 m109 查出；②改宽后误报 NBT 键名字符串，补剥注释与字符串），**自证是发现坏尺子的唯一手段——若当时「看它绿了就过」，这闸会以永远绿的姿态躺着**；新闸一上来就抓到两代各一处（主线 FabricLedger 视图里也在裸用 exactTpl，同一个错只是 Gradle 还没跑到）。验证=两代冒烟真语法错 0、括号配平、19 闸全绿，**但这次不敢再说「验证充分」——纯语法冒烟证明不了能构建，作者的 Gradle 才是判据**。**教训：「我的验证过了」和「它能构建」是两件事，而我一直把前者当后者汇报**；m479 写过「能构建和能跑之间隔着一整个运行时」，这刀补上更前面一段=**「我的冒烟」和「能构建」之间也隔着东西**，第 19 闸补了一半，另一半只能靠作者的 Gradle，**所以每刀之后的构建反馈要当成必经环节而不是可选确认**。 上笔 m486：**C 阶段主刀——路由脑判定层两代共用，第一批对表闸退役**。接着 m485 的方法（是移植不是重做，把仿写件直接移除）办 StructureCore120 的路由脑：主线六个判定函数 accepts0(3241~3282)/chainWants0(2818~2887)/chainEndsInTrash(2752~2768)/sensorOpen(2198~2207)/allGatesClosed(2210~2221)/extractorLive(1886~1888)**共 154 行整段搬**成 xplat/node/RouteBrain，**只做三类机械替换**=①身份判定 instanceof 五种物品类→NodeTags.defOf 与 Machines 常量引用同一性（m472 已证两代通用）②传感器库存来源（主线 supplyFor→resolveInputSource 默认主存储回落）→Host.sensorObserved③画布状态/出边/profiler 计数→Host；**其余一个字未改**，含 m153 垃圾桶授权语义与传感器「闸门在下发阶段生效」那格的注释（它们看起来像 bug，改一下就是 m132-6 重演）。两代各删各的：1.20.1 那 128 行重写删除、主线 154 行原地退役，双方改转发。**账目：业务双写 1944→1872 行。** **第一批对表闸退役（这刀最有意思的部分）**：删完第 16 闸当场红报「蓝本名单抓到 0 条」——m473 立的那三条项（accepts 分派表/accepts↔chainWants 成对表双向）**本来就是为仿写路线设的补丁**（当年两代各写一份靠尺子追平名单），现在两边共用同一份代码，「名单漏抄」这种失败模式**按构造不存在**了，尺子失去量的对象；摘除并在闸文件里留退役记录，行为改由 m481 路由域跨代契约压着——**从「对表」升级成「对行为」**，正是 m477 定路线时预言的第一批兑现。**还没搬的**：tick 编排/清运六分支/拉料循环/灯表——这些是**执行面**，两代取舍确实不同（主线有输出缓存与断网喷射，本世代是回灌持料，见 m473/m475 记档），不是照抄能解决，要先把那两处取舍统一或参数化，下一刀专门处理。验证=两代冒烟真语法错 0、自家符号 symbol 级报错各 0、18 闸全绿、零配置零资源改动。**教训：闸是有寿命的**——m470/m473 那批对表闸在仿写期是救命的（m469 血案就是它们防的），但路线一变它们量的东西就消失了，留着不删会变成噪音甚至逼人把已合一的代码再拆回去喂尺子；判断标准=**闸描述的失败模式还可能发生吗**，不可能了就退役并把理由写在闸文件里。**下一步：tick 执行面（编排/六分支/拉料/灯表）——先统一那两处世代取舍，再照同样方法整段搬。** 上笔 m485：**直接移除仿写件——存储账本核心两代共用，B 阶段主体一刀做完**。作者定调「主要是你一直在做错。是移植不是重做。我觉得可以把以前写的 1.20.1 直接移除，然后可以直接移植」——指出了方法上的根本问题：**我一直在「读主线→理解→在本世代重新表达」，那是重做；正确动作是把仿写件删掉直接用主线的代码**。本刀照做=①主线 StorageCoreBlockEntity 账本段（139~275 行：m130 精确账本/m293 类型硬顶/m295 哈希索引/m273 饱和加法/m218·m322 修订号/m80c 经验库）**整段搬**成 xplat/storage/StorageLedger，**只做一处机械替换** setChanged()→onChange.run()，其余一个字未改连注释刀号都原样带过来；②**1.20.1 那 181 行「按主线重写的账本」整段删除**，改成与主线同一份 ledger 的转发；③两代存档/FTA 事务（undoJournal 前像回滚）/漏斗对接路径机械替换成 ledger.storeView()/exactTemplates()/exactCounts()/markIndexDirty()/bumpStoreRev() 等。**账目：业务双写 2099→1944 行，StorageCore120 从「待合一双写件 426 行」降级为世代壳（271 行，只剩 BE 注册/存档签名/FTA/漏斗对接）。** **为什么敢一刀做完**：m480 那把「契约先立、手术后做」的网就是为这刀架的——十类跨代判定两代各喂自己的实现跑同一套，账本换了实现这套断言必须继续全绿；**先架网再拆房子，拆的时候就不用小心翼翼**。过程中两处机械替换误伤（都被冒烟当场逮住）：xpBank 全局替换把转发方法定义与 NBT 键名一起改了、tier 字段删后存档还在直接赋值——**机械替换必须配冒烟，不能配自信**。验证=两代冒烟真语法错 0、自家符号 symbol 级报错各 0、18 闸全绿、零配置零资源改动。**这刀动了两代存档路径，务必实机验**：两代各开现有存档看库存/精确账目/经验库是否原样、存取普通件与精确件、漏斗塞件（FTA）、退出再进不漂、跑 storage_domain_contract(_retro) 两代都该绿。**教训：「移植」和「重做」的分界线在于你有没有把原文粘过来**——判断方法很简单，改完 diff 一下：新代码与原文逐句对得上是移植，只是「功能一样」就是重做；我前面十几刀有相当一部分是后者，账面看着在推进实际在制造第二份要维护的代码。**下一步照同样方法：StructureCore120 991 行——主线 StructureCoreBlockEntity 的路由脑与 tick 编排整段搬成共用件，本世代那份重写整段删；m481 路由域契约已架好网。** 上笔 m484：**画布视觉第二件——节点卡整张下沉共用**。作者批评「你有 1.21.1 的原版你不能看看吗，你这样完全是在浪费我的额度」——完全正确：m483 我在 1.20.1 侧自己编了状态灯点的坐标，而主线本来就有 drawStatusDot（带呼吸动效、停机变灰、位置 x+NW-11,y+5）；**手上有原版还去重新设计，既慢又必然不一样**，这正是仿品的生产方式。**本刀起立规矩：有原版的一律整段搬，不自己想。** 搬的是主线 drawNode **216 行**→共用件 xplat/client/NodeCardRenderer，1.20.1 节点卡自此有：卡面工艺+m86 分类配色顶条+m120 标题读数底带+m352 双侧进出口柱与「进」「出」字标（含 m341 互换配置）+m123 阶位图标放大与超级·/神级·/GM· 前缀变色+机器名自动截断+**主线那颗带呼吸动效的状态灯点**+六族逻辑节点各自读数行（分配器均分→N路/余数轮转、抽取●抽取中○待命+速率与已抽+自动启停行、开关●开○关、垃圾桶[白名单·N]/[虚空]+已吞、过滤器名单前三图标+「+N」、传感器目标图标+<阈值放行+[−][+]）。**切三段是被一次错误逼出来的**：第一版我把主线 216 行整体删掉换转发，那会**丢功能**（里面还有虚空处理器/区块储存器/扫描器/区块过滤器/作物多选/合成目标/酿造/附魔/交易/复制器等本世代独有分支），发现后 git checkout 回滚重做，改成 drawBase（骨架两代逐位相同）→drawBody（六族，**返回 true 表示已画完**）→drawGeneric（通用兜底：数量+阻塞原因）；主线拿 base+body、body 回 false 时走自己原有独有分支（原文逐句未动），1.20.1 拿完整三段——**零功能损失零重叠**。三处世代差=①宿主数据（状态灯/阻塞原因/出线条数/是否运行）→Host 小接口两代各实现四行②机器身份判定改走 defOf 引用同一性（m472 已证两代通用，本世代没建的机器自然不命中走通用分支）③物品 id 解析→SciSkin.Gfx.id() 世代口。验证=两代冒烟真语法错 0、自家新符号 symbol 级报错各 0、18 闸全绿、零配置零资源改动。**教训：「参考实现」不是拿来参考的，是拿来搬的**——前几刀我反复在做「读主线→理解→在本世代重新表达」，每一步都在引入偏差而且慢；有原版时正确动作是整段挪、只改真正编译不过的地方，**理解是搬完之后的事，不是搬之前的门槛**。**下一步按件继续：连线缎带（m136 逐顶点着色，现在还是直线段）+m193 归并徽章→数据面板屏工艺→小地图→悬停详情浮层，都照本刀规矩整段搬。** 上笔 m483：**绞杀者第六刀——SciSkin 上挂 + 节点卡工艺归位（画布视觉开始按件搬）**。作者实机反馈「结构核心完全不一样，这个没搬过去吗？一个一个搬啊」——对，m477 写下了 UI 绘制件下沉的方向后一直在做底层合一，画布视觉没动。**第一件必须是 SciSkin**（主线所有卡面质感出自它，而 m452b 记档「因内联 1.21 API 不可挂」，本世代只挂了纯色值表，所以 retro 两屏只能拿 ctx.fill 平涂色块）。**量下来比记档乐观得多：326 行里只有 10 行世代专属**（两处纹理 id 构造+八行顶点写法），其余 316 行全是 ctx.fill 与纯算术；收进 Gfx 世代口（tex/quad 两方法）整文件上挂——**第九个世代口**。**quad 参数顺序刻意不「整理」**（左上→左下→右下→右上，与原下笔顺序逐位一致；顺序错会画出扭曲渐变而**编译器管不着判官也难写**）；1.20.1 侧 ARGB 拆分刻意对齐 1.21 的 setColor(int) 内部口径，两代颜色逐位相同。**上挂时撞到一个真隐患并修掉**：SLOT_TEX/BUTTON_TEX 原是 static final 直接初始化，收进世代口后那句 gfx() 会在**类加载时**执行——任何在 installGfx 之前碰到 SciSkin 的代码（哪怕只是读一个颜色常量）都会当场炸；改惰性求值（首次用到才解析并缓存）行为逐位不变。**这类「把常量改成走接口」是绞杀者手法最容易踩的一脚：静态初始化不看你的装配顺序。** **第一件搬的：节点卡**——原来是两个 fill（整圈灯环+面）+图标，现在换成主线同一份 drawCard（软投影三层渐淡+外分离暗环+顶点插值渐变面+顶部冷光泽+内顶受光棱线/内底压边+四角括号刻，一张卡 13 次绘制），**状态灯也从「整圈边框」改成主线口径的「右上角小圆点」**（截图里那个绿色大方框就是旧的整圈灯环）；存储节点卡同改。**还没搬的（下几刀按件来）**：节点卡信息层（分类配色顶条/机器名带截断/阶位前缀变色与图标随阶放大/进出口柱+「进」「出」字标/类型专属读数行）、连线缎带（m136 逐顶点着色，现在还是直线段）+连线归并徽章、数据面板槽位与面板工艺、小地图、悬停详情浮层。验证=两代冒烟真语法错 0、自家新符号 symbol 级报错各 0、18 闸全绿、零配置零资源改动。**教训：「不可挂」这种记档要定期重新量**——m452b 写下那句时大概是对的（那会儿世代口一个都没有），攒到第九个口之后同一句话已不成立；**旧结论会随基础设施积累而过期，而它不会自己举手说自己过期了。** 下一步=继续按件搬（节点卡信息层→连线缎带→面板工艺）；业务侧欠验清单（m479 §5）仍挂着，UI 刀不碰存档与业务路径可并行推进。** 上笔 m482：**真移植 D 阶段收判官双写——顺带发现第 18 闸的账目一直在骗人**。开工先定义清楚「双写」=**同一语义在两边各验一遍**，不是 1.20.1 独有的覆盖；逐个对照用例名后只有**六条同名**，其余（cable_*/tick_*/pair_*/chain_pull_* 等）主线根本没有对位是本世代独有覆盖**不该收**。本刀收三条（语义纯零平台依赖）=①分次取尽不复制不蒸发②**精确索引经中间移除仍正确**（m295 哈希索引最容易错的那格：提净中间条目触发删键+平移，平移后并账必须仍命中原条目，错了不报错不崩只表现为同款物品悄悄分裂成两条账）收进 StorageDomainAssertions.runAll，两代各删一份本地实现；③type_safety_limit 收成契约⑪ 类型硬顶两代改调它。**⑪ 的分工值得记**：它要开配置窗而 m480 契约刻意「不碰配置」，解法=**配置窗归判官管（各代本来就有 try/finally 还原）、契约只管判定**，故 ⑪ 单独提供不进 runAll。剩三条不收并记档（两条 fabric 事务依赖 transfer API 两代版本不同、ledger_nbt_roundtrip 依赖 save/load 签名不同）——**依赖面没收干净就硬收等于把世代差塞进契约里装作没有**。**顺带修了第 18 闸的真缺陷（本刀最有价值的部分）**：收完双写后跑闸欠账不降反升（3755→3817），查因=闸按**整文件行数**算欠账而判官文件里既有双写也有大量本世代独有覆盖，m481 加一条契约判官就把删两条双写的账冲没了；改法=判官类文件改按**与主线同名的用例数**计账，改完账目立刻说实话：**业务双写 2099 行**（原报 3817，虚高的 1718 行全是判官文件里本世代独有覆盖）+**判官同名双写 4 条**（收前 6 条），且四个判官文件里**三个是 0 条**——RetroCanvas/RetroNet/RetroPanel 根本不是双写，之前整体算欠账是错的。**教训：账目口径错了比没有账目更坏**——没有账目至少知道自己在猜，错的账目会让人以为在进步（或像这次以为在退步），而且它是我自己在 m477 定的口径，**自己造的尺子最容易忘了校准**；m109「先怀疑尺子」这次应验在第 18 闸自己身上——**看到反直觉的数字（收了双写反而涨），第一反应该是查尺子不是查代码**。验证=两代冒烟真语法错 0、18 闸全绿、零配置零资源零业务方法体改动（只动判官与契约）。**下一步：m479 §5 欠验清单仍挂着；清单一过=B 阶段 StorageLedger 下沉→C1 路由脑下沉（两张契约网已架好），剩三条判官双写等 B 做完依赖面收干净再收。** 上笔 m481：**真移植 D 阶段先行·第二域——路由域跨代行为契约，给 C1 铺网**。配方域 m372→存储域 m480→本刀路由域，第三次推广；目的同=C1 要把路由脑下沉成共用代码，下沉后凭什么说语义没变，先立契约。**做法必须改**：存储域十三方法两代本来就同名同签名（各加一行 implements 即可），路由域**方法面对不齐**（主线没有 routeOut/isTrashLike/hasSinkFor——我仿写时把蓝本 distribute+depositOrBuffer 合并成了 routeOut），逐个核签名后**只有四个对得齐**：accepts(Level,int,String)/chainWants(Level,int,String,int,Set)/sensorOpen(Level,int)/extractorLive(Level,int,ItemStack)，契约只覆盖这四个；**另两个显式记档留给 C1 统一**=allGatesClosed 主线是 (Level,int[] targets) m355 数组化收目标数组、1.20.1 是 (Level,int from) 收起点自己遍历出边，chainEndsInTrash 两代参数个数不同——**这不是语义差是我仿写时的切分差，不硬塞进契约装作一致**。落地=RouteBrainProbe 四口（level 走不透明代际句柄）+RouteDomainAssertions 六条成对判定+两代 BE 各加四行转发（纯加法）+两代各一条判官包装；场景由各代判官自己搭，断言只吃「哪个下标是什么」。六条全部成对（收料面×需求面）：过滤器白名单内外/开关关开/**传感器关闸时两面刻意不同**/暂停两面都拒（m110b）/垃圾桶（accepts 看安全白名单、chainWants 无条件为真 m153 授权语义）/通用耗料机吃不吃，外加抽取启停。**最值钱的是第③条**：传感器关闸时收料面拒但需求面照样放行（蓝本「闸门在下发阶段生效」）——它**看起来像 bug**，最容易在 C1 下沉时被顺手「改对」，一改就是 m131b→m132-6 血案重演；契约钉死它并在失败信息里直接写「这不是 bug，改成一致会让上游停产」=**给未来那个想动手的自己留话**。**第 17 闸第二次立功**（判官用了 sl 没登记当场红，补登记复绿；m474 那把尺子上线两刀两中都是真活）。过程中纠了自己一次错=主线判官初稿写了 NodeTags.setSwitchOn/setNodePaused/setExtractorOn 三个**根本不存在的写口**（NodeTags 是纯读门面），改走 ItemData 五口；**教训：「凭感觉写 API 名」是仿写路线留下的坏习惯**——两边各写一份时猜错大不了改自己那份，共用之后猜错就是编译不过，这其实是好事。验证=两代冒烟真语法错 0、四口转发签名全部匹配、18 闸全绿、零配置零资源**零业务方法体改动**。**下一步：m479 §5 欠验清单仍挂着，跑完才动主线业务路径；清单一过=B 阶段 StorageLedger 下沉（m480 契约压着）→C1 路由脑下沉（本刀契约压着），两张网都已架好。** 上笔 m480：**真移植 D 阶段先行——存储域跨代行为契约「契约先立、手术后做」**。①**C0 取消（普查稿修正，如实记档）**：m479 稿说 C 阶段要建 CoreWorld 约 5 方法，动手前逐行核对那 20 行世代触点后发现估错了——dimension().location()/getServer().getTickCount()/getForcedChunks()/setBlock/instanceof Container **全是两代同名 API 不用打洞**（1.20.1 侧现成就在用），真世代差只剩服务器节拍指标一处（getCurrentSmoothedTickTime vs getAverageTickTime），观众推送与区块机器线本就该留世代壳；**C0 没必要做**，不为接口好看造空方法（RecipeAccess「不预开空方法」同律），那一处推迟到 C5 随手收掉。②改做的事=等待期最该做的不是叠手术而是**先给后面的手术架安全网**：B 阶段要把账本核心下沉成共用类，下沉后凭什么说行为没变？靠「我记得没改语义」不够，所以先把存储域跨代行为契约立起来，B 做完这套断言必须继续全绿。③**零方法体改动的接线**：实测两代存储核心**十三个账本方法全部同名同签名**，新建 StorageLedgerProbe 接口把这件事写进类型系统，两代 BE 各加一行 implements 即可——m180 家法极致形态：连方法都不用搬，只让编译器盯着「它们本来就长一样」；与生产侧窄契约 StorageAccess（四口 m464）刻意分开=生产代码不该因要测试而被迫吃大接口。④StorageDomainAssertions 八类判定（一份在 xplat，两代各喂自己实现，照 m372 RecipeDomainAssertions 样板推广出配方域）：普通账基本律/精确账分流/并账分账/**附加数据保真**（走 m478 共用 StackKey 判同款）/类型额度同占/**类型闸零丢件**/视图一致（含不许零负条目）/经验池；断言只碰账本口**不碰世界不改配置**零暴露窗，类型闸那条额度无限或池子不够时自动跳过**不为测试造假物品**。判官两条累计六十三（主线 storage_domain_contract 与 1.20.1 storage_domain_contract_retro **跑同一套断言**，两代同绿=账本行为在两个世代上确实是同一个东西而非「看起来差不多」）。验证=两代冒烟真语法错 0、**两代 BE 都完整实现探针十三口无 abstract 报错**（本刀最实的验证：证明两代账本方法面本来就一致）、18 闸全绿、零配置零资源**零业务方法体改动**。**教训：普查也会估错，估错了要当场改口不要将错就错**——按稿子照做会造出一个包着两代同名 API 的空壳接口，以后每个读到它的人都要问「这为什么要抽象」；稿子是给自己看的路线图，不是给自己下的军令状。**下一步：m479 §5 欠验清单跑完之前不动主线业务路径；清单一过就开 B 阶段主体 StorageLedger 下沉，本刀的契约会全程压着它。** 上笔 m479：**真移植 C 阶段小普查与欠验登记（零代码笔）**，稿子 docs/真移植C阶段小普查_m479.md。**作者回报「现在可以构建成功，但暂时没时间测试」**——编译层面已确认（真 Gradle 比纯语法冒烟严得多），但 m477/m478 都碰了存档读入路径、运行时未验；B 阶段主体是真移植路线**第一次在主线动业务代码**，在未验证基础上叠手术出问题会分不清是哪一刀的锅（m465 教训同族），故本刀改做零风险的普查+登记欠验债。**量出的第一条：tickInner 一个方法 1457 行但 MC 触点密度只有 7%，真正要打洞的世代触点只有约 20 行（1.4%）**（世界写入 3/玩家网络 7/区块票维度 8/容器 FTA 2），其余全是纯业务或 ItemStack/CompoundTag/BlockPos/Level 两代同名类型——**所以 C 阶段的正确形状不是「拆 4005 行巨类」而是「打二十个洞」**，风险预期直接降一个量级。**第二条：密度这把尺子自己把分层划出来了**——高密度的恰好都是真壳（chunkFxBurst 37%/findTarget 31%/dropAll 29%/saveAdditional 19%），低密度的恰好都是业务核心（fillDrain 3%/bumpTopo 4%/accepts0 5%/distribute0 7%）；字段同形（纯业务 47 个 vs 带 MC 类型 7 个）。八个世代口已就位，C 阶段只需再加 CoreWorld（世界效应口约 5 方法）一个。**分片六刀**：C0 建 CoreWorld 口（零业务改动纯加法）→C1 路由脑下沉（~350 行密度 3~7% 最独立，**且 1.20.1 侧有 m471~m475 刚写的对位实现可直接同值对拍再删**）→C2 供料拉料→C3 逻辑节点六分支→C4 通用机器分支→C5 tick 骨架与灯表→C6 特种机器族；纪律=一刀一段、段间可独立回滚、每刀之后等一次实机确认。**欠验清单（五分钟跑完）**：①1.21.1 开现有存档画布节点位置/连线/分组/状态灯/总线库存全部原样②1.21.1 存取带附加数据物品同款并账异款分账组件保真③1.20.1 同样两条各验一遍④两代各退出再进不漂；**一到四全过=m477/m478 运行时确认，C 阶段可开工**，任一条不过就停下来修。**教训：「能构建」和「能跑」之间隔着一整个运行时**——前两刀的验证栈是纯语法冒烟+静态闸+判官源码，作者的构建成功补上了编译层，但判官没真跑过、存档没真读过；真移植动的全是存档与运行时路径，这条缝必须由实机来缝，所以宁可插一刀零代码普查也不在缝上之前往下叠手术。**普查是等待期最有价值的活：它不消耗信任，只积累判断。** 下一步=C0 零业务改动纯加法不依赖欠验清单可先做；C1 及以后等清单跑完再开。** 18 闸全绿。上笔 m478：**真移植 B 阶段第一刀——精确账本身份键合一**（账本下沉的前置）。量 B 阶段时发现账本核心里**第一个也是唯一一个真世代差**就是身份键：StackKey（xplat m404）与 TagStackKey（retro m443）同一个东西两份写法，差异只有 equals 与 hash 两句；账本下沉必须先统一它，否则搬过去还得带两套判定。落地四件=①共用 StackKey 加 Kind 世代口（same/dataHash，install/req 同律——**这是第八个世代口，手法已成套路**）②两代各写实现（LegacyStackKind 组件口径/RetroStackKind tag 口径，均原句照搬 m180 家法）③白名单挂共用类、**TagStackKey.java 整个删除**、StorageCore120 里 7 处引用机械切换④两代 bootstrap 装配（StackCodec 下一行）。**哈希契约是这刀红线**：精确账本靠 exactIdx 哈希索引查条目，键的 equals/hashCode 不自洽会表现为同款物品分裂成两条账目或取货取错——不报错不崩只有对账才看得出来；合一后契约论证也统一成一份。判官两条累计六十一（主线压契约全项+作 HashMap 键时同款命中同桶；1.20.1 压同一份 StackKey 换 tag 口径后契约照样成立+本世代特有的「空 tag 在场≠无 tag」）——**两条判官压的是同一份类的两种装配，这正是真移植该有的验法**。双写账目：已合一 2 个（图状态、身份键），待合一 10 个，**欠 3727 行**。18 闸全绿。**下一刀 m479（B 阶段主体）=StorageLedger 下沉**：store/exactTpl/exactN/xpBank/tier/storeRev/exactRev 七字段与count/deposit/depositExact/withdraw/withdrawExact/exactTemplates/exactCount/storeView/usedTypes/maxTypes/typeGate/upgrade/xpAdd/xpTake/satAdd 全套账本方法抽成共用纯类，两代 BE 各持一实例并转发，BE 只留世代壳（全局注册表/FTA/Container 漏斗对接/存档签名/BlockEntity 继承）；账本方法里唯一碰 BE 的是 setChanged()，用 Runnable onChange 注入即可——**这刀之后 425 行里能消掉约 300 行**。之后 C 主刀 SCBE 业务核心下沉、D 判官合一。** 上笔 m477：**路线变更——真移植 A 阶段第一刀：图状态两代合一**。作者看实机截图后拍板：「现在移植的状态就是个仿品，根本不是一样的东西」——判断成立，且**对表闸的存在本身就是证据**（两边跑同一份代码就不需要「名单漏一条不报错」那种闸，那是我为仿写路线打的补丁）。先量后切：1.20.1 世代 5300 行里约 3800 行（72%）双写；而 MC 触点密度实测极低（StructureCore120 最密的方法才 18 个触点，绝大部分是 ItemStack/CompoundTag/BlockPos/Level 四个两代同名类型）——**卡点不是 MC API 差太多，是主线 SCBE 那个 4005 行巨类把业务与 MC 壳搅在一起**。本刀拿图状态当试金石（124 vs 132 行，字段名一个不差，真世代差**只有栈↔NBT 编解码一对**）：共用类加 StackCodec 世代口、lookup 改不透明代际句柄 Object（RecipeAccess 同一约法，主线调用点零改动）、两代各写实现（Legacy 原句照搬/Retro 取自被删仿写件原文）、白名单挂共用类、**CanvasGraphState120.java 整个删除**、retro 侧 9 处引用机械切换、两代 bootstrap 装配。**加固推广**：m459 修④ 两处读侧剪（越界自连连线/坏存储边）原只在 1.20.1，合一后两代共享——真移植第一份红利=加固不再只在一边（行为变更已记档并配判官）。**第 18 闸 tools_dualwrite_audit**（真移植看门人）：retro 侧 30 文件逐个登记为世代壳（20 个，该各写）或待合一双写件（10 个），已合一的必须真删干净，出现未登记文件即红——拦「随手再写一份仿品」；**欠账摆明面：还欠 3697 行**；尺子自证=把仿写件放回去当场红，删掉复绿。判官两条累计五十九（十三字段往返对拍含组件保真——**它与 1.20.1 侧同名判官跑的是同一份代码**；加固推广判官）。**教训：「对得很齐的仿品」仍然是仿品——指标齐 ≠ 是同一个东西。但那些闸不白做，它们精确标出了哪里在双写，第 18 闸正是把它们的经验反过来用：闸从「追平仿品」改成「盯着欠账清零」。** **路线图：A（本刀试金石）→ B 存储账本下沉（StorageCore120 425 vs 558）→ C 主刀 SCBE 业务核心下沉（4005 行拆「业务核心共用+世代壳各写」，风险最高，57 判官+18 闸压着）→ D 判官合一（照 m372 RecipeDomainAssertions：断言一份在 Common、两代各喂自己实现——项目早有这做法，只用在配方域，本次推广）。UI 几刀方向同步改写：SciSkin 上挂后主线 drawNode/drawWire 等绘制件下沉共用，1.20.1 屏只留输入与布局壳，不再重画一遍。** 18 闸全绿。上笔 m476：C2-⑤d 小普查与分片稿（**零代码笔**，稿子在 docs/生产tick移植1201_C2-5d小普查_m476.md）——量清=Planner 四件（CraftPlanner 320 行/EnchantPlanner 76/BrewPlanner 73/SmeltPlanner 67）**早就在 Common 零 MC 依赖**，本世代一行业务逻辑不用重写，只需补 RetroRecipeAccess 八口（m362~m373 配方域 SPI→拆四域→分域适配器的红利第一次由 1.20.1 兑现）；八口成本=craft/smelt/remainder 低（只差 RecipeHolder 包装/getId()/ResourceLocation 构造三处）、brew 中（1.20.1 药水存 NBT、酿造注册表是静态类，但 m425 那棵 BFS 前驱树结构一字不动）、ench 中；四台机器 tick 分支各 ~56~76 行。**三条发现**：①**附魔定价有真实语义缺口不是抄漏**——蓝本吃 Enchantment.getAnvilCost() 是附魔数据包化后才有的字段，1.20.1 无直接对位，A 按 rarity 反推（数值接近不保证逐位相等）/B 抄 id→cost 常量表进 Common（逐位相等但要配对表闸防手抄漏），**不擅自选等作者拍板**（它在 RecipeDomainAssertions 第七类断言里是硬的，选错要返工）；②**⑤d 与 ⑤e 不是并列是交叉**——附魔工厂吃结构核心 xpPool，而池子进项来自熔炼与虚空处理器即 ⑤e；存储核心侧 xpBank 已在但结构核心侧池子不存在，按原计划顺序做到附魔工厂会当场卡住，⑤e 的池子要先于它或同刀；③**目标选择 UI 是硬前置且是 UI 活不是路由活**——自动合成/酿造/附魔三台的 ct 键靠屏侧二级界面写入，本世代 retro 屏 ct 零触点，唯熔炉族例外（接什么烧什么，目标由 m149 白名单 fl 键表达，m472 已随 NodeTags 上挂）。**白捡红利**：RecipeDomainAssertions 七类断言现成（26.2 已在跑同一套），1.20.1 装上实现+白名单+五行判官即全跑。**分片=⑤d1 RetroRecipeAccess 四域（brew/ench 走 26.2 同款分域适配器）+契约判官 → ⑤d2 熔炉族 tick（零阻塞零歧义，一落地万能熔炉就可玩，accepts/chainWants 两面 smelterFamily 行同刀成对划掉）→ ⑤d3 目标 UI+自动合成机（建议单独开别与路由混刀，m465 教训：UI 与逻辑同刀时实机反馈分不清哪半坏了）→ ⑤d4 酿造塔+附魔工厂（后者必须等 xpPool）。教训：「下一笔做什么」是可以被普查改写的——普查的产出不只是工作量估计，更是**依赖顺序**，后者错了写得再快也是白写。下一步：⑤d1 可直接开工但**需作者先对附魔定价 A/B 拍板**；作者暂不回则先做 ⑤d2 熔炉族。** 17 闸全绿。上笔 m475：C2-⑤c3 链式拉料供料侧接线——**⑤c 分片收官**，1.20.1 的产线第一次「自己找料吃」：落地大件 pullSupply（5t 与清运同拍、在生产循环之前，遍历五族逻辑节点=过滤/开关/传感/分配/抽取，**刻意不含垃圾桶**防「仓→垃圾桶」手滑清空整仓）：①普通支路=chainWants 门控（m473 已就位）+m116 每种 4096 封顶+m270 类型上限 withdraw 前判（拒收=不抽物品留仓零损失）；②抽取节点=主动泵（m154）带 m157「没去处的不抽」（否则把全网络吸进缓存囤着失踪）、m160 白名单名单外碰都不碰、有 kind0=搬仓模式全抽、bufCapL=max(4096,pumpRate*2)（m163a）；③精确账本支路（m155→m158）只在 chainEndsInTrash 授权时抽（终点是销毁才允许抹组件；链上关着的抽取节点=闸断不抽）。同刀补 chainEndsInTrash（沿途尊重过滤/开关/抽取启停/暂停，深≤8 防环）。**三处世代取舍**：无数量升级故泵速=挡位原值；供料源只认自家存储核心（无数据面板聚合视图）；**蓝本泵支路不查 nodePaused 逐位照抄不擅自改**（疑似蓝本疏漏，留待作者拍板，改要两代一起改）；同族另记：全库从没有地方写过 xc 抽取累计=蓝本卡面「已抽取」恒 0，本刀不补写（m474 刚把这个键救回来）。对表闸加第 6 条项（拉料五族清单，正则按两侧同名 stL 锚定，豁免关），尺子自证拆传感器当场红点名 isSensor 还原复绿；过程中纠了一次坏尺子=蓝本方法名填成 tick 但那只是 m359 计时壳真身是 tickInner，闸如实报「抓到 0 条先怀疑正则」按提示改对。判官四条累计五十七（m132-6 血案复现形：仓→过滤器→耗料机拉得动且名单外一件不动／抽取泵三态含 m157 失踪案／精确账本授权闸链尾非垃圾桶时一件不许动／拉料侧守恒对账+4096 封顶）。**教训：「先怀疑尺子」要连尺子的输入一起怀疑——给闸写的错误提示是写给未来那个正在犯困的自己看的，值得像产品文案一样认真写。下一笔 ⑤d 规划器族（CraftPlanner/BrewPlanner/EnchantPlanner/SmeltPlanner，依赖 RecipeAccess 本世代未建），先做小普查量清依赖面再动刀（m463/m468 家法）；accepts/chainWants/chainEndsInTrash 三处已各留好标了刀号的到序行。** 17 闸全绿。上笔 m474 插队刀：画布坐标键位归位（xc/yc → nx/ny）+ 节点栈 NBT 键位登记表闸（第 17 闸）——写 ⑤c3 抽取泵前 grep 了一下 "xc" 的全库触点，发现**本世代的画布坐标就写在这个键上**，而 xc 在 NodeTags 里是 m159 抽取节点累计抽取量（long）：①抽取节点卡面「已抽取」读的其实是自己的画布 X 坐标（getLong 对 IntTag 走 TAG_ANY_NUMERIC，不崩不报错判官照绿）；②⑤c3 的泵一写累计就把节点**弹飞**到 x=已抽取件数；③蓝本坐标键叫 nx/ny，键不同名还破 m443 DFU 红利（升 1.21.1 坐标全丢）。差一步就把血案写进去。修法四件=坐标读写全改 nx/ny（蓝本同名同型）/cleanNode 洗净连旧键一起剥/**旧档自愈 healNodeCoordKeys**（m469 healEnds 谱系：有旧键且无新键才搬、搬完删旧键、新档一个字不动，读档一次性不落热路径）/判官助手与断言同步归位。**第 17 闸 tools_node_key_check**（节点栈 NBT 是两代共享命名空间）三判据=本世代物品栈用键必须登记、登记为共用的必须在 NodeTags 真存在、**本世代键 ∩ NodeTags 键中未登记为共用的一律红**（xc 那种形状当场现形）；只扫 getOrCreateTag/getTag 链不虚报 BE 存档子表；尺子自证退回 xc 当场红点名两文件，还原复绿；顺带清出未登记键 k。判官两条累计五十三（史前存档自愈+新档不动、坐标不再污染抽取累计且写累计不弹飞节点）。**教训：「键同源 NodeTags 谱系」这句注释是坏尺子——注释也会说谎且比代码更容易说谎（代码撞了至少还有编译器，注释撞了什么都没有），真正兜住的只有会红的闸。下一笔 m475=⑤c3 本体：链式拉料供料侧接线（chainWants 已就位）+抽取节点主动泵（pumpRate=extractorRate×(1+nodeCount)，本世代恒 ×1；bufCapL=max(4096,pumpRate*2)；m157 没去处不抽；m160 白名单名单外碰都不碰）+ 精确账本支路走 chainEndsInTrash 授权闸，判官照 m132-6 复现形：仓→过滤器→耗料机拉得动。** 17 闸全绿。上笔 m473：C2-⑤c2 逻辑节点七分支**成对**落地——1.20.1 的画布第一次「会分流」：accepts（收料面）与 chainWants（需求面）同刀写同刀测同刀上闸（m131b→m132-6 血案：只写一面拉料恒不通拖了整刀），七分支=暂停/过滤器/传感器/开关/分配器/垃圾桶/抽取，虚空→⑤e、规划器四族→⑤d 两面各一行成对记档；落地九件=isTrashLike 真判定（两轮垫底生效）/accepts 全表/chainWants 全表（深≤8+visited 防环，⑤c3 与判官共用）/sensorOpen/extractorLive/allGatesClosed/depositTail 抽出共用/distributeEvenOut 均分余数轮转/tickLogicNode 六分支 5t 清运；tick 加两道前置闸=暂停最先判（m99 early-continue）与下游闸门全关整台停。**三处显式世代差**：①送不出去的货原样回灌本节点缓存亮黄「去处满，持料待命」（本世代无输出缓存，账面可见零吞件，缓存到顶=天然背压）；②传感器未连供料线按 0 计（无「默认主存储」概念）；③虚空处理器两面均显式拒收（xpPool 未建，收了会压死在缓存里）。对表闸加**三条成对项**（正则 NodeTags.isXxx，豁免关）：蓝本 accepts0→本世代 accepts、accepts→chainWants、反向 chainWants→accepts；**尺子自证**：拆掉 accepts 传感器分支两条同时红点名 isSensor，还原复绿。判官八条累计五十一（含传感器「accepts 拒收但 chainWants 照样放行」那格两面刻意不同的判别性断言、闸门全关时上游连着产出仓也不许绕道白产）。**教训：成对的东西要成对上闸，不能只成对写——靠「记得同时改」是靠不住的（m131b 当年也记得了），真正兜住的是双向对表项：单向闸只能防一半。下一笔 m474=⑤c3 链式拉料供料侧接线（chainWants 已就位），抽取节点主动泵，判官照 m132-6 复现形：仓→过滤器→耗料机拉得动。** 16 闸全绿。上笔 m472：绞杀者第五刀——NodeTags 上挂 1.20.1：物品身份出入口收进 Ident 世代口（六族 is* + defOf），Legacy=LegacyNodeIdent 原句照搬（旧走法原位保留同值，卌二号同值判官逐族逐样本压着）、Retro=RetroNodeIdent（def 引用同一性零手抄）；死 import CustomData（1.20.5+ 专属，m437 起躺了三十多个里程碑）一并拔除，NodeTags 剩余 MC 触点=四个两代同名类型，整文件进 1.20.1 白名单。对表闸加三可选键（整文件/豁免/正则，缺省旧行为逐位不变）并登记"身份口六族"条目，尺子自证红→复绿。判官累计四十三。**教训：死 import 也是世代触点——1.21 编译器不报未用 import，跨代上挂前 import 区要当接口面全量过目。下一笔 m473=⑤c2 本体：accepts 与 chainWants 七分支成对落地+垃圾桶两轮垫底真判定+分配器均分+逻辑节点 tick 清运分支。** 16 闸全绿。上笔 m471：C2-⑤c1 在途缓存 + 直连路由落地——1.20.1 第一次「产线接产线」：nodeBufs 每节点在途缓存 + 遗留共享池 + BUF_CAP/bufTypeOk/mergeLegacy 六件、routeOut（蓝本 distribute 两轮垫底同构，垃圾桶壳先立）、accepts 单支（MachineItem·耗料机）、供料侧 hasIn 半（dualCount/dualWithdraw/m340 topUpSource）、hasSinkFor 白耗料护栏（m466 那条的超集，无出线时逐位退化）、detachNode 在途回收、nodeBufs/internalBuffer 存档写读同刀。**两处不照抄显式记档**：①路由尾巴——蓝本装不下走输出缓存/断网喷射，本世代没有，改成把剩余件数**原样回吐**给调用方点折损黄灯，绝不静默吞件；②邻接派生位——弃用蓝本 m179 修订号缓存，改每拍重填复用数组 + 路由直遍历 connections，换掉整条「漏 bump 即静默错路由」回归面。判官五条（直连产线跑通/余量必落仓守恒对账/类型上限拒收零丢件/摘节点回收+下标对齐/存档写读对拍），累计四十一。**教训：javac 默认只报前 100 条错误，拿截断清单跑「自家符号报错=0」等于没验——尺子的量程也是尺子的一部分（m109 家法自己撞上来）。** 16 闸全绿。**下一笔 ⑤c2=NodeTags 上挂（绞杀者第五刀）+ 逻辑节点七分支成对落地**，accepts 方法注里已把蓝本整张分派表逐条列好标了到序刀号，照着划即可。上笔 m470：两代手抄名单对表闸落地（第 16 闸，挂 CI）——把仿写路线的头号漏点变成会红的闸：判据=蓝本名单 ∩ 本世代已建方块 ⊆ 本世代名单，本世代未建的自动豁免并列名（一旦 reg( 登记就当场开始要求进名单）；加项=往 ITEMS 加一条字典零逻辑改动，首批只登记数据线端点名单一项不虚报覆盖面；**尺子自证**：把 m469 修复退回去闸当场变红并点名漏抄两块，还原复绿（m109 家法）。16 闸全绿。上笔 m469：数据线连接修复——数据线连接修复（作者实机截图"线怼上去不伸插头"）——根因①自家 PLUG 名单只抄了 STORAGE_CORE 一条，结构核心/数据面板都不实现 Container 于是恒 NONE 不伸臂（**只是视觉**：BFS 只认方块类型，逻辑一直通，所以躲过了全部判官）；根因②updateShape 原注"双端重算同果"是坏尺子——endFor 的 FTA 分支自带 !isClientSide 闸，客户端本地重算会把插头臂算没，照蓝本改成形状听服务端。修法三件：补名单／加客户端闸／**旧档自愈 healEnds**（BE 首拍重算六面、transient 不落盘、邻块未加载不发强制加载票）。判官两用例（端点名单按计数断言避开 GameTest 旋转／自愈复现陈旧态），累计三十六。**教训：手抄的名单/常量/判定表是仿写路线头号漏点——漏一条不报错、判官照绿、只有眼睛看得出来。下一笔 m470=两代名单对表闸 tools_retro_parity_check。** 上笔 m468：C2-⑤c 小普查与分片稿（零代码笔）——C2-⑤c 小普查与分片稿（零代码笔）——两颗路由脑量清（chainWants 78 行/distribute 31 行/distributeEven 37 行/accepts 51 行+在途缓存四件）；**核心发现：chainWants 与 accepts 是同一张节点类型表的两面，必须成对写成对测**（m131b 只写 accepts 那面 → 拉料恒不通拖到 m132-6 才实锤，移植头号回归源）；缺口三条=在途缓存 nodeBufs 整套不存在／NodeTags 不可挂（6 处 MC 触点含 1.20.5+ 专属 CustomData，但 m451 的 TagItemData 已装好，改出入口即可上挂）／规划器族无本世代实现；取舍=⑤c 只做机器↔机器直连（零 NodeTags 零规划器依赖，盖住 ⑤b 那 11 台耗料机的上游喂料）；分片=⑤c1 在途缓存+直连路由 → ⑤c2 NodeTags 上挂（绞杀者第五刀）+逻辑节点七分支成对落地 → ⑤c3 链式拉料。**下一笔 ⑤c1，可直接开工。** 上笔 m467：透明二次排查（零代码笔）——透明二次排查（零代码笔）——作者复报"进存档后界面还是透明、能直接看到桌面文件"，m452 那刀（改半透明填充为不透明）**打空**。本笔不做第三次盲猜，先做归属审计：全库 GL 状态零触点（RenderSystem/GlStateManager/colorMask/clearColor/blend 全 grep=0）、retro 两屏 15 个色键 alpha 全 0xFF、1.20.1 零 mixin、四方块全 noOcclusion——**本模组没有能写坏窗口 alpha 的代码路径**；"看到桌面"属 OS 窗口合成层现象，但不下"非本模组"结论（m140 前车），改交付分诊协议：**F2 截图分流**（图正常=窗口层/图也透明=渲染真没画）+ **挪走 jar 的对照组**，两刀之内必定分流。等作者回数再动刀。**下一笔 m468=C2-⑤c 小普查（两颗路由脑 chainWants/distribute），已可离线开工。** 上笔 m466：C2-⑤b 落地——1.20.1 生产 tick 吃上料：通用分支补 consumesInputs=true 半（supplyTarget kind1+蓝本按料折算 doCycles+withdraw+扣料即点绿），灯表三句新话（未连供料/缺料带量/类型满），防白耗料护栏（acceptsPlainType 扣料前先验产出仓类型余量，概率产物也计入），覆盖 11 台 defConsume；判官三用例（缺供料红灯/料折算+进出账恒等式/类型护栏零消耗——同步手拍 tick 零配置暴露窗）。下一笔照 m463 分片 ⑤c 机器↔机器连线语义（届时先单独小普查两颗路由脑）。上笔 m465：C2 插队刀（作者实机反馈）——1.20.1 模型与动画归位；m464：C2-⑤a 落地——1.20.1 生产 tick 第一次转起来：StructureCore120 挂 tick（cyclesThisTick 四层预算闸逐位对照蓝本，世代差=无升级恒 1 倍）+kind0 产出边入账+灯表说人话（未连仓红/特种黄/预算剪零黄）+setChanged 去重；StorageCore120 补 implements StorageAccess；白名单挂载 DropRolls+StorageAccess；判官 RetroTickTests 三用例（灯表三色/cobble_maker 确定性对数/维度闸 kind 闸）。世代取舍：自动运转（开停闸随屏侧到序）、产出必须接仓无缓存喷射。下一笔 ⑤b 耗料类。上笔 m463：C2-⑤ 小普查收官（零代码笔）

**作者本地 gradle build 全绿至 c5b5982=m176（2026-08-01 实测：Loom 1.7.4，BUILD SUCCESSFUL 1m1s，
仅两条"已过时 API"注提示非报错；作者自备"拉取并构建"工具直接同步 jar 进 1.21.1 测试实例）——
m129~m176 编译层欠账一次性清账，m173h 热修就此验明（修前的远端 m173 编不过）。**
实机游玩验证按待办池清单跑（编译≠游玩验证，DEVLOG 各里程碑验证脚本照旧）。近期里程碑：

- **m163 三连**：a 抽取量五挡 64→512→4096→32768→262144（撤 BUF_CAP 钳位+精确支路封顶统一
  bufCapL）；b 抽取白名单候选=仓库现有（总线聚合并入精确条目、前10→前400、复用 busIds 通道
  零新协议，选择器走 pickerSrcOverride）；c ItemStackProMax 大堆叠兼容（全库已动态 getMaxCount
  白捡；终端两处"合到一整组"循环加 4096 轮冻死护栏，原版下永不触发）。
- **m162 图标换皮**：垃圾桶/抽取节点用户新版立绘归位。
- **m161 三连**：a 交易机/打折机图标换皮（用户新版 1254² 立绘归位）；b 终端搜索框去黑壳
  （撤原版黑底灰边，CELL底+细边聚焦亮青）；c **跨模组直连**——存储核心挂 Fabric Transfer API
  （ItemStorage.SIDED），Create/MI/TechReborn 等 FTA 管道怼核心即存取，双账本全暴露、
  事务快照回滚、markDirty 推迟提交。截图里粉↓箭头/紫K/右上四钮经像素级排查属 IPN（外模组），留痕在 DEVLOG。

- **m99 升级数学重写**：工作量累积模型。速率=(1+speedGain)^速度级×productionRateMultiplier，
  每 tick 累积、溢出折同 tick 多周期永不触底；并发=直接乘台数(台数×(1+级)×tier)；数量封顶只剩
  "产出只能进内部缓存"时。五条生产分支（自动合成/农场/万能熔炉/通用机/抓物笼）统一。
  config 新增 upgradeSpeedGainPerLevel(0.5)/upgradeMaxCyclesPerTick(20)，configVersion=4。
- **m100 批量取出**：数据面板右键浮层第二行 2组/4组/8组/填满背包；服务端分块取+余量回仓绝不落地。
- **m101 交易所**：图书管理员 10 本好附魔书（绿宝石+书，治愈折扣生效）；列表 4 行滚动窗口；
  附魔书直发背包（仓储按 id 记账会抹组件，绝不入仓）；修双输入交易不扣第二种料的旧 bug。
- **m102 深层采掘平台**：钻石(0.15)/远古残骸(0.05)/深层地质/原矿三件套加权掉落；引子配方
  钻石×2+残骸×2；残骸→万能熔炉→下界合金碎片，量产链打通。
- **m103** 滚轮只在悬停交易列表时翻页；**m104** 深层采掘平台真美术归位，**全库 79 张物品图零占位**。
- **m106 存储终端双修**：a) 修"无存储核心"假红字（0xFFFF 无限哨兵过 short 通道符号扩展成 -1，
  收包端补掩码）；b) 合成终端 AE 式补完——shift 点结果连续合成一整组（服务端权威零预测）+
  网格模板化网络自动补料（学 AE2 CraftingTermSlot 思路，代码自写，LGPL 不抄）。
- **m107 存储终端体检 8 项全落地**：性能（经验属性缓存撤每秒 40 次 BFS/面板 viewer 门控闲置零空转/
  搜索本地化名静态索引）+ 滚动（真实比例滚动条可拖拽、行数属性 id4 同步、滚轮只在存储区生效）+
  合成区（清空回仓按钮 id=3、带组件拒收 actionbar 提示）。
- **m108 三连**：a 绘图名单更正（五张"待重绘"实为用户素材，零待画）；b 交易所/村民合同等 15 基础件
  进超大工作台浏览器（与原版配方逐字一致，修配方书无解锁不显示的发现性死角）；c 面板 cores() 40t 缓存
  （撤每调一趟 BFS，高产线卡顿主治）。
- **m109 量产覆盖四提案收官**（引子模式照 m102）：a 考古工作站（20 考古陶片随机/回响碎片/唱片残片5/
  三稀有唱片/海洋之心/附魔金苹果极低；引子=回响碎片×2+海洋之心×2）；b 末地远征平台（末地石/龙息/
  鞘翅 0.004 极低；引子=末地石×2+龙息×2）；c 试炼农场（试炼/不祥钥匙/不祥之瓶/1.21 三陶片/
  重锤核心 0.008 极低；引子=试炼钥匙×2+不祥之瓶×2）。三台均 40t 周期、六件套断言过、
  签名多重集全表 62 条唯一、贴图程序占位挂绘图名单。
- **m110 画布概念图收尾**：a 小地图（顶条「地图」开关；节点分类配色概览+视口白框+点击/拖拽跳转，
  几何快照防抖，纯客户端零协议）；b 节点齿轮设置+单节点启停（状态存节点栈 NBT "np"；
  NodePausePayload 照开关包样板；tick 最先判不攒进度、闸门/链需求/收料四处消费；
  右键菜单抽成 openNodeMenu 与标题栏齿轮图标共用，暂停=压暗+黄字角标+黄灯）。

## 架构速查（改哪类问题去哪个文件）

- **组合玩法/物流语义**：`机器组合.md`（m134 照代码实证：三层物流/四逻辑节点/闸门连锁/链式拉料/蓝图集/防坑）——改路由代码须同步更新

- **生产/升级/tick**：`block/StructureCoreBlockEntity.java`（~1900 行核心；五分支 tick、
  cyclesThisTick/runningCount/rollDrops、供料 supplyFor/入库 depositFor/分发 distribute、链式需求 chainWants）
- **机器定义/注册**：`machine/Machines.java`(掉落表) + `registry/ModItems.java` + `machine/SuperBenchRecipes.java`(引子签名配方)
- **配方规划器**：合成=`machine/CraftPlanner.java`；熔炼=`machine/SmeltPlanner.java`；
  酿造=`machine/BrewPlanner.java`(m131b,BFS原版酿造注册表,目标串「药水id|p/s/l」,缓存挂SERVER_STOPPED)；
  附魔=`machine/EnchantPlanner.java`(m132,附魔动态注册表,目标串「附魔id|等级」,成本=书+青金石3×级+经验B×级×25点从核心经验池xpPool扣,缓存同位)
- **存储网络**：`block/StorageCoreBlockEntity.java`（connectedCores=贴邻/数据线 BFS4096；类型默认无限 m98）
- **存储终端**：`screen/DataPanelScreenHandler.java`(按钮 id=1000+格×10+档位0..8；id6=右键整组合成) + `client/DataPanelScreen.java`；合成网格 m126a 起常驻 `block/DataPanelBlockEntity.java`(craftGrid, NBT 持久化)
- **交易所**：`machine/VillagerTrades.java`(纯Java,Trade record 含 enchant 字段) +
  `block/TradeCenterBlockEntity.java`(employ/trade/heal) + `client/TradeCenterScreen.java`
- **画布 UI**：`client/StructureCoreScreen.java`（节点/总线/机器库侧栏/视图控制）
- **网络包**：`net/*Payload.java`，注册与接收器在 `Sdzjz.java`
- **美术管线**：物品图 128×128 RGBA 透明底进 `textures/item/`；归位=裁边→留4%边距补方→LANCZOS 128→
  断言尺寸/模式/覆盖率→勾 `绘图名单.md`

## 立项候选（作者已拍板方向，动工前再对细节）

- **"小说系统"金手指（作者 2026-08-08 定调：先记录不往里做，可做联动）**：系统流包装——
  绑定宿主/"叮！"提示（m311 随身仓库已用此风格）/宿主面板（总产能/仓储总量/产线数纯展示）/
  签到/系统任务（生产目标发奖）/系统商城（绿宝石或经验池兑换）。**联动方向**：与随身仓库、
  交易所、画布经验池共用经济口径；奖励表/定价/任务池等数值设计留作者拍板后再排里程碑。
- **随身仓库后续**：GUI 取物屏（m312 在做）；压缩包第三级 ×262144（m241 级联架构现成，
  差一张档位边框贴图，作者点头即做）。

## 待办池（按优先级）

0. **【P0 现场】透明分诊未回数（m467 立）**：作者复报"进存档后界面还是透明、能看到桌面文件"，
   m452 那刀打空。**等两个数**：①F2 截图长什么样（正常=窗口合成层／也透明=渲染真没画）；
   ②挪走 sdzjz jar 的对照组还透不透明。回来即定归属再动刀，别再凭观感改色值。

0. **外部审计②余账对表（m345 登记；P0 CraftPlanner=m343 已销、P1 玩家扫描=m344 已销，均逐行对源核实后才动的手）**：
   - **属实待做**：①SmeltPlanner 稳定选序=m346 已销，余"按库存挑输出"升级项待作者拍板；②tick 头维护段=m348 已销（扫描分档+看门狗冻结坑，区块票 20t 管停机转变沿属必要留驻）；③孤儿 claim 渐进核销=m347 已销；④DataPanel"重复解析"=已有等效实现（cores() 自 m108c 40t 缓存+幽灵重建，m290 升 public 共链路），不另做快照层；⑤BrewPlanner 全图一次 BFS=m425 已销
     （前驱树缓存双侧同刀，"首开选择器偏重"归因经对表不准——真账是不可达目标必扫全图）；⑥bigStacks 兼容分档（OFF/VANILLA_ONLY/FULL，
     审计标 P1 兼容风险，涉及三方模组 int 假设——机制改动需作者拍板）；⑦portableVaultSlot 双端
     一致改握手或恒留槽（m332 立档项，审计再点名）；⑧生产预算默认值出 safe/normal/high 预设
     （现四键天文数字=零变化哲学，审计建议预设化——产品口径待作者拍板）；⑨Machines.java 数据
     驱动化 json（长期）；⑩SCBE 拆 Runtime 家族（绞杀者 m180 开线、m426 第二刀拆净 26 垫片、m427 第三刀升级折算族迁 NodeUpgrades、m428 第四刀掉落结算迁 DropRolls，GraphNbt 线收官 m430/m431，mA3 缓做，SCBE 3965 行；六漏斗接口化收口 m433~m435；P-A 收官、P-B① bootstrap 落地、P-B② 刀①落地；m442 构建插件多版本适配（清单契约入仓）；刀②账本=m443/刀③ Create 验收=m444 在队，长期工程项继续）；
     ⑪Canvas 增量快照/复用集合 GC 优化（P3）。
   - **已有等效实现/部分失效**：审计⑬ Mixin method="*" 建议加 codec smoke——GameTest 九号用例
     m310 已是该判官；⑰⑱ GameTest/CI 已覆盖其点名的多数（withdraw 竞争/事务/精确件），其补测
     建议中"Ingredient 替代材料"=廿六号已加，"多熔炼配方选择稳定"随①做，"512 节点压测矩阵"=
     /sdzjz bench 已有待作者实机跑，"多人同 Panel"=十四号已有，"orphan claim 重启"=框架内做不了
     （m326 #6 同口径）留实机。
1. 验证 m112-m163。**m163**：五条见 DEVLOG（换挡五挡/白名单窗只列仓库现有含精确条目/
   ISPM 上限 10 万实测拿一组与 4096 轮护栏）。**m161**：b 搜索框观感（无黑壳/聚焦亮青/resize 留字/中文输入照常）；
   c 跨模组直连六条见 DEVLOG（需装任一 FTA 管道模组如 Create 实测存取/精确抽取/类型上限背压）。
   **m161 后续三件（用户拍板即上）**：①漏斗对接（核心实现幻影槽 SidedInventory，插入即入账、
   禁抽取，内部 BFS 无自冲突已核实）；②反向直连（pushOutput 补 ItemStorage.SIDED.find 分支，
   往 MI/TechReborn 这类只暴露 FTA 的机器里塞料）；③EMI 插件（超级工作台配方进浏览器，需加
   EMI 编译依赖）。**m133 强制加载**：开机核心跑远 500 格回来产量不断档；/forceload query 见核心
   区块、停机 15 秒后消失、拆核心消失；重启服务器不靠近产线仍跑（旧存档升级后需先路过一次核心
   建立标记）；跨维度端点持续入库；config coreChunkLoading=false 复旧行为。**m130 精确存储**：附魔书/损耗钻镐/GM机器 各存取一轮组件应无损；
   同一种书不同附魔应分行显示各自计数；批量取出（右键浮层）对附魔书应可用；"类型 X/Y"计数含精确条目；
   带组件物品 shift 存入不再弹"不入仓"提示。**m128（m125三修代码补推——m125 那笔提交只有文档没有代码，沙箱宕机丢失，已按 DEVLOG 重建）**：验证项即原 m125 三条（融合聚敛/取出保阶位/批量升级不卡）+ 融合入口现按"全画布同类同阶总数≥4"显示。**m127**：a 终端左侧立牌已撤、物品 tooltip 显示"DY：乔大仙"；
   b 结果格防线——终端/超级工作台的结果格：右键（结果数>1 时）、Q 键、双击收集都不应再取走部分产物
   （终端右键=整组合成到光标是 m126b 正路；超级工作台右键应无动作），左键空手整取/Shift 照常，
   重点盯"取一半白丢一半"绝迹。**m126 存储终端合成双修（AE2 源码实拉对照后落地）**：
   ①网格常驻——终端摆好配方关界面再开，9 格原样还在；退出重进世界仍在；拆面板方块网格内容散落不吞；
   两人同开一个面板看网格同步；②右键结果格=连续合成一整组到光标（光标拿着同类=续装到满），
   不再出现"右键取一半白丢一半"；左键单取/Shift 整组进背包/清空按钮不回归；
   ③shift 整组连打应比之前顺（配方缓存撤全表扫）。其余：m125 审计三修：①画布散放 4 台同类 ×1 机器→任一节点菜单"融合"应聚敛合成，
   被抽空节点消失、其升级回背包、连线不错乱；②取出超级/GM 机器再放回，阶位前缀仍在；
   ③Shift 批量塞升级不再瞬卡。其余：m117 换肤第一步：四屏观感应与之前几乎一致（刻意变化仅两处：终端/交易所的
   按钮悬停色并入画布蓝族、超级工作台多了全屏暗底），重点看有没有哪块颜色明显错位；
   后续换肤只改 client/SciSkin.java；m118 已接 slot.png/button.png 贴图管线
   （简易稿复刻旧观感），实机看终端槽位/画布按钮有无错位拉花，用户 GPT 精修图同名覆盖即换肤。
   m116：白名单远古残骸的 面板→过滤器→熔炉组 链，残骸碎片应从 ~100/秒跳到
   万级/秒（受熔炉自身产能上限）；打开过滤配置窗，已选项应固定显示在最前排。
   m115a：升级格 Shift+左键应一口气吃光背包同类升级（至多64）、Shift+右键全取回；
   m115b：断网首次喷射有聊天警告、接回存储后再断会再提醒；/tick 或大量实体压到 >45ms 看机器全线
   黄灯暂停+提示、流畅后自动续跑；>60ms 喷出的掉落物被清而玩家扔的不动。
   m114 断网喷射（现 2t 一组≈10组/秒）：裸核心+机器不接任何存储，顶面应急速喷掉落物，
   贴上箱子/面板 2 秒内停喷改落库，拆掉又恢复喷射；挂机十分钟看掉落物堆不失控（原版合堆+消失兜底）。
   m112（修 m111 整页清空回归）：复现视频操作——拿着东西 shift 点背包物品入库，展示区不再
   全黑；取整组/半组后格子即时显示余量；shift 入库/取出各连打十几下看有无错乱；其余按键表照验（m113 终版：
   空手左键=抓整组、空手右键=数量浮层(一组/拿满)、拿着左键=全存/右键=存1、Shift+左键=一组入背包）。
   m110 地图/暂停顺带。
2. **已拍板路线（m129 用户确认）全部落地**：精确存储(m130✅) → 酿造塔(m131b✅) → 附魔工厂(m132✅，均待编译验证)。
   m132 验证：超级工作台 附魔台+书架+书+青金石 合出附魔工厂；画布节点菜单"选择目标附魔"开**行式**
   选择器（搜"锋利"出 锋利V..I 五行，图标+罗马数字名，当前目标绿框）；选 锋利V 挂网络+同画布
   烈焰人塔攒经验，应吃 书1+青金石15+经验125/本（扣的是画布经验池——工厂与"领取经验"按钮抢同池，
   属设计特性）出附魔书入库为精确条目（终端分行、与交易所同款书合并计数）；池空/缺料红灯；
   断存储时输出缓存书不混堆不变裸书；上游连线直喂书/青金石可行；经验修补/诅咒/模组附魔均可选；
   m132-6 顺修验证：面板→过滤器(白名单书+青金石)→附魔工厂 链应能拉料生产，同构验酿造塔
   （过滤器喂酿造材料——m131b 这条此前恒不通，chainWants 漏接实锤）。
   m131b 验证：超级工作台 brewing_stand×2+blaze_rod+nether_wart 合出酿造塔；放画布点徽章开药水选择器
   （三形态按钮/搜索/绿框回显）；选 强化迅捷·喷溅 挂网络后应吃 玻璃瓶3+地狱疣+糖+萤石粉+火药+烈焰粉
   （4步→每5批1粉）出3瓶入库为精确条目（终端分行显示）；断存储时输出缓存药水不变裸瓶不混堆；
   力量药水材料粉+燃料粉双账扣数对得上；上游机器连线直喂材料可行（accepts）。
3. **队列已拍板（用户"按顺序来"）**：G组杂项(m135✅) → 凋灵机(m137✅) → 幽匿线(m138✅) → 砂轮祛魔(m139✅)——**队列四项全部收官**。
   m139 验证：砂轮2+书2合机；仓里塞附魔书挂机→书变裸书+经验池按表涨（锋利V一本+45）；
   纯诅咒书纹丝不动；断网络红灯；附魔工厂+砂轮同挂池不凭空涨（回收<成本，防泵封顶0.8×）。
   m140（实测反馈轮）：崩溃排查结论非本模组（栈零 sdzjz 帧+实体触点全审+毒实体在存档后出现
   重进即回滚，详 DEVLOG m140）；砂轮加青金石退款 1/级（33%有损）；图标裁切路线废弃、
   12 张全改规整机身占位（用户判词"异形"）。
   **m141 图标收官**：用户重发八张概念图（三张新入库），连通域抠图路线归位 12 张
   （矩形粗裁+alpha连通域剔邻居残片+直切沿框柱框线，256/128 双档亲眼核对），m140 异形清零；
   附魔工厂顺带从 m132 旧裁切换整图。**全库唯一待画：grindstone_recycler（砂轮）**，
   需作者单独出一张单机身透明底立绘。
   **m142 崩服修复（最高优先验证）**：两轮崩服真凶实锤=本模组 m133 毒区块票——OUTPUT_IFACE
   哨兵被 refreshForceChunks 解成天边区块发加载票，radius 邻块回卷打崩实体管理器；m140
   "非本模组"结论误判已撤回（错在把哨兵当真实端点+以为需要毒实体）。三层修：源头跳哨兵+
   合法性校验 / ticket 末端拒边界外票 / NBT 读入清洗存量毒条目（老存档进图即自愈，无需回档）。
   **m143 机器合并**：用户拍板"整合到一起"——凋灵线5台→wither_farm一台、G组3台→g_misc_machine、
   幽匿3台→sculk_line（经验20/轮总账不变），图标=概念图整图；核心NBT读入 MERGED_IDS 重映射
   旧id防节点丢失与 inputBuf 错位；背包/箱子里旧机器物品会消失（画布上已插的自动换新）。
   下一步候选（待用户拍板）：①**优先**：编译装新包进上次崩的存档验 m142（应不再崩+/forceload query
   只见核心自身区块）；②验 m143（三引子/旧档节点自动换新/凋灵机多产物/sculk_line吃池20/轮/
   创造栏只见3台）；③游戏内验证 m136-m141；④验 m144（砂轮新图标/合同悬停四步用法+职业折扣动态段）；④b 验 m166 配方=原版建造清单（刷铁机=实拍进货单+村民僵尸双笼、唱片机双笼、小黑塔=末地石+命名牌+矿车，七条见 DEVLOG；m165 三档材质盘已废、角标保留；m167+m168 刷石机双轨=入门款俩桶角标Ⅰ照旧 + 新机'百万刷石机'吃用户520万/h蓝图实测蒸馏123件18种全表最重/岩浆桶10对齐四千桶/产能722每10t≈520万每h、角标Ⅲ，验证五条见 DEVLOG m168；m169 双轨第二台'40核刷铁机'=用户40核蓝图实测蒸馏119件18种/村民僵尸双笼/产能40每40t=单核×40、角标Ⅲ，验证六条见 DEVLOG m169；m170 双轨三连'200万史莱姆农场/140猪灵交易场/920万船吸刷怪塔'=三张litematic实测蒸馏82/68/116件、产能200万球每h/46万件每h吃16.8万金/920万件每h、全Ⅲ档，验证六条见 DEVLOG m170；m171 双轨两连'沼泽刷怪塔(女巫×64≈101万件每h)/强力守卫者农场(×16≈9.2万件每h,zip电梯+击杀舱+仓储三件合账)'+图标政策定案=工程款永久复用基础款只改名、五张待画撤单，验证五条见 DEVLOG m171；m172 双轨九连+m173 特殊分支双工程款+m173h 热修(远端m173曾混入重复定义编译必炸,已修;javac.args 倾倒文件清库)+m174 新线六连"动物农场(五笼兔子线)/凋灵玫瑰农场(26k对表)/龙池杀凋机(付费快档星720/h)/屠龙炮(龙息+500xp每轮全库最强经验引擎)/疣猪兽农场/弱加载盾构机(吃TNT出地形18.8万块每h)"——38张litematic分拣收官,验证七条见 DEVLOG m174；m175 CI备好=GitHub Actions 三道闸(PAT缺workflow权限,工作流暂存docs/ci/ci.yml,重发带权限PAT或网页端手动放入.github/workflows即启用)(配方校验+全库资源审计+真编译出包上传artifact)——【待编译验证】欠账自动化闭环,首跑若红即是m112以来第一次真编译报错,贴回即修；m176 文档漂移清理=README机器数94标记块+fabric.mod.json撤电力系统+优化与缺口挂历史快照声明+新tools_docs_sync.py唯一数据源生成机器清单.md并入CI防再漂；m177 性能尺子=/sdzjz profile core|network|reset + dumpgraph(OP2),核心tick环形窗计时+路由供料链查同步逐点计数,增量同步与执行计划改造的前后对表基线,验证七条见DEVLOG m177；m178 错误解释=节点阻塞原因通道(nodeReason平行nodeStatus走NBT同步,statR+转绿清因,全库22红灯3黄灯配人话,命名缺料带账目"缺料：铁锭(仓3/需8)",卡面y+38常显红字金字徽章副行让位,零新协议,验证七条见DEVLOG m178；m179 编译执行计划=hasOut/hasIn/outT按topoRev修订号缓存零分配复用(五处突变点bump+长度兜底+派生列表只读已核),profile core行尾报编译次数稳态应≈0增长自带漏bump回归检测,验证五条见DEVLOG m179；m180 绞杀者第一刀=25个节点标签纯函数外迁node/NodeTags(方法体一字未改),SCBE原位同签名垫片零调用点改动回归风险按构造为零,SCBE 2921→2871行,后续每刀一里程碑,验证三条见DEVLOG m180）；⑤验 m160 三件内置（抽取白名单/阈值自动启停/安全桶,六条见 DEVLOG）与 m159（换挡/升级生效/状态栏裁剪）与 m158（三种摆法等价销毁）与 m157（卫星mesh归位/退料找回失踪货/搬仓）与 m154 抽取节点（重点背压闭环）与 m153（过滤→垃圾桶定向清仓/牧师青金石交易/深掘平台青金石）+复验 m152 卫星节点（材质/位置/欧拉序三修后：锅材质双面可视/馈源臂朝向/UV镜像三点）、m150 垃圾桶（两轮分发垫底/不抽仓/已吞计数）与总线卡观感、m149 机器二级界面（熔炉选烧什么/多产物机选产物/竖排整理，五条见 DEVLOG）与 m148 菜单3A化（弹入动画/行图标/危险项垫底红显/点选音，五条见 DEVLOG）与 m145 打折机、m146 无限交易机（交易机六条验证见 DEVLOG m146——注意 v1 交易表无'线收购'条目，刷线机直连演示前可一行补 t(string,20,emerald,1)）；⑥待办池其余。
   m138 验证：深暗之域三配方合三机（催化体+幽匿3/传感器2+幽匿2/尖啸+传感+幽匿2）；三机全吃
   画布经验池（催化2/轮、传感与尖啸9/轮），池空红灯、与附魔工厂/领取经验抢池属设计；催化机出
   幽匿块流水+催化体0.08；幽匿产物无组件、出线走正常物流（与山羊角出线无视相反，别测串）。
   m137 验证：超级工作台 星+凋骷头2+灵魂沙 合出凋灵机（另四台：三色灯+岩浆膏/角2+雪块2/鳞2+蛛眼2/
   双花籽+苔藓2）；凋灵机挂网络出下界之星（≈50秒/星基准）且画布经验池在涨（50/星）；山羊角机产出
   8 变体随机、终端按变体分行显示（精确条目）、给它拉出线时产物仍进仓（组件产物出线无视属设计）、
   断存储时缓存里角不混堆不变裸角；青蛙灯三色齐出；模组若加自定义山羊角（GOAT_HORNS 标签）自动进掉落池。
   m136 连线验证：画布连线应为丝滑发光缎带（旧为点阵方块）、两端发光圆点、脉冲顺流向、缩放 0.4~2.5
   线宽恒定、总线连线从卡底垂直进出且从节点卡片下层穿过；盯点 DrawContext.getVertexConsumers/
   RenderLayer.getGui/vertex(Matrix4f,f,f,f).color(int)。
   m135 验证：超级工作台 蛛网2+线2 / 孢子花2+苔藓2 / 紫水晶块2+方解石2 合出三机；放画布产出入库；
   蛛网机/孢子花圃两张为程序占位（G组概念图一体式构图，分区待视觉核对，见绘图名单）。
   幽匿线设计输入：概念图含 催化体/传感器/尖啸体 三塔（见绘图名单 m131a 条）——原版链条是
   催化体吃经验长蔓延，可考虑接核心经验池（与附魔工厂同池竞争先例已开）。
4. 组合玩法/物流语义手册：`机器组合.md`（m134）——改路由代码须同步更新。
4. 真美术：m147 再度收官（打折机/交易机立绘归位），零占位零待画。后续新机器出图规格照旧：单机身透明底立绘，同名覆盖即换。

## 用户使用速查（作者常问）

离人生产（m133）：核心**开机即强制加载**自身与存储端点区块，人走了照跑、服务器重启自动恢复
（旧存档升级后需先路过一次核心）；不想加载就停机或 config 关 coreChunkLoading。
村民合同：工作台 纸6+面包2+绿宝石1；交易所=绿宝石4+铁锭4+核心模块，**必须贴存储核心或数据线连通**，
就业耗网络里 1 个职业工作方块（图书管理员=讲台），治愈耗金苹果每级-10%最高5级，附魔书进背包不进仓。
