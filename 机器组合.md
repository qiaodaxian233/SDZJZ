# 机器组合手册（m134 · 照代码实证，不是照感觉写）

> 目标读者：玩家/作者。每条语义都对着 `StructureCoreBlockEntity` 的实际实现核对过；
> 「为什么这样接」写在每张蓝图里。改了路由代码请同步更新本文（架构速查里挂了链接）。

## 一、三层物流，一张图

产出从机器出来后，按优先级走三条路，**永远有兜底、绝不堵死**：

```
机器节点 ──出线──▶ 下游机器（画布连线，进它的输入缓存，按需下发）
   │ 吃不下/没人要
   ▼
定向连线（机器绿口 ↔ 存储节点：定向产出/定向供料，跨维度可用）
   │ 没接定向
   ▼
全局默认路由（贴邻/数据线/无线 连通的存储网络，自动入库）
   │ 全都连不到
   ▼
断网喷射（顶面 2t 一组≈10组/秒，自限；贴上箱子/面板 2 秒内恢复落库）
```

- **出线=按需下发（distribute）**：只把下游机器「吃得下」的物品送下线（accepts 门控），
  吃不下的部分当场走定向存储/默认路由——下游缓存永远不会被垃圾堵死。
- **供料两路**：机器有「存储→自己」的定向供料边就吃边上的；没有就吃全局网络。
  上游机器出线直喂=进本机输入缓存（每种在途上限 BUF_CAP=20 万），优先于两者。
- **缓存类型上限（m270）**：单节点输入缓存最多 256 种物品（config maxBufferTypesPerNode，0=无限）。
  满型后拒收**新类型**（已有类型照常合并）：泵料干脆不抽（物品留仓）、分发的残量走定向存储/
  默认路由——与「吃不下」同款去处，零物品损失。
- **全服生产预算（m302）**：所有核心每 tick 生产周期总量共享 config maxRecipesPerNetworkTick
  （默认 1_048_576 极高≈不束缚，0=无限）。耗尽只欠不丢——工作量照常累积下 tick 续；本 tick
  一个周期都没吃到的核心，下 tick 持**保底 1 周期先食权**（饥饿名单），tick 序靠后的核心
  不再恒饿。单核心另有内层预算 maxRecipesPerCoreTick（m270），先核内后全服双层封顶。
- **区块生产预算（m324）**：maxRecipesPerChunkTick 真接线——同区块全部核心每 tick 共享一份
  区块预算（四层封顶=节点 cap→核内→区块→全服），耗尽同口径只欠不丢。区块层不带公平名单
  （同区块内按 BE tick 序竞争），定位是"大量核心挤一个强加载区块"的管理员钝闸；0 或负=关，
  默认 262144 极高不束缚。
- 万物账本按物品 id 记（普通账本）；**带组件产物（药水/附魔书）只入 m130 精确账本或
  输出缓存，出线一律无视**——id 账本会抹组件，这是保护不是缺陷。

## 二、四种逻辑节点（组合的骨架）

| 节点 | 收料语义 | 转发语义（5t 一拍清运自己的缓存） |
|---|---|---|
| 过滤器 | 白名单放行/黑名单拦截 | 放行的沿出线下发；**拦下的自动回仓**（定向存储/默认路由），不丢不堵 |
| 开关 | 开=收，关=不收 | 开=直通转发；关=持料不动（缓存封顶后上游自然停） |
| 传感器 | 开闸=收 | 监测**仓库里某物品数量**：`<阈值放行` 或 `>阈值放行`；关闸持料不动 |
| 分配器 | 什么都收 | 在所有「吃得下」的下游间**均分**（余数轮转）；分不出去的回仓 |

三条全局规则：
1. **闸门连锁**：一台机器的全部出线目标都是关闸（关的开关/关的传感器/暂停节点）→
   上游整台黄灯暂停，不白产不塞库。
2. **链式拉料（m92/m116）**：任何逻辑节点接了「存储→自己」的供料边，就会替下游从仓库拉料——
   拉的只有下游机器**真实需求**（合成机=目标全部候选配方的材料并集（m234：同目标多配方时谁的料齐做谁，
   装 ProjectEF 等模组后原版配方优先；m235：节点菜单可手选固定某条配方，手选后只拉/只吃那条的料；
   m343：并集含**槽位替代材料**——"任意木板"类配方全部候选都算真实需求且生产真吃得下，组内按
   原版候选序贪心取用，仓里只有云杉木板也照做；开关 craftIngredientAlternatives 关=回旧"只认首选"，
   消耗与路由永远同口径）、
   熔炉=可熔炼物、酿造塔=材料+燃料、
   附魔工厂=书+青金石、消耗机=定义输入；支持逻辑节点串联，深度 8 防环），5t 一拍、
  每种在途 4096 上限。**这是把吞吐从「机器自己 40t 拉一次」提到万级/秒的关键**。
3. **暂停即隔离（m110b）**：单节点暂停=不收不产不转发，上游视同关闸。

## 三、产能三轴 + 阶位

- **速度**升级：速率 ×1.5^级，溢出折同 tick 多周期（永不触底，m99）。
- **数量**升级：单周期产出 ×(1+级)。
- **并列**升级：同时运行台数 = 节点台数 ×(1+级) ×阶位倍率。
- **融合（m125/m128）**：画布同类同阶总数≥4 → 节点菜单「融合」聚敛合成高一阶
  （普通→超级→神级→GM），取出/放回阶位不丢。
- 经验：刷怪/熔炼机器按原版经验值攒进**画布核心经验池**，画布领取；附魔工厂从同池扣。

## 四、组合蓝图集（拿来即用）

### 1. 矿→成品直线（下界合金链，m102 打通）
```
深层采掘平台 ──▶ 万能熔炉(残骸→碎片) ──▶ 自动合成机(目标:下界合金锭)
```
为什么：出线按需下发——熔炉只吃可熔炼的残骸/原矿，钻石等其余掉落自动回仓零配置。

### 2. 稳压线（防爆仓/防饥荒）
```
任意高产农场 ──▶ 传感器(监测:该产物, <100000 放行) ──▶ (直通回仓)
```
为什么：传感器监测的是**仓库存量**。仓里到十万就关闸→上游黄灯整台停（闸门连锁），
低于阈值自动续跑。比手动开关省心，服务器负载也稳。

### 3. 分拣线（一产多用）
```
刷怪塔 ──▶ 过滤器(白名单:火药) ──▶ 自动合成机(TNT)
```
为什么：白名单放行火药进合成机，骨头/腐肉等**拦下的自动回仓**——一条线同时供产线和仓库。

### 4. 均分线（一供多耗）
```
全自动农场(甘蔗) ──▶ 分配器 ──┬▶ 自动合成机(纸)
                              └▶ 自动合成机(糖)
```
为什么：分配器在吃得下的下游间均分，余数轮转；某台停机/暂停，份额自动让给其他台。

### 5. 万级吞吐熔炉组（m116 实测姿势）
```
数据面板(供料边)──▶ 过滤器(白名单:远古残骸) ──▶ 万能熔炉×N(高并列)
```
为什么：过滤器接「存储→自己」供料边=链式拉料，5t 一拍替熔炉组从仓库抽料，
残骸碎片从 ~100/秒跳到万级/秒（受熔炉自身产能上限）。

### 6. 经验+附魔全自动闭环（m132）
```
烈焰人塔(攒经验池)         书/青金石(仓库或上游产线)
        │                          │
        └────── 同一画布核心 ──▶ 附魔工厂(目标:锋利V) ──▶ 附魔书精确入库
```
为什么：附魔耗的是**本画布核心的经验池**——刷怪塔必须和附魔工厂在同一核心画布上。
锋利V=书1+青金石15+经验125/本。想给自己留经验：先点「领取」或暂停工厂（同池竞争）。

### 7. 药水线（m131b）
```
地狱疣农场 ─▶┐
甘蔗→糖 产线 ─▶ 酿造塔(目标:强化迅捷·喷溅) ──▶ 药水精确入库
烈焰人塔(燃料粉+强化粉双账) ─▶┘
```
为什么：酿造塔 accepts=计划材料+燃料，上游可连线直喂也可全走仓库；
力量药水的材料烈焰粉与燃料烈焰粉分账不混。

### 8. 跨维度离人基地（m133）
```
主世界：结构核心+机器群(开机=自动强加载)  ⇄  下界：存储核心仓库(端点有期票自动加载)
```
为什么：核心开机就钉住自身区块（重启自恢复），并给存储端点区块续票——人去末地打龙，
主世界产线照跑、下界仓库照收。停机 15 秒全部自动卸载。

### 9. 抓物笼定制刷怪
```
抓物笼(装目标生物,超级工作台合成对应机器) 或 直接放画布产该生物掉落
```
为什么：掉落表跟生物走（MobDrops），凋零骷髅头/潜影壳这类原版难刷的走这条路。

## 五、边界与防坑（都是刻意设计，别当 bug 报）

1. 带组件产物（药水/附魔书/带阶位机器）**出线无视**、机器不吃精确件、合成终端不自动补精确件。
2. 经验不是物品：不走线、不占 accepts；附魔工厂缺经验=红灯（画布经验池数字自查）。
3. 农场类（免费产出）不吃任何供料，给它连供料边没意义。
4. 断网喷射自限≈2 组/秒且会警告一次——这是兜底不是物流方案。
5. 交易所/附魔书直发背包是肌肉记忆保护（m101），现在手动可存仓（m130 精确存储）。
6. 传感器监测的是**存储网络存量**，不是节点缓存、不是产量。
7. 下游全暂停≠上游报错：黄灯=闸门连锁在正常工作。
8. 交易所合同有等级（m333：新手→大师，交易攒经验、高档交易到级解锁；旧合同按大师接管不没收）；
   **交易机不受等级闸**——自动侧平衡在机器造价与预算，已有产线零影响；目标串"职业|序号"序号锚定，交易表只许尾追。
9. 连线喂料语义（m340 改版）：**连线优先、显式存储供料线补足**——机器同时接"机器入线+存储供料线"时，
   先吃线上来料、缺口自动从供料线补（旧版二选一：接了线就把仓当空气，实锤"第二台不生效"）；
   只接线不接供料线=只吃线（老口径不变）；隐式网络永不自动补（防意外吃库存）；**熔炉族除外**
   （"接什么烧什么"补上仓=误烧库存，m173 防线不动）。开关 supplyTopUp。
10. 无限复制机（m334）：母本压阵（网络里 ≥1 件目标，不消耗）+ 经验池付费（duplicatorXpPerItem/件）；
   **组件不复制**（附魔书/药水复制出来是素体，第 1 条物理不开洞）；只吃经验不吃料——不收线、不拉链式需求。

## 六、研究中发现并已顺手修掉的组合断点

- m132-6：`存储→过滤器→酿造塔/附魔工厂` 链式拉料此前恒不通（chainWants 漏接，m131b 遗留）——已修。
  新机器接线从此按**五件清单**：tick 分支 / accepts / setNodeTarget / 客户端 / chainWants。

## m150 垃圾桶节点（路由语义变更，按规矩同步）
- **accepts**：垃圾桶对任何物品返回 true（连啥吞啥）。
- **distribute 两轮分发**：第一轮跳过垃圾桶把料分给正常目标，第二轮才喂垃圾桶——
  **垃圾桶永远是最后去处，与连线先后无关**。典型接法：机器→过滤器(白名单)→面板/存储，
  机器→垃圾桶——过滤器收想要的，其余自动流进垃圾桶销毁。
- **不进中继拉料回路**：仓→垃圾桶 直连不会抽仓（防手滑清空仓库）；过滤器也不会替垃圾桶
  向仓拉料（chainWants 无垃圾桶分支）。垃圾桶只吞"推送"来的。
- 卡面显示累计吞噬量（NBT "tc"）。

## m153 垃圾桶链式需求（路由语义变更，按规矩同步）
- **chainWants 补垃圾桶分支**：垃圾桶作为终端消费者对一切物品返回"想要"。
  由此：仓→过滤器(白名单)→垃圾桶 = 定向清仓销毁（白名单即玩家授权，只抽名单内）；
  仓→开关→垃圾桶 = 手闸清仓线（开=全抽销毁，玩家显式搭的就是这个）。
- **不变的边界**：仓→垃圾桶直连依然不抽（拉料回路节点类型清单不含垃圾桶）——
  防手滑的 m150 底线原样。

## m154 抽取节点（路由语义变更，按规矩同步）
- **主动泵**：拉料回路新 pump 分支——抽取节点开着时**无条件**抽上游供料仓入自身缓存
  （不问 chainWants），每种封顶 4096 与逻辑节点同拍。想选择性搬运：抽取→过滤器→目标。
- **下发无默认路由**：只推给"收的"出线目标（垃圾桶两轮垫底照旧），推不出去留缓存
  背压停抽——绝不抽出来又存回同仓空转刷账。
- **中继语义**：accepts 开=收关=不收；chainWants 关=关闸；下游全为关着的抽取节点时
  上游整台暂停（closedGate 并入）。启停复用开关的 NodeSwitchPayload 收包口。
- 典型接法：仓A→抽取→仓B（搬仓）；仓→抽取→垃圾桶（全清仓，比开关线更明确的主动语义）；
  仓→抽取→过滤器→机器（强制喂料）。

## m155 精确账本抽取（路由语义变更，按规矩同步）
- 泵的普通拉料只碰普通账本；带组件物品（山羊角/附魔书等精确账本条目）**只在
  「该 id 的出线链通向垃圾桶」时**被抽取（chainEndsInTrash 尊重过滤白名单/开关/抽取闸），
  抽出即抹组件转普通 id 计数——反正终点是销毁，抹组件无损语义。
- 出线链不到垃圾桶（如 抽取→仓B 搬仓）：精确物品**纹丝不动**——节点缓存按 id 记账
  带不动组件，抹组件搬运=毁物，不做。

## m157 抽取节点语义修订（路由语义变更，按规矩同步）
- **没有去处的不抽**：泵拉料前判目的地——出线机器目标当下肯收（过滤白名单在 accepts 生效
  →只抽名单内）才抽该 id；有定向存储出线（抽取→仓B）=搬仓模式全抽。m154 的"无条件抽"
  会把全网络吸进缓存囤着失踪（每种 4096），废弃。
- **歇工退料**：抽取节点关机时把自身缓存沿默认路由退回存储——缓存不留黑洞，
  被误吸的货点一下"停止抽取"全数找回。
- **搬仓打通**：开机时机器目标推不完的余量走定向存储出线（明确目的地），
  仓A→抽取→仓B 成立；无任何出线时余量留缓存背压停抽。

## m158 精确账本抽取推广（路由语义变更，按规矩同步）
- 精确账本条目（山羊角/附魔书等带组件物品）的销毁抽取从"仅抽取节点"推广到
  **任意逻辑节点的供料边**——授权闸不变：该 id 的出线链必须通向垃圾桶
  （沿途过滤白名单/开关/抽取启停任一关断=不抽）。
- 由此三种摆法全部成立且等价安全：
  仓→过滤(白名单)→垃圾桶；仓→过滤→抽取→垃圾桶（抽取=启停阀）；仓→抽取→过滤→垃圾桶。

## m160 三件内置（路由语义变更，按规矩同步）
- **抽取白名单**（fl 名单长到抽取节点）：空=全抽（现状），非空=名单外碰都不碰
  （普通+精确支路同闸）；中继身份的 accepts/chainWants/chainEndsInTrash 同步闸。
  仓→抽取(白名单)→垃圾桶 两节点即定向销毁，过滤节点可省。
- **抽取自动启停**（传感器键位 si/sv/sl 长到抽取节点，sensorOpen 通用判定）：配了监测
  物品=阈值自动启停（如"钻石>10万才抽"）；感应暂停=持料待命不退料（条件到自动续跑），
  手动关才退料；下游视角感应暂停同视关闸（closedGate）。
- **安全桶**（fl 名单长到垃圾桶）：空=连啥吞啥（现状），非空=只吞名单内——accepts 拒收
  名单外（distribute 垫底两轮照旧），chainEndsInTrash 名单外不算销毁终点（精确抽取授权
  随之收紧）。

## m254 工程款造价段（mC 压缩包 BOM 改造 18/18 台收官，按规矩同步）

**工程款机器（角标Ⅲ，18 台）的配方从 m245 起改为"原版全量总数"记账**——数字对齐作者
litematic 实测蓝图逐块过账（归一化口径：红石线→红石、壁挂→手持、耕地→土、水=64 桶打水税、
岩浆=一桶一源全量、传送门/技术方块/生存不可获得剔除、载具展示类实体入料、活体不入、
铁傀儡折铁块4+南瓜1），再按压缩档向上取整（<64 散件原数；否则一级 64:1 向上取整；
二级 4096:1 仅在溢价≤15% 时用）——**只多不少**。

玩家侧怎么用：
- **备料**：超大工作台右栏"材料→压缩包"钮把同种普通物品每满 64 压一包、64 包级联成超级包
  （附魔书/药水等带组件物品不压，防抹组件）；"拆开材料包"反向，产物先落网格溢出进背包、每击报账。
- **认包**：匹配内核把包按 内容物×倍率 折算原版件数——1 个圆石一级包=64 圆石，1 个超级包=4096；
  裸包不顶数；取成品时包被消耗、找零自动拆回网格。
- **填料**：工程款配方的"填料"钮从背包按 二级包→一级包→散件 三轮贪心搬包进网格自动堆叠。
- **对账**：右栏材料对照 ≥1 万缩写 K（精确数看聊天缺料摘要，按原版件数报）。

账目权威源：`docs/litematic_实测_2026-08.json`（14 台精账）+ `docs/超级压缩改造核查.md` 第五节
摘录（4 台粗账=下限账：40核/920万船吸/史莱姆/猪灵交易，重传投影即可精算重跑）；
过账工具 `docs/tools_pack_bom.py`（多键合账/--extra 外挂账/断言双算）。

## 七、区块机器线（m376 移除器 / m377 过滤器——招牌组合）

**区块移除器**（m376）：手持对目标区块内任意方块右键=绑定（可交互方块潜行右键；重绑=清进度
重扫）→放**同维度**核心画布→自顶向下逐层移除，掉落物（如同正确工具无附魔挖掘）走出线/存储。
边界：基岩永不动；带方块实体的默认跳过（`chunkRemoverSkipBlockEntities`，防误吞基地——关掉
照拆但容器内容物按原版散落原地**不进机器**）；目标区块未加载=红灯**不强载**。**移除区域可调**（m382）：节点菜单换挡 1×1/3×3/5×5（上限 `chunkRemoverMaxRadius`），层主序扫描（整层推进，过滤停靠/续挖语义不变），换挡=新工程重扫；区域内任一分块未加载=**黄灯等待**整单停摆不跳块留窟窿（m387：等待非错误；核心自带 5×5 保载，把核心放进目标区域中心即可挂机；悬停节点看完整原因全文）。**区域自由调+双掉落模式+手持面板**（m386）：手持按设置键（默认 R，可改键位）开终端主题面板——
区域 ±1/±10 自由调（上限 `chunkRemoverMaxRadius` 默认 64≈129×129，改区域=重扫）；掉落模式
二选一：**有掉落·出货**（默认）/ **无掉落·极速蒸发**（不产任何物品，预算 ×`chunkRemoverNoDropSpeedMult`
默认 8，每拍另有 4096 硬顶护 tick，中途可切不重扫）；潜行右键空处=快切模式；画布节点菜单同口径三项。

**技能选中特效**（m384，`chunkFxEnabled` 总开关）：手持已绑定移除器=世界内浮现紫色能量选区框（全高线框呼吸脉动+分块格线，手持潜行右键空处即时换挡框随之变）；机器首铲=选区顶粒子环+信标激活音（"施法锁定"）；进行中削切面持续冒紫色传送门粒子（每台每拍封顶 6 点）。免费型，成本=
时间（默认 64 块/周期，config 可调，三系升级照常放大）。

**区块过滤器**（m377）：移除器的**规则挂件**——画布上与移除器连线即生效（**方向随意**，多台=
规则 AND 叠加，暂停即隔离）。两类规则：
1. **方块名单**：白名单=只挖名单内 / 黑名单=名单内不挖；**空名单=不限方块**（与物品过滤节点
   "白名单空=全拦"刻意相反——只配 Y 挡不配名单是核心用例）。名单选的是方块的物品形态。
2. **Y 挡位**（五挡循环换挡）：全高度 / 地表下 Y≤62 / 深层 Y≤0 / 深板岩 -64..0 / 地上 Y≥63。

三条要点：
- 过滤器本体**不收不产不转发物品**（往它连线只是挂规则，物流语义恒拒收，派过去的走默认路由回仓）。
- 名单外/窗外的方块**留在世上不拆**（"只挖矿石不破坏建筑"就是白名单矿石+Y≤62）。
- 游标单向：Y 挡缩窗会跳过窗外层且**不回卷**；停在过滤下限=黄灯（换挡/撤过滤器自动续挖），
  只有真挖穿世界底才置完成位；全量重扫=重绑移除器。

**虚空处理器**（m378）：垃圾炼经验——吞掉路由进来的物品，按 `voidXpPerItemsEaten` 件=1 经验
（默认 64）炼进**本核心经验池**（复制机/附魔工厂消费的那口）。收料语义与垃圾桶同律：内置
白名单**空=连啥炼啥**、非空=只收名单内（名单外走默认路由回仓）；distribute 两轮**同垫底**
（有别的目标先喂别人）；直连仓不抽（防手滑），经逻辑节点转接=授权照拉；精确件（附魔书等）
经授权链抵达=抹组件炼掉（磨石回收附魔经验的工业版）；余数记账进位不丢。停用（config）=
拒收+持料待命。招牌闭环：**移除器挖 → 过滤器筛 → 虚空炼经验 → 经验池驱动复制机/附魔工厂**。

**区块扫描器**（m380）：只读侦察端——绑定区块（移除器同款右键）→自顶向下只读扫描→统计报告：
方块总数/类型数/矿物合计（c:ores 标签∪`_ore` 后缀∪远古残骸）/容器数/生物数（完成拍一次性
清点）+方块 Top 榜（封顶 64 种溢出归"其他"）。卡面看摘要，**右键节点菜单看 Top8 明细**与
"重新扫描"；不动世界不收不产。标准姿势：**先扫描侦察 → 按报告配过滤器名单 → 再上移除器**。

**区块储存器**（m381，第 100 台）：模板端——绑定区块（同款右键）→只读全量扫描→模板入
**服务端模板库**（PersistentState，`chunkTemplateMaxCount` 封顶，满=红灯拒存清理后自动续存）
→产出**区块数据核心**×1。模板不变量=入模板的位可付可建：空气/基岩类/带方块实体（空箱防
复制基地）/无物品形态（火/传送门/**水岩浆**）一律不入。数据核心是引用凭证（只揣 UUID+摘要，
组件物品自动走精确账本）：**丢核心≠丢模板，复制核心≠复制区块**（重建照样逐份收料，无经济
漏洞）。重绑=新扫新模板，旧模板留库。半途重启=回顶重扫（暂存器刻意不落盘）。
