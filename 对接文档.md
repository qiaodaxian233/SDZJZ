# 《生电终结者 SDZJZ》项目 · 新对话对接文档

> 用法：开新对话时把本文档整个粘贴（或上传）给 AI，然后直接说任务。本文档只含**不变的规则**，
> 一切**当前状态**以仓库里的 HANDOVER.md / DEVLOG.md 为准——所以它永远不过期，下次下下次都能用。

## 一、项目是什么

- 《生电终结者 SDZJZ》：Minecraft **Fabric 1.21.1** 模组（Yarn 映射，纯 Java，前置仅 Fabric API）。
- 玩法：**节点画布替代红石生电**——94+ 台机器 · 存储网络 · 自动合成 · 跨维度物流。
- 仓库：`https://github.com/qiaodaxian233/SDZJZ`，主分支 `main`。
- 语言：代码注释、commit、文档全部**简体中文**。

## 二、开工三步（每个新会话固定动作）

1. `git clone --depth 1 https://github.com/qiaodaxian233/SDZJZ.git`
   （无 git 凭据时用 tarball：`curl -sL https://codeload.github.com/qiaodaxian233/SDZJZ/tar.gz/refs/heads/main | tar xz`）
2. **先读 `HANDOVER.md`**（开发守则+当前状态+待办池）和 `DEVLOG.md` **尾部几节**（最近里程碑），不要全量扫项目。
3. 按任务定向读文件（grep/sed 取行，省 token 口径），然后开干。

## 三、工作流铁律（违者返工）

1. **做一步推一步**：每个功能/修复 = 一个里程碑 `mNNN`（编号接 DEVLOG 最后一条往下排），独立 commit，
   commit message 详尽中文一行带 mNNN。完成即推送 `main`——**沙箱随时重置，绝不在本地攒**。
   **版本号一笔一跳（m317）**：提交前把 `gradle.properties` 的 `mod_version` 跳成 `0.1.<里程碑号>`
   （热修加字母尾号不抬数字段）；CI 版本闸按 DEVLOG 最大 mNNN 对表，忘跳即红。
2. **PAT 规矩**：**不要向作者索要 PAT**。作者会在对话里直接贴 `github_pat_...`，贴了就用
   （`git push https://qiaodaxian233:<PAT>@github.com/qiaodaxian233/SDZJZ.git HEAD:main`），
   推完**不要提醒撤销**。PAT **绝不写进任何提交文件，也不嵌进本文档的任何粘贴/上传副本**；git 身份沿用仓库既有提交者。
   没有可用 PAT → 本地提交 + `git format-patch` 导出 patch 交付，不专门开口要。
3. **每笔记账**：DEVLOG.md 追加 `## mNNN 标题` 条目（现象→根因→修法→教训 + **实机验证脚本**）；
   HANDOVER.md 的「当前状态」标题行随里程碑滚动改写。
4. **配置铁律**：新功能带配置项（开关+关键数值），不硬编码；改配置结构就把
   `config/SdzjzConfig.java` 顶部 `configVersion` +1 并在注释记 mNNN。
5. **编译风险控制**（沙盒跑不了 gradle，Fabric/Mojang maven 不可达）：
   - 新 API 一律先核名：**在树先例优先**（grep 项目里已用过的写法照抄）；否则拉 FabricMC/yarn 1.21.1
     官方映射核对（`raw.githubusercontent.com/FabricMC/yarn/1.21.1/mappings/net/minecraft/.../Xxx.mapping`）；
   - 核不到的列 DEVLOG「待编译验证」；沙箱可 `apt-get install openjdk-21-jdk-headless` 做**纯语法冒烟**
     （grep `expected|illegal|reached end|not a statement|unclosed`，其余报错都是缺 MC 依赖的正常噪音）；
   - **冒烟盲区**（m123/m180 教训）：缺**自家类**的符号错误会被"依赖噪音"过滤器吞掉——凡新引用自家类，
     必须对该类名定向 grep 报错=0 验证委托链；
   - 作者本地"拉取并构建"工具（gradlew build + jar 自动进 1.21.1 测试实例）是编译验证主链路，
     报错会贴回，按报告逐个修——**构建报告要专扫 warning 段**，弃用警告沙箱看不见（m328 冒烟盲区#6）。
   - **CI 已启用（m258 起）**：`.github/workflows/ci.yml` 三 job=Gradle 真编译出包 / GameTest 全量用例 /
     全部校验闸（tools_* 回归尺）。推完轮询 `api.github.com/repos/qiaodaxian233/SDZJZ/actions/runs?per_page=1`
     自主查结果，**闸红即停、禁预写账**（m291b）；GameTest 报告 blob 读不到时看 `ci-gametest-report` 分支（m310b）。
6. **注册六件套逐项计数断言**（m92b 教训，新物品/机器必做）：MachineDef + ModItems（注册+创造栏两处）+
   SuperBenchRecipes 配方 + 中英 lang + 模型 json + 贴图 png——改完逐项 grep 验数，JSON 过 json.load。
   **新机器接线另有五件**：tick 分支 / accepts / setNodeTarget / 客户端徽章 / **chainWants（最易漏，m132-6 实锤）**。
7. **文档同步**：机器数唯一数据源=Machines.java，动机器后跑 `python3 docs/tools_docs_sync.py`
   （校验模式漂移即红；`--write` 改 README 数字并重生成机器清单.md）。改路由代码须同步更新 `机器组合.md`。
8. **交付**：多文件一律走 **git push**，不甩一堆 attachment。

## 四、协作风格（作者明确要求）

- 用户反馈是第一信号，说了就照做，别拿"原则/最佳实践"包装拖延。
- 有担忧先把活干完，末尾一句话提醒即可，**不重复、不展开**。
- 别装看过资料；抓不全就说抓不全。不臆想业务，边界不清先问。
- 不猜接口、不造新接口（先复用已有）、不动架构红线、不盲目重构。

## 五、踩过的坑（新会话必读，别再踩）

- **多文件重构脚本：先建目录、先写新家、再动旧家**——m180 事故：先改写旧家后建新家，建档半途炸，
  方法体一度只存在于 git HEAD。恢复用 `git show HEAD:文件` 取原文。
- **ScreenHandler 的 quickMove/onSlotClick/Slot 钩子双端执行**：任何触碰库存的逻辑必须服务端权威；
  C2S 包一律服务端先验资格/材料再执行，别信客户端。
- **16 位通道上的哨兵值发收要成对审**（m106）：发端 clamp 到 0xFFFF，收端不掩码照样符号扩展成 -1 炸。
- **哨兵别用 Integer.MIN_VALUE**（m90），用 -1000 这类不会参与运算溢出的值。
- **升级/封顶公式必问"到顶之后玩家再投入会怎样"**（m99）：静默无效比数值弱更伤，界面上看不出来。
- **滚轮/点击要区域化**（m103/m107）：悬停哪个区域才响应哪个，防穿透误缩放画布。
- **bbmodel 转换**：多贴图必看 name/uv_width，不许无脑 textures[0]（mer 是光影图）；
  cube 与 mesh 坐标制式不同，逐类核对（m67/m152）。
- **排查崩溃先清自己嫌疑**（m142 毒区块票：哨兵被解成真实坐标发加载票打崩服务器，
  曾误判"非本模组"）；系统级新触点先过一问："哨兵/特殊值会不会被下游当真实数据解释"。
- **校验器自身也要先怀疑**（m109 坏尺子）；断言脚本也要审（m137 逐行拆参重核）。
- **python 补丁断言失败后续命令仍会跑**：逐对计数断言 + 写盘置尾 + 残留 grep 终检。
- **别人的 UI 叠在咱们屏上时**，先坐标反推+资产扫描定归属（m161 IPN 前科），别上来就翻自己代码。
- 界面文本只用 MC 字体能渲染的 BMP 字符（▶ ■ ★ ✔ ◆ 可用，emoji 渲染不出）。
- 精确存储/组件物品：仓储默认按 id 记账会抹组件——附魔书/药水这类走精确条目通道，改动时盯"不混堆不变裸"。
- **共享 Inventory 禁原地改栈**（m326b）：`decrement/increment` 绕开 markDirty，多观者下=幻影结果格+复制窗；
  改栈走 setStack，或收尾补 `markDirty()` 通知全体观者。
- **事务作用域内禁调手账四口**（m323/m327）：`withdraw/withdrawExact/deposit/depositExact` 混进事务，
  abort 时同键前像覆盖手账=复制窗；要混部先 commit 再手账（CI 有闸，豁免标记须注明理由）。
- **冒烟盲区家族**（沙箱 javac 缺 MC 类时查不出）：同签名重复方法（m271）/@Override 校验失效（m288）/
  弃用警告不可见（m328）；全库冒烟要 `-Xmaxerrs` 放开上限，默认 100 条封顶会假绿（m257）。
- **新增动画物品两件配套**（m320/m330）：登记 `client/SodiumSpriteKicker` 的 SPRITES 表（漏了=装 Sodium
  冻第 0 帧）+ CI 的 jar 内 mcmeta 计数闸随件数抬。

## 六、关键文件地图（按需读，别全读）

| 文件 | 管什么 |
|---|---|
| `HANDOVER.md` / `DEVLOG.md` | 当前状态+待办池 / 全部里程碑流水（尾部最新） |
| `Sdzjz.java` | 服务端注册入口 + 全部 payload 注册与接收器 |
| `block/StructureCoreBlockEntity.java` | **SCBE 超级类**（核心 tick 五分支/供料 supplyFor/分发 distribute/链需求 chainWants；绞杀者拆分中，纯函数往 `node/` 迁） |
| `node/NodeTags.java` | 节点栈 NBT 标签纯函数（m180 起，新代码直接引用它别走 SCBE 垫片） |
| `machine/Machines.java` | **机器唯一数据源**（MachineDef 定义+掉落表） |
| `registry/ModItems.java` | 物品注册+创造栏（六件套之一） |
| `machine/SuperBenchRecipes.java` | 引子签名配方（多重集全表唯一断言） |
| `machine/*Planner.java` | 合成/熔炼/酿造/附魔规划器 |
| `block/StorageCoreBlockEntity.java` | 存储网络（贴邻/数据线 BFS） |
| `screen/*ScreenHandler.java` + `client/*Screen.java` | 四屏：画布/数据面板/超级工作台/交易所 |
| `client/SciSkin.java` | **全 MOD UI 配色唯一出口**（换肤只改它；新颜色进它不许屏内硬编码） |
| `debug/SdzjzCommands.java` + `CoreProfiler.java` | `/sdzjz profile core\|network\|sched\|phase\|reset`、`bench`（一键压测）、`dumpgraph`（OP2 性能尺，改性能前后必对表） |
| `machine/CoreScheduler.java` | 生产预算四层封顶（节点/核内/区块/全服）+防饥饿调度（m270/m302~m324） |
| `gametest/SdzjzGameTests.java` | GameTest 全量用例（CI 行为判官，用例即语义规格） |
| `.github/workflows/ci.yml` | CI 三 job：真编译出包 / GameTest / 全部校验闸（挂着全部 tools_* 回归尺） |
| `任务看板.md` | 任务池（**停更于 m218 前后**，恢复维护前进度事实源=HANDOVER+DEVLOG） |
| `config/SdzjzConfig.java` | 全部配置 + configVersion |
| `机器组合.md` | 物流语义手册（三层物流/四逻辑节点/闸门/链式拉料），改路由须同步 |
| `docs/tools_docs_sync.py` | 机器数防漂移校验/生成 |

## 七、给新会话 AI 的开场白模板（复制改任务即可）

```
这是《生电终结者》Minecraft Fabric 1.21.1 模组项目，规则见我贴的对接文档，严格遵守。
先 clone 仓库读 HANDOVER.md 和 DEVLOG.md 尾部接上状态，然后完成以下任务（做一步推一步）：
1. <任务>
```

## 任务看板（m195 增补；m331 勘误）

仓库根 `任务看板.md` 原定为唯一进度事实源，但实际维护停在 m218 前后（表内 #1 还写着
"CI 待激活"，与 m258 已启用矛盾）——**恢复维护前，进度事实源=HANDOVER「当前状态」+
DEVLOG 尾部；看板内容一律先与 HANDOVER 对表再采信**。去留待作者拍板：恢复则按其页首
用法铁律执行（先做表再做事，m195 原案）；退役则删该文件与本节。
