# 作业表

> **用途**：让我不必每次问「下一步做什么」。每刀开工前读本表 → 取「下一刀」那项 → 做完打勾、
> 滚动更新本表 → 继续下一项。作者只在**需要拍板的事**和**实机反馈**上介入。
>
> **维护规矩**：每刀必须更新本表（与 DEVLOG/HANDOVER 同刀）。新发现的活儿当场加进待办，
> 不要攒在脑子里。已完成的移到文末归档区，保留刀号便于回查。

---

## 一、取活规则（我自己按这个排，不用问）

按序号从上往下取第一个**不阻塞**的：

1. **修构建失败 / 回归** —— 作者贴来的编译错、实机故障。**随时插队，最高优先**。
2. **作者当轮点名的事** —— 优先于本表任何既定项。
3. **当前分片的收尾项** —— 已开的分片先做完再开新的（避免半成品堆积）。
4. **无阻塞的主线活** —— 按本表「待办」区顺序。
5. **需要拍板的** —— 移到「阻塞区」，**不猜、不擅自选**，在汇报时提一句即可。

**并行纪律（m509 撞出来的）**：作者可能同时开着多个会话取活，本表「下一刀」不是锁。**开工前 fetch 一次、push 前再 fetch 一次**对表 HEAD；
若同项已被别的会话推上去，先读对方 DEVLOG 条目，再决定是补刀（做对方没做的那半、退役重复件）还是换项——不重做、不覆盖。

**主线画布改动波及检查（m523 评审②，A12 收官前有效）**：凡一刀碰了主线 `StructureCoreScreen`，收尾必问「改的段在 1.20.1 `CanvasScreen120` 有没有搬入副本」——锚点 `grep -n 'm164b\|m519\|m193\|m184\|m352' versions/1.20.1/.../CanvasScreen120.java`（悬停聚焦块 / 拖线三段 / 归并分流 / 选缘与柱心）；有副本=同刀同步或在 D 线登记欠账一行，**不许静默漂**。A12 下沉完这条自动作废。

**闸别无限补（m524 评审提醒①）**：第 20 闸三类判据都是血案事后补的，泛型/默认方法冲突/可见性还会冒新形状——维持「闸拦大流、CI 兜底」双层即可（m523b 正是 CI 抓的），**不为闸补判据陷入无限补丁**；新形状出现时先问"这一类有没有通用判据"，没有就记 DEVLOG 盲区清单，别硬写一条只认那一次的正则。

**普查稿防过期（m523 评审④）**：`docs/主线对比1201_全量缺口与超大工作台普查_m521.md` 是快照——**S 线每收官一刀、或两代缺口表有变化（新上挂/新退回/新方块屏），同刀在该稿末尾「状态更新」段追加一行**（刀号+改了哪格），别让它三个月后变第二张没人敢信的长文档。

### 我自己能决定 / 必须问

| 自己决定 | 必须问作者 |
|---|---|
| 技术实现方式、分片顺序、先搬哪件 | **玩法/语义变更**（数值、规则、平衡） |
| 加闸、改闸、闸退役 | **跨代行为取舍**（两代要不要一致、不一致算不算 bug） |
| 重构、下沉、命名 | **高风险主线手术**的时机（作者在玩的版本） |
| 文档、注释、教训记录 | 需要新素材/资源的事（贴图、音效） |

---

## 二、每刀标准流程（照做，别靠记忆）

**搬代码时**（m483~m492 全部教训固化）：
- [ ] 1. **先查 `docs/世代API对照表.md`** —— 要搬的原文里有没有 1.21 专属 API？有就查现成世代口。
- [ ] 2. **整段搬，不重写** —— 原文粘过来，只改真正编译不过的地方。判据：改完 `diff` 得跟原文逐句对得上。
- [ ] 3. **连调用点一起搬** —— 函数体只是一半，**调用点的参数怎么算往往才是那些刀号真正住的地方**（m492 血案）。
- [ ] 4. **切段别按行号盲切** —— 先看区间里有没有夹着别的成员（m487 把构造器一起删了）。
- [ ] 5. 注释里的刀号、血案记录、"看起来像 bug 其实不是"的说明 —— **一起搬，不许精简**。

**上挂白名单时**（m523，评审③：SB2 的教训是先上挂、构建红、再补闸——验收顺序倒置）：
- [ ] 5c. **切片搬代码按方法整段切**（m529b 血案：按手打行号切漏了两行局部变量声明，冒烟两代绿、CI 三 job 齐红——`cannot find symbol: variable` 与继承字段噪音在无 MC jar 下不可区分，筛选器兜不住）：生成脚本按签名定位 + 括号配平取段；切完对产物跑一次"用到的小写标识符是否都有声明"结构自证（m529b DEVLOG 有脚本）。
- [ ] 5b. **先候选过第 20 闸再写白名单**：`python3 docs/tools_gen_api_check.py --candidate com/sdzjz/x/Y.java …`（三类判据：1.21 专属符号 + 自家类可见性 + **抽象必自声**（m523b：`extends MC类 implements MC接口` 的文件，接口抽象方法不许靠 1.21 父类兜底，对 yarn 1.20.1 数一遍谁实现；表列不到的自己数——冒烟对 does-not-override-abstract 是盲的））绿了才写 `versions/1.20.1/build.gradle`，写完再跑一次正式模式。
- [ ] 5c. **每刀开工先跑 21 闸**（SB3~SB5 起硬性）——绿着开刀，红了先修；作者本地构建不是第一判官。
- [ ] 5d. 新立带默认值的宿主口/安装口 → **同刀配一条「装了没」判官**（默认关是静默的，m523 教训②）。

**验证时**（每刀必跑）：
- [ ] 6. 两代 javac 冒烟 → **`python3 docs/tools_smoke.py`**（全量两趟排序清单 + 改动文件逐文件单编，内部走 `tools_smoke_filter.py`；不要手写 grep，m491 血案；不要手拼 javac 清单，m508 血案：`find` 无序清单下主线屏整类不归因）。
      **源集要全**：两代都按各自 `build.gradle` 的真实源集跑（主线=`src`+`xplat`+`common`；
      1.20.1=白名单**现读不手抄**+`versions/1.20.1`+`common`）。**只跑改动那一代 = 换了量程的尺子**——
      m500 抓到的那条主线访问错就是这么在十九刀里没人看见的。
- [ ] 7. **22 道闸全绿**（一条命令跑完，见下；m527 起第 22 闸=1.20.1 数据包机械转换漂移闸：主线加配方/战利品表或本世代注册新件后先 `python3 docs/tools_datapack_1201.py --write` 再 commit）
- [ ] 8. 版本号 = DEVLOG 最新刀号；DEVLOG + HANDOVER + **本表**同刀更新
- [ ] 9. commit（中文一行带刀号）+ push
- [ ] 10. 汇报时说清**实机预期**（作者据此验），并说明**这刀改没改存档/运行时路径**
- [ ] 11. **推完读 CI**（m510 立）：`api.github.com/repos/qiaodaxian233/SDZJZ/actions/runs?per_page=3`（带 PAT 头）看三 job；红就拉
      `ci-mojmap-errors/ci-javac-errors.log`、`ci-retro-errors/ci-retro-errors.log`+`ci-retro-server-tail.log`、`ci-gametest-report/ci-gradle-errors.log`+`ci-server-tail.log`
      （raw.githubusercontent.com 可读）。**CI 红=这刀没完**，先修再开下一刀；m473~m509 三十多刀红着推是这条从来没被执行过。

```bash
for t in m172_check ci_resources docs_sync color_scope_audit dup_method_check pinyin_check \
         bounded_codec_check override_check core_scheduler_sim version_check common_gate \
         tx_scope_audit layer_gate yarn_residue_check gauge_audit retro_parity_check \
         node_key_check dualwrite_audit dead_ref_check gen_api_check arity_check datapack_1201; do
  python3 docs/tools_$t.py >/dev/null 2>&1 || echo "❌ $t"; done; echo "22 闸跑完"
```

---

## 三、下一刀

**先跑 22 闸、读 m534 CI**（主线 GameTest job 必须绿：FTA 判官七处调用点改走 `FabricStorageAdapter.of`）。**下一刀 F1d NeoForge 模块编译**（见 F 线段；开工首步 API-01 锁坐标与接口，别一上来写 build.gradle）。

**m533 评估报告入账后的两条新规矩**（详见 `docs/评估报告入账_m533.md`）：
- **`docs/版本能力表.md` 与本表同刀更新**——动了哪格（注册/配方/运行/存档/客验）就改哪格，没证据写「？」不猜。
- **1.20.1 轨道新序**（F 线之外的 1.20.1 活按此取，作者可随时改）：**C4a/C4b 自动合成 → U1 升级安装 → U2 融合阶位/存储升级 → C3 经验池 → C2b-brew 酿造域 → N3-a/b/c → A12 → C5 特种机型逐族 → 兼容与观测（K1 Create / 命令 / JEI）**。理由：自动合成是当前最大玩法缺口且配方查询（C2a）已接好；升级件能拿能合不能装是明面矛盾；经验池与酿造域不依赖附魔定价（核过 `xpPool` 进项=熔炉族/虚空处理器），只有 C2b-ench 与附魔工厂被挡。
- **F 线与 1.20.1 轨道交替**：F1c/F1d 收官（NeoForge 第一个生产闭环）优先；其后按取活规则在两条轨道间取无阻塞项。

**N 线：非机器物品 20 件对齐主线（作者拍板「做成一样」，按行为类是否可上白名单分刀）**——每刀同样：注册句照主线、六件套逐字节拷、`tools_datapack_1201.py --write`、判官②白名单滚行、`EXPECTED_REACHABLE` 按 CI 改、缺口表 21→N。
- ~~N1~~ ✅ m528：六件注册（`RetroItems.plain`）、四升级件创造栏机器后同主线序、包框不上栏同主线、六件套逐字节、datapack 19→23（+4 升级件配方）、判官账 56→60。**升级件本世代能拿能合不能装**：消费方 `xplat/node/NodeUpgrades` 引 src `ModItems` 未上白名单、画布升级槽未搬——装进机器等 **U1**（m533 起：A8 当年按「本世代无机制」销项，机制随 U 线补，见 U 线段）。
- ~~N2a~~ ✅ m529：压缩包×2——`CompressedPackItem` 上白名单（死 import 清掉、id 解析走 `ItemData.itemById`、tooltip 签名差留两代壳 `LegacyCompressedPackItem`/`CompressedPack120`，类转 abstract）；渲染体下沉 `client/CompressedPackIcon`（两代共用，加载器接口留壳，扫光实现由壳传入）；`RetroSuperBenchHost` 压缩包八口真实现（空宿主退役）；判官 +1 压缩/拆包往返（累计 74）。
- ~~N2b~~ ✅ m530：抓物笼——`ItemData` 加 `setCustomName` 口（两代实现）、`CaptureCageItem` 两代化（abstract + `LegacyCaptureCageItem`/`CaptureCage120` 壳）、`RetroHooks`=FabricHooks 原文装 Hooks 五口、抓物事件主线原句挂 RetroBootstrap、Host `cagedType` 接原句（十口全真）、笼子六件套、datapack 24；判官 +1 抓僵尸往返（累计 75）；可达 60→**100**（m530b 按 CI 实测改，离线预估 101 差 1）。原计划：`CaptureCageItem` 上白名单需 ①`ItemData` 加 `setCustomName(ItemStack, Component)` 口（1.21 组件 / 1.20.1 setHoverName）②`ResourceLocation.parse`→`ItemData.id` ③tooltip 签名差留壳（同 N2a 手法）④`UseEntityCallback` 挂 RetroBootstrap（主线 FabricHooks m94 同句，Fabric 1.20.1 有）；`RetroSuperBenchHost.cagedType` 接主线原句；44 条刷怪类配方随之"可达"（判官②账面 60→104 由 CI 定）；创造栏插回升级件之后、包之前。
- **N3（m533 按评估报告拆三组，横跨多个玩法域，不当一个小任务）**：
  - **N3-a 跨维度绑定族**：`linker`（数据链接器）+ 方块 `wireless_node`/`satellite_node`（各带 BE/渲染）。验收：可绑定/可断开、距离与维度行为明确、重启后恢复、失联时状态可解释。
  - **N3-b 交易族**：`villager_contract` + 方块 `trade_center`（交易所屏，`VillagerTrades` 纯 Java 已在）+ 交易相关机型。验收：合同来源→就业→交易闭环；物品/折扣/经验状态持久化。
  - **N3-c 随身族**：`terminal`（远程面板）、`auto_feeder`、`portable_vault`（随身仓库屏 handler + 必要 mixin）。验收：远程开屏/库存同步/补货喂食逐项；仓库物品与专属槽位不因死亡或重进丢失。
  - `logo` 随资源整理归位、`chunk_data_core` 随区块族（C5）能力验收归位，不单独排刀。m521 缺口表「方块 8→5」余三在 a/b 里。
- 每刀开工先 `--candidate` 过第 20 闸量其行为类，能挂就挂、不能就世代壳各写（RetroBlocks 内部类样板）。

**F 线：Forge 加载器（作者 m528 口头：「我还要做 Forge 的 1.20.1、1.21.1」）**——
- **资产盘点（m528 量）**：`xplat/`149 文件 + `common/`23 文件 **0 行加载器符号**（第 13 闸 layer_gate 硬保）；Fabric 胶水只在 `src/`（Legacy 1.21.1：21 文件 66 行）与 `versions/1.20.1/src`（19 文件 64 行）。**Forge 壳 = 把这两份胶水各重写一遍，业务层零改动**——五类：①注册（`Registry.register` 直调→`DeferredRegister`/`RegisterEvent`，Forge 冻结注册表）②网络（`FabricPacket/PacketType/PayloadTypeRegistry`→Forge `SimpleChannel` / NeoForge `PayloadRegistrar`）③传输（Fabric transfer v1 的 `RetroXfer/FabricXfer`→`IItemHandler` capability，Xfer 五口已是世代口）④创造栏/菜单/BE 类型（`ItemGroupEvents`→`BuildCreativeModeTabContentsEvent`、`ExtendedScreenHandlerType`→`IForgeMenuType.create`/`IMenuTypeExtension`、`FabricBlockEntityTypeBuilder`→`BlockEntityType.Builder`）⑤GameTest 注册（`FabricGameTest`→`@GameTestHolder/@PrefixGameTestTemplate`）+ 客户端物品渲染器（`BuiltinItemRendererRegistry`→`IClientItemExtensions`）。
- **已有地基**：`versions/1.21.1/neoforge/`（m408 骨架，只挂 `common/`，当时被 Yarn↔Mojmap 卡住——**m409 全仓迁 Mojmap 后那道墙已经没了**，xplat 可直接挂）；CI 已有 NeoForge 1.21.1 编译出包 job。
- ~~F0~~ **已拍板（m529）：2×3 矩阵**——1.21.1：Fabric（主线 ✅）/ **NeoForge 21.1**（m408 地基）/ **MinecraftForge 52**；1.20.1：Fabric（✅ 世代壳）/ **MinecraftForge 47** / **NeoForge 47**（与 Forge 47 近同源，可从 Forge 47 壳派生）。
- **F1（拆四刀）**：目标=NeoForge 模块整挂 `common`+`xplat`+`src`（排除 Fabric 胶水）+ NeoForge 胶水。前提=`src/` 除胶水白名单外零 Fabric 符号（第 13 闸 m531 起管：白名单 9 / 待拆欠账 9，逐刀销）。
  - ~~F1a~~ ✅ m531 机械项：入口拆 `loader/FabricEntry`+`client/FabricClientEntry`（四加载器口安装+提供侧传输注册+Fabric 专属客户端注册进入口，业务体留 `Sdzjz.init()/SdzjzClient.init()` 静态方法，fabric.mod.json 入口改）；BE 类型建器换原版 `BlockEntityType.Builder`（8 处）；创造栏换原版 `CreativeModeTab.builder(...).displayItems(...)`（lambda 体原文 129 行）；BER 注册换原版 `BlockEntityRenderers`；命令注册走 `Hooks.onRegisterCommands` 新口（Fabric/Retro 两实现）。src 含 Fabric 文件 21→18（9 胶水 + 9 待拆）。
  - ~~F1b~~ ✅ m532 菜单数据口：xplat `loader/MenuData<D>`（provider 给数据）+ `loader/Menus`（`type(factory, codec)`/`open(player, provider)` 两口，Net/Xfer/Env/Hooks 同族，1.21 世代专用不上 1.20.1 白名单）+ `FabricMenus`（type=原 ExtendedScreenHandlerType 原句；open=把 MenuData 包成 ExtendedScreenHandlerFactory 三方法原文）；四 BE `implements MenuData<BlockPos>`、TerminalItem 匿名工厂、ModScreenHandlers 四类型、xplat 四调用点改 `Menus.open`；FabricEntry 装 Menus 早于 Sdzjz.init。欠账 9→3。原计划：`ExtendedScreenHandlerFactory`×5（四 BE + TerminalItem）+ `ExtendedScreenHandlerType`（ModScreenHandlers）→ xplat 口：`MenuData<D>`（provider 给数据）+ `Hooks.openMenu(player, provider)`（Fabric 包成 ExtendedScreenHandlerFactory / NeoForge `openMenu(provider, buf 写入)`）+ `Hooks.menuType(factory, codec)`（Fabric ExtendedScreenHandlerType / NeoForge IMenuTypeExtension）；先 grep 全部 `openMenu(` 调用点。
  - ~~F1-0~~ ✅ m533 依赖闭包（评估报告 P0-①）：xplat 显式 import 根 src 类 **23 文件 / 9 类**（Sdzjz、四 BE、TradeCenterBE、ModBlockEntities、ModItems、ModScreenHandlers），8 类零加载器符号、1 类（StorageCoreBlockEntity）在待拆表=F1c；**第 13 闸新段**：xplat 引 `SRC_GLUE_OK` 任一文件→红，引 `SRC_GLUE_PENDING`→报数。结论：xplat→src import 不是 F1 阻断（F1d 整挂 src），闸看着「不许引胶水」即可。
  - ~~F1c~~ ✅ m534 存储传输提供侧适配器：内部类 `FabricLedger` 66 行整段搬 `src/loader/FabricStorageAdapter`（三类机械替换），BE 只留不透明缓存槽 `transferAdapter(Supplier)`（实例缓存=事务日志归属，NeoForge 同槽）；`FabricEntry` 注册改 `of(be)`；`DataCableBlockEntity.coreStorage` 改走既有 `Xfer.find`（零新口）；判官七处同改；第 13 闸待拆 3→2、依赖闭包引待拆 0。**NeoForge 对位留 F1d**：`IItemHandler` capability（`Capabilities.ItemHandler.BLOCK` + `RegisterCapabilitiesEvent`，高频查询 `BlockCapabilityCache`），`(be, side) -> be.transferAdapter(() -> new NeoForgeStorageAdapter(be))`。**F1d 传输验收（m533 按报告 §14.1 补，F1c 主线行为零变化不适用）**：Xfer 双侧——访问外部容器 + 暴露自家存储；场景八条：目标全满 / 部分接收 / 源不足 / 命名物品 / 药水·附魔书组件保真 / 端口切换 / 区块卸载重载 / 双向物流同时运行；账本结算以实际插入余量与实际提取栈为准，模拟结果只用于规划；大仓库对槽位接口暴露用有界视图分批搬，不遍历全库。
  - **F1d ←下一刀 NeoForge 模块编译**：build.gradle 挂三源集（排除 `SRC_GLUE_*`），写 `NeoForgeEntry/NeoForgeClientEntry` + 四口 NeoForge 实现（Net=PayloadRegistrar、Xfer=IItemHandler、Env=FMLPaths、Hooks=NeoForge 事件）+ 注册时机（`RegisterEvent` 内触发 ModBlocks/ModItems/… 类初始化）+ 模型插件 `ModelEvent` + 判官 `@GameTestHolder`；CI job 升编译+GameTest。
    **开工首步=API-01 锁定接口与依赖（m533 并入，不另立）**：NeoForge 21.1 坐标 / `IItemHandler`+`Capabilities.ItemHandler.BLOCK`+`RegisterCapabilitiesEvent` / `RegisterPayloadHandlersEvent`+`PayloadRegistrar`+`PacketDistributor` / `IMenuTypeExtension` / `@GameTestHolder`+`@PrefixGameTestTemplate`；**`ModBlocks/ModItems` 静态字段初始化即注册**是报告点名的生命周期检查点，NeoForge 须在 `RegisterEvent` 内触发这些类的初始化。收官判据不是「jar 出来了」，是**服务器启动 + 客户端进得去 + 拿到并放下核心与工作台 + 最小生产链（箱子供料→生产→入仓→取物）**。
  - **F1e 差异收尾（m533 新登记，评估报告 F1-5）**：JEI 插件入口（三加载器发现方式各异）、mixin 族（大堆叠 / 随身仓库）、判官全量在 NeoForge 跑、CI job 升玩法测试；逐项核对主线功能表（`docs/版本能力表.md` 第 3 节填满七态）。
- **F2**：MinecraftForge 1.21.1 壳（`versions/1.21.1/forge/`）——F1 的壳按 Forge 52 API 再写一份（事件总线/网络 SimpleChannel/能力 ForgeCapabilities 不同名，业务层不变）。
- **F3**：MinecraftForge 1.20.1 壳（`versions/1.20.1/forge/`）——对位 `versions/1.20.1/src` 19 文件胶水；白名单机制复用（同一份 xplat 子集）。
- **F4**：NeoForge 1.20.1 壳——从 F3 派生（maven 坐标与包名差为主）。
- **三加载器共用件不变的硬约束**：xplat 零加载器符号（layer_gate 加 `net.minecraftforge|net.neoforged`，F1 开工同刀）；每个壳的胶水都是"一句转调 xplat"，像 m529 的 `CompressedPackRenderer` 壳那样。
- **纪律**：加载器壳按 B 线规矩=业务不进壳、壳不进 xplat（layer_gate 加 `net.minecraftforge|net.neoforged` 到 LOADER_SYMBOLS，F1 开工同刀）。

**贯穿 B 线的结论（五次逐句对反复验证，A5a 是第六次——服务端六方法归一后 diff=0）**：「两代行为有差异」几乎
从不意味着「代码没法共用」——差异通常只住在很少的几行里，收进世代口（带默认值的钩子/
Impl 接口），剩下的就能整段共用。B3/B4/B5 全部照此办理，B5 还多验证了一条边界：
**世代口必须是"零加载器符号"的**——业务逻辑和它依赖的具体接口类型要分开看，后者若是
FTA/加载器类型，哪怕两代代码逐行相同也不能塞进 xplat，这层"薄壳"必须留在各自源集
（m503→m504 那次返工的教训，`layer_gate` 硬闸看着）。
**开工纪律（m495 教训）**：先把两代对应段**逐句对一遍**再决定能不能共用——
不要从"行为描述不同"推"代码没法共用"，m473/m475 那句「不是照抄能解决的」就是这么推错的
（真去对，二十行相同、三行不同）。**引用 m4xx 排刀稿结论前先问「它预设的路线还在吗」（m502 规矩）。**
**闸的自证纪律（m506 教训）**：新立/改闸时，自证要用它声称能防的那次血案**重放**，不是随手捏一个错——
第 19 闸被 m504 的语句级裸调用形状考了一次才发现从来抓不到那一类。

## 四、待办（按取活顺序）

### A 线：画布视觉（真移植·搬主线绘制件）
| # | 事项 | 依赖 | 验收 |
|---|---|---|---|
| ~~A1~~ | ~~SciSkin 上挂 + 节点卡~~ | — | ✅ m483/m484 |
| ~~A2~~ | ~~连线缎带 + 锚点~~ | — | ✅ m488/m492 |
| ~~A3~~ | ~~节点悬停详情浮层~~ | — | ✅ m493 |
| ~~A5a~~ | ~~分组服务端下沉（六方法+网络包对位）~~ | — | ✅ m506（CanvasGraphState 六方法两代共用 + 1.20.1 两包/接收器 + 契约八条判定，两代判官 +5） |
| ~~A5b~~ | ~~分组客户端读+组框渲染~~ | — | ✅ m507（GroupFrameRenderer 两代共用，diff=0；1.20.1 组框要等 A5c 建出组才可见） |
| ~~A5c~~ | ~~分组客户端交互（框选/拖动/菜单/重命名）~~ | — | ✅ m508（照主线 m192 事件段逐句写进 CanvasScreen120；分组三片收官，1.20.1 分组第一次完整可玩） |
| ~~A5c-补~~ | ~~右键菜单机制两代共用（m508 最小菜单件退役）~~ | — | ✅ m509（CanvasMenu 整段搬 diff=0；1.20.1 菜单换主线 3A 工艺；第 19 闸并入 mt 贴图路径普查） |
| ~~A4~~ | ~~连线归并徽章 m193~~ | — | ✅ m511（WireBundler 两代共用 diff=0；唯一世代口=存储端卡接口位置 StoragePorts；顺手扶正 1.20.1 机器线矩阵/线色/配色作用域） |
| ~~A6~~ | ~~缩放平滑动效 m186~~ | — | ✅ m512（ZoomAnim 两代共用，归一差=Host 四句；1.20.1 滚轮改主线原文结构，顺手补侧栏滚轮/地图区不缩放） |
| ~~A7a~~ | ~~节点菜单 / 存储菜单条目~~ | — | ✅ m513（主线原文只装本世代有的五条+两条；右键一键摘回退役；拾取器一族无机制不搬） |
| ~~A7b~~ | ~~整理布局 autoLayout~~ | — | ✅ m514（几何下沉 CanvasLayout，发包留各代；主线发的是普通移动包，世代口=包名） |
| ~~A7c~~ | ~~设置面板 m199 / 帮助卡 m219~~ | — | ✅ m515（量下来 1.21 专属为零→整段搬；1.20.1 顶栏两钮；背景四行等 A11） |
| ~~A8~~ | ~~升级槽 drawUpgradeSlots~~ | — | ✅ m519 核实：1.20.1 当时无升级系统（无 `NodeUpgrades`/升级包/三级数，m464 记档）→ 当刀不搬。**m533 更正：机制随 U 线补，`drawUpgradeSlots` 整段搬并入 U1**（N1 已把四升级件注册进来，「能拿能合不能装」是明面矛盾） |
| ~~A9~~ | ~~悬停聚焦 m164b~~ | — | ✅ m518（主线 m164b 块整段搬，API 面 1.21 专属为零；归一差=存储端一支走 `storageAt`；m511 两处分流 `true`→`lit/lit2`） |
| ~~A11~~ | ~~1.20.1 画布底/网格/暗角层随主题+配置~~ | — | ✅ m516（CanvasBackdrop 两口，帧序不变；装饰底图贴图拷入；面板背景四行生效） |
| ~~A10~~ | ~~1.20.1 卡片层随缩放~~ | — | ✅ m517（节点卡进世界矩阵；存储卡两代都不随 zoom 属主线口径，线口改回 24 修 m458 悬空） | 缩放时卡尺寸随 zoom 变（现在只挤位置不缩卡，5% 档位互叠）；命中框同口径 |
| **A12** | **`CanvasScreen120` 减负**（m523 评审②：992 行里 ≈464 行是主线原文搬入段——renderBg 181/mouseClicked 143/mouseReleased 111/mouseDragged 29——主线画布每改一处要人工同步且无闸管）：①连线两循环 + m164b 悬停聚焦 + m193 分流喂料 → xplat `CanvasWireLayer`（View 口：节点/存储端坐标、hov 态、StoragePorts）；②m519 拖线三段状态机（起手/预览/落靶）→ `CanvasWireDrag`（落靶发包留各代：`linkNodes`/`linkStorageTo` 三态）。m515 手法：先量 API 面，世代差预期只剩 StoragePorts/包名/三态连线三处已知口；主线屏退壳 | 排 **SB5 之后**（作者点名 S 线优先）；若主线画布改动先落在搬入段，按取活规则提前 | 两代归一 diff=0；CanvasScreen120 降到 ~500 行；取活规则里那条「波及检查」作废 |

### S 线：超大工作台（作者点名 m521，排 C4 前；全量缺口表见 docs/主线对比1201_全量缺口与超大工作台普查_m521.md）
| # | 事项 | 依赖 | 验收 |
|---|---|---|---|
| ~~SB1~~ | ~~普查+分片稿~~ | — | ✅ m521 |
| ~~SB2~~ | ~~三文件上挂白名单~~ | — | ✅ m522 挂三件→m522b 退两件→**m523 重挂**（Recipes/Handler/CraftGridInventory 三件齐，白名单 40） |
| ~~SB2b~~ | ~~handler 四口 → 两文件重新上挂~~ | — | ✅ m523：菜单类型安装口 `installType` + 物品宿主口 `Host` 十口（主线 LegacySuperBenchHost 原句照搬 / 1.20.1 空宿主全关）+ 删 asCraftInput 覆写（1.21.1 源码核过）；第 20 闸候选模式先闸后挂（血案重放红 5→绿）；判官 +1 |
| ~~SB3~~ | ~~方块壳 + BE + 注册 + 创造栏 + 资源六件套~~ | — | ✅ m524：`SuperBenchBlock120`（RetroBlocks 内部类，主线原文三处世代差）+ `SuperBench120` + 三注册 id 同名同源 + 注册后紧跟 `installType` + 创造栏 + 五件逐字节同主线 + lang 两键；最小屏壳 `SuperBenchScreen120` 让"右键能开屏"可验；判官 +1（累计 68） |
| ~~SB4~~ | ~~`SuperBenchScreen` 绘制体下沉共用件~~ | — | ✅ m525：xplat `SuperBenchView`（392→共用 404 行，归一 diff 只剩 gfxTex/gfxItem/Host 十口/`return false`×3/发包两处），主线壳 83 行、1.20.1 壳 91 行（最小壳退役）；白名单 +2（PinyinInitials/SuperBenchView）；gui 贴图拷入；宿主前提头注第一份（D5）|
| ~~SB5~~ | ~~1.20.1 判官~~ | — | ✅ m526：`RetroBenchTests` +5（累计 73）——多重集全表唯一+蓝图逐格==BOM / 配方 id 对本世代注册表**已知缺口白名单双向对表** / 端到端铺料→出机器→PICKUP 扣料 / 填料钮+空宿主两钮守恒+越界 / quickMove 三向。**S 线收官**。量出 1.20.1 配方可达 **0/126**（m526b CI 对表纠正离线"3 条"；core_module 一件卡死全部）→阻塞区 |

### 其余缺口（m521 表登记，按建议序，不动；**m533 起各有归属**：交易所=N3-b、升级系统=U1/U2、卫星/无线=N3-a、随身仓库+mixin=N3-c、命令/JEI=P 线「兼容与观测」/F1e——本行只留作 m521 出处）
交易所（方块+屏，`VillagerTrades` 纯 Java 已在）→ 升级系统（三级数+升级包+槽 UI，A8 复活）→ 命令 `/sdzjz profile`（性能尺）→ 卫星/无线节点（跨维度）→ 随身仓库+mixin 族 → JEI。

### B 线：真移植剩余（业务代码合一）
| # | 事项 | 依赖 | 验收 |
|---|---|---|---|
| ~~B1~~ | ~~图状态 / 身份键 / 账本 / 路由脑~~ | — | ✅ m477/m478/m485/m486 |
| ~~B2~~ | ~~tick 执行面下沉~~ | — | ✅ m495/m496/m498/m499（StructureCore120 已降为世代壳） |
| ~~B3~~ | ~~面板聚合/并账/排序/过滤/开窗下沉~~ | — | ✅ m500（PanelAggregator，业务双写 991→978） |
| ~~B3b~~ | ~~取物回账下沉 → DataPanel120 降世代壳~~ | — | ✅ m501（业务双写 978→801） |
| ~~B4a~~ | ~~DataCable120 抽取口业务下沉 + RetroXfer~~ | — | ✅ m502（业务双写 801→512） |
| ~~B4b~~ | ~~DataCableBlock120 方块层下沉，CableEnd120 退役~~ | — | ✅ m503（业务双写 512→309） |
| ~~B5~~ | ~~StorageCore120 FTA 事务包装下沉~~ | — | ✅ m504（业务双写 309→0，**B 线业务代码收官**） |
| D-判官合一 | 4 条判官同名用例收进契约（`fabric_abort_restores_normal_entry`、`fabric_nested_abort_restores_exact_entry`、`ledger_nbt_roundtrip_reconciles_at_scale`、`type_safety_limit_rejects_new_types`） | 非阻塞，可另立时机 | 断言一份，两代各喂自己的实现（照 RecipeDomainAssertions 样子） |

### C 线：功能补齐（⑤ 分片）
| # | 事项 | 依赖 | 验收 |
|---|---|---|---|
| ~~C1~~ | ~~⑤d2 熔炉族 tick~~ | — | ✅ m494（路由两面 m486 下沉时已随主线原文带过来，配方域一装即生效） |
| ~~C2a~~ | ~~⑤d1 RetroRecipeAccess 合成+余料两域~~ | — | ✅ m520（Legacy 原文，世代差三处；契约拆 runCraftSmelt/runBrewEnch；1.20.1 判官接上） |
| C2b-brew | ⑤d1 RetroRecipeAccess **酿造域**（m533 拆：1.20.1 药水走 NBT、酿造注册表静态类，m476 量过无阻塞；26.2 分域适配器同款） | — | RecipeDomainAssertions 酿造类断言绿；酿造链能生成正确药水 |
| C2b-ench | ⑤d1 RetroRecipeAccess **附魔域** | **阻塞：附魔定价 A/B 拍板** | 七类断言全绿（runBrewEnch 接上换 runAll） |
| C3 | ⑤e 经验池（结构核心侧 xpPool） | **—**（m533 核过：进项=熔炉族 `0.1*done`（m494 记档）+ 虚空处理器，不依赖酿造/附魔域；原「C2」是笔误） | 经验有入账、使用与存档回归；熔炉族 m494 那句「xpPool 未建」同刀销 |
| **C4a（1.20.1 轨道第一刀）** | ⑤d3 自动合成机目标选择 UI（`ct` 键 + 包 + 接收器；先逐句对主线拾取器入口与 payload） | — | 节点菜单进入，选目标后卡面显目标；服务端验资格；重进存档目标保留 |
| C4b | ⑤d3 自动合成机 tick 分支（主线整段搬；**先单目标**，递归/循环配方/跨核心订单另排） | C4a | 供料线端到端出工作台，非配方料一件不动；无目标黄灯可识别；配方余料正确返回 |
| C5 | ⑤d4 酿造塔 + 附魔工厂 + 其余特种机型逐族（复制器/虚空处理器/区块族/信标/交易族/组件产物山羊角） | 酿造塔=C2b-brew；附魔工厂=C2b-ench + C3；其余各族独立 | 每族独立记录：目标可设置 / tick 可执行 / 产物正确 / 持久化 / 故障恢复 |

### U 线：升级系统 1.20.1（m533 重开，评估报告 P1-①；作者 m527「按 1.21.1 做成一样」适用）
| # | 事项 | 依赖 | 验收 |
|---|---|---|---|
| U1 | 加速/数量/并发升级：`NodeUpgrades` 上白名单前提=它引 src `ModItems` → 身份判定走世代口（m472 `defOf` 同款）；安装/拆卸包与接收器（服务端验材料）；三级数进 tick 预算（m464 起恒 1 倍那几处换真值，逐句对主线 m99 工作量累积模型）；画布升级槽绘制（原 A8，主线 `drawUpgradeSlots` 整段搬）；Shift 批量插拔 | — | 装入扣正确物品、拆出返还；三类分别生效（速率/并发/数量封顶）；拆机返还守恒；存档保留等级；判官钉「装了没」+ 数值 |
| U2 | 融合阶位（超级·/神级·/GM·）与存储升级，**分别核对消费方** | U1 | 融合数量/阶位/拆解与主线规则一致；存储升级独立验收（类型硬顶/容量），不只查物品注册 |

### P 线：产品与发布（m533 新开，评估报告 §八/§九/§15.2/API-04~06 落点；不阻塞开发轨道，按容量选入）
| # | 事项 | 依赖 | 验收 |
|---|---|---|---|
| P1 | **`docs/版本能力表.md` 维护**（每刀同刀改格，无证据写「？」） | — | 表与代码不漂；F1e/N3/U 线收官时对应格从 ✗→✅ |
| P2 | **停机原因码**（产品能力）：服务端出稳定原因码、客户端翻译玩家语言（未设目标 / 缺料 / 输出已满 / 外部端口不可用 / 配方失效 / 当前版本未支持），画布显受影响节点与恢复动作，核心/Jade 只显摘要 | **先问作者**（碰主线灯表文案与协议） | 多故障显主要原因可看详情；两代共用一份原因码表 |
| P3 | **Beta 验收清单**（报告 §八）：目标提交+jar；生存链完整（核心模块→工作台→机器→画布→供料/出料→入仓→终端取物，刷怪链含笼）；守恒动作对账；存档回归（节点/连线/分组/升级/库存/目标/经验，重启后原样）；单机+专服各一次；兼容锁定 MC/加载器/Create/JEI 版本组合三态（未装/装/真连）；性能沿 `/sdzjz bench` 1核×64→10核×256→100核×64 记 MSPT/吞吐分布/同步量/内存/帧时 | 六格各自功能清单 | 无崩溃、无丢件复制、无永久饿死；清单全过；性能差异可解释 |
| K1 | **Create 传送带↔数据线互通实测**（1.20.1 README 原承诺项；主线 FTA 直连 m161c 同验） | 锁定 Create Fabric 版本组合 | 选定物流部件真供料/出料、堵塞恢复、重进存档；数量与附加数据两边一致 |
| 候选（不入待办） | Jade 摘要适配 / EMI / KubeJS 数据化配方 / CC:Tweaked 只读外设 / 公开 `sdzjz-api` / GitHub Releases·Modrinth 更新检查 / 液体·能量端口 | 作者有需求再立 | — |

### D 线：欠验与债务
| # | 事项 | 依赖 | 验收 |
|---|---|---|---|
| D1 | **m479 §5 欠验清单**（存档/账本行为四条） | 作者实机 | 见 `docs/真移植C阶段小普查_m479.md` §5 |
| ~~D3~~ | ~~主线 GameTest 自 m481 起从未真跑~~ | — | ✅ m510/m510b：主线编译+主线 GameTest+1.20.1 九条全绿，m511 开工读数确认三 job 全绿（m472 以来第一次） |
| D4 | **白名单挂载人工同步点**（评审意见③）：xplat 文件"已下沉但忘挂载"无闸管。候选两法：整挂+排除清单（要先把主线专属 xplat 文件列成排除表，量一次）/ 或闸查「xplat 里被白名单文件引用的自家类必须也在白名单」（第 20 闸自家类可见性已管这半，反向"该挂没挂"另立） | — | 先量主线专属 xplat 文件数再定；不在 S 线中途开 |
| D5 | **共用件「宿主前提」头注制度化**（**m525 第一份模板已落地=`SuperBenchView` 头注五条：坐标层/字体/控件/取色/事件**；余四件回头补）（评审意见④，四例 m511/m516/m517）：每个 xplat/client 共用件头部写清宿主必须给什么（字体/矩阵层/缩放口径/取色作用域）；先补已有共用件（WireBundler/GroupFrameRenderer/NodeCardRenderer/CanvasBackdrop 四件），新件按模板写 | — | SB4 下沉 SuperBenchScreen 时按此模板写第一份，回头补四件 |
| D2 | 主线 Gradle 构建从未由我验证过 | — | **只能靠作者**；每刀汇报时提醒——m504 挖出的 `FabricLedger` 六处裸调用真编译错（潜伏自 m485）是实证：这条阻塞不是走流程式的提醒，主线代码里可能还有别的同类问题没被冒烟筛选器+第 19 闸的责任真空接住，**建议尽快跑一次真实构建** |

---

## 五、阻塞区（等作者拍板，我不猜）

| 事项 | 选项 | 影响 |
|---|---|---|
| ~~1.20.1 超大工作台配方可达 0/126~~ | **已拍板（m527）：作者「按 1.21.1 的来，做成一样」**=A 注册 core_module（m527 落地）+ 余 20 件非机器物品按 N 线逐刀对位主线；12 条含 1.21 原版新料的配方**照旧**（本世代没那些物品，配方表两代同一份不改，浏览器显示空气是事实不是 bug）；抓物笼随 N 线（整块特性） | S 线交付价值解锁：m527 起可达 56/122 |
| ~~F0~~ | **已拍板（m529）：作者「就是 MinecraftForge，我提的那些版本都有 Forge、Fabric、NeoForge」**=1.20.1 与 1.21.1 各三加载器（2×3 矩阵，见 F 线） | F 线按矩阵排刀 |
| **附魔定价 `getAnvilCost()` 无 1.20.1 对位** | **A** 按 `Rarity.getWeight()` 反推（数值接近，不保证逐位相等）／**B** 抄一张 id→cost 常量表进 Common（两代逐位相等，但原版加新附魔要补表，须配对表闸） | 挡住 C2 → C3/C4/C5 全线 |
| 节点卡信息密度 | 照搬主线（信息密集）／做精简版 | A 线观感，已按「照搬」执行，作者可随时叫停 |
| **1.20.1 画布坐标钳幅两口径**（m506 观察） | 单节点拖动 `StructureCoreMenu120.clampCoord` 钳 ±1e5，下沉后的组整体位移走主线 `CanvasGraphState.clampCanvas` 钳 ±1e6 | 不影响正确性（int 内、渲染乘 zoom 不溢出）；要不要统一到 ±1e6 属跨代行为取舍，作者点头即改一行 |
| **首批 Beta 范围**（评估报告 §十一） | 报告建议先承诺：生产 / 存储 / 画布 / 工作台 / 自动合成 / 升级主链；区块族、随身仓库 mixin、跨版本专属内容逐项定「同批」或「后续」 | 决定 P3 清单边界与 F1e/N3 的完成定义 |
| **HANDOVER 结构是否瘦身**（评估报告 P1-②） | 报告建议首页只放当前阶段/有效约束/未决项/证据链接，旧过程转索引；`版本能力表.md` 已兼「一页现状」 | 作者文档，点头才动；不点头维持「当前状态」行滚动改写 |
| **人力与工期折算**（评估报告 §十一） | 报告按 1 名熟练开发者全职估 10~18 周到六格 Beta；作者业余+多会话并行，作业表不做工期承诺 | 只记不催 |
| ~~面板取物申报策略是否两代统一~~ | **已按 A 落地（m501）**：各代保留各自策略，共用件只吃算好的 want；作者若想要 B（两代取物手感一致，本世代协议加档位字段）随时说 | — |

---

## 六、已完成（归档，倒序）

- **m534** F1c：`FabricLedger` 内部类整段搬 `src/loader/FabricStorageAdapter`（三类机械替换），BE 只留不透明缓存槽 `transferAdapter(Supplier)`；FabricEntry/DataCable（改走 `Xfer.find`）/判官七处调用点同改；第 13 闸待拆 3→2、依赖闭包引待拆 0；主线行为零变化
- **m533** 评估报告入账（零业务代码）：`docs/评估报告入账_m533.md` 逐条处置 + `docs/版本能力表.md` 首版（七态×四格）+ 本表重排（U 线/P 线新开、N3 拆 a/b/c、C2b 拆 brew/ench、C3 解阻塞、F1-0 ✅/F1e 新增、1.20.1 轨道新序、阻塞区 +3）+ 第 13 闸「xplat→src 依赖闭包」段 + 1.20.1 README 现状 + `StructureCore120` 两句运行时文案去开发编号
- **m532** F1b：菜单数据口三口（MenuData/Menus.type/Menus.open）+ FabricMenus，四 BE/TerminalItem/ModScreenHandlers/四调用点去 Fabric；src 待拆欠账 9→3
- **m531** F1a：`src/` 去 Fabric 化机械项（入口拆分/BE 建器/创造栏/BER/命令口），第 13 闸加三加载器符号 + src 胶水白名单与待拆欠账（9/9）
- **m530** N2b：抓物笼对齐主线（ItemData +setCustomName 口、笼子两代化、RetroHooks 装平台事件五口、抓物事件原句、Host 十口全真）；判官 75；可达 100（CI 实测）
- **m529** N2a：压缩包×2 对齐主线（item 两代化 + 渲染体下沉 xplat + Host 真实现），F0 拍板落地=2×3 加载器矩阵，F1~F4 排刀；判官 74
- **m528** N1：纯 Item 六件对齐主线（四升级件+两包框），datapack 23 文件，判官账 56→60；升级件能拿能合不能装（A8）；**F 线（Forge 1.20.1/1.21.1）排入作业表 + F0 拍板项**；生成器 RetroItems 解析加自证（本刀被自己的重构打断过一次）
- **m527** SB6（作者拍板「按 1.21.1 做成一样」）：`RetroItems` 骨架 + core_module 六件套；1.20.1 `data/` 从零到有=主线 34 文件机械转换 19 个（`tools_datapack_1201.py`），**第 22 闸**漂移校验；判官账本 core_module 滚出、`EXPECTED_REACHABLE` 0→56；N 线三刀排好
- **m526b** 热修：判官②CI 首次实机对表红——白名单两条过期（trade_center/villager_contract 是结果件不是材料）+ 离线可达账 3→**0**（addSmall9 字母常量 MM 漏数）；加 `EXPECTED_REACHABLE=0` 等值断言，③④账说 0 时明说空转
- **m526** SB5：1.20.1 超大工作台五判官（累计 73），S 线收官；量出 1.20.1 配方可达 0/126（core_module 一件卡死全部、12 条含 1.21 原版新料）→阻塞区
- **m525** SB4：主线 `SuperBenchScreen` 392 行整段下沉 xplat `SuperBenchView`（两代共用，归一 diff 只剩世代口/宿主口），两代屏退壳；白名单 +2；1.20.1 最小壳退役换共用件；宿主前提头注第一份
- **m524** SB3：超大工作台 1.20.1 方块壳/BE/MenuType 注册（注册后紧跟 installType）+ 创造栏 + 资源五件逐字节同主线 + lang；最小屏壳；判官 +1（累计 68）；评审两条提醒入账
- **m523b** 热修：m523 CI 1.20.1 红（`CraftGridInventory` 靠 1.21 父类 `SimpleContainer.getItems()` 兜底接口抽象方法，1.20.1 父类无）→ 自声明 `NonNullList<ItemStack> getItems()`；第 20 闸第三类判据「抽象必自声」（重放自证）；第七类冒烟盲区记档；对照表 +1
- **m523** SB2b：超大工作台 handler 四口重做（菜单类型安装口 installType/物品宿主口 Host 十口 default/删 asCraftInput 覆写）→ Handler/CraftGridInventory 重挂白名单（38→40）；主线 LegacySuperBenchHost 原句照搬、1.20.1 空宿主全关且压缩钮明说；第 20 闸 `--candidate` 先闸后挂（血案重放红 5→绿）；判官 +1 super_bench_host_pack_roundtrip（累计 67）；评审四条入账（A12 登记、波及检查、先闸后挂、普查稿防过期）
- **m522b** 热修：作者 1.20.1 构建红→两文件退出白名单；第 20 闸补 1.21 新类名表 + 自家类可见性判据（重放红 5 处后绿）
- **m522** SB2：超大工作台 Recipes/ScreenHandler/CraftGridInventory 上挂 1.20.1 白名单；ItemData 三口 itemById/clearCustomName/id（服务端可用）；11 处三类换口 + 第 20 闸抓漏的 isSameItemSameComponents×5→StackKey.same
- **m521** 零代码笔：主线↔1.20.1 全量缺口普查 + 超大工作台分片 SB2~SB5（作者纠正「我说的是超大工作台」；作业表新开 S 线）
- **m520** C2a：1.20.1 配方域补合成/余料两域（LegacyRecipeAccess 原文，世代差三处=无 RecipeHolder/getId()/new ResourceLocation）；RecipeDomainAssertions.runAll 拆 runCraftSmelt+runBrewEnch（断言不动）；1.20.1 判官 recipe_domain_contract_craft_smelt（累计 66）
- **m519** 作者点名：1.20.1 连线改拖线（主线 mouseClicked 三段起手/mouseReleased 三支落靶/三条预览线原文；世代差三口=存储卡 24×24 供料口抓取、`linkStorageTo` 三态包连发、`linkNodes` cut 客户端判）；点击式连线模式+顶栏钮+首端描边退役，lang 两键删；A8 升级槽核实无机制不搬，A 线清空
- **m518** A9：1.20.1 悬停聚焦 m164b——主线 `hovN/hovEnd/hovAny` + 两处分流 lit + 非归并线 `mix(termInk,wire,0.30)` 压暗式整段搬进 `CanvasScreen120.renderBg`；世代差只有三处记法（`wnx(i)`/`NH`/`storageAt`）；归并线与徽章的 lit（m511 共用件）第一次真被喂到 false
- **m517** A10：1.20.1 节点卡层进世界矩阵随 zoom 缩（同一份 NodeCardRenderer 两代坐标层不同=环境第四例）；存储卡两代都不随 zoom（主线放置卡口径），线口 m458 起 24*zoom 悬空改回 24；死 ring 拔除
- **m516** A11：画布底色/装饰底图/32px 双色网格/暗角整段搬成 xplat `CanvasBackdrop` 两口（主线浅带夹在两口之间帧序不变）；1.20.1 调色板定色+随缩放网格退役；装饰底图贴图拷入；m515 面板背景四行在 1.20.1 生效
- **m515** A7c：画布设置面板 m199（十行+RGB 滑杆+主题预设+恢复默认）与帮助卡 m219 整段搬成 xplat `CanvasSettingsPanel`/`CanvasHelpCard`（API 面量下来 1.21 专属为零）；主线 13 处收成三壳六句；1.20.1 顶栏添「设置」「帮助」、六处派发主线同句同序、lang 两键；背景四行等 A11；新登记 A11
- **m514** A7b：整理布局落位几何下沉 xplat `CanvasLayout`（m149 竖排/m221 间距配置/存储右列），两代各自发 NodeMove/StorageNodeMove；1.20.1 画布菜单补「整理布局」
- **m513** A7a：1.20.1 节点菜单/存储连线菜单条目（主线原文过机制面过滤：断开全部连线/组合所选/组合相连/取出机器/取消；存储菜单两条）；`connectedComponent` 下沉 GroupFrameRenderer；右键一键摘回退役改开菜单；存储边断线走三态循环包按态连发；三张菜单贴图入 1.20.1；A7 拆 a/b/c，拾取器不搬
- **m512** A6：m186 缩放平滑动效整段搬成 xplat `ZoomAnim` 两代共用（归一差=Host 读写四句；无 MC jar 单编 0 错）；主线四方法转发壳七处调用点零改动；1.20.1 滚轮改主线原文结构（因子 1.1/0.9、范围走配置、指数缓动），顺手补**侧栏滚轮滚清单**（m459 记档说修了、入口那支从没写过）与地图区不缩放；新登记 A10 卡片层随缩放；对照表补一口两行
- **m511** A4：m193 连线归并整段搬成 xplat `WireBundler` 两代共用（六段归一 diff=0；唯一世代口 `StoragePorts`=存储端卡接口位置；机器归并线约法"调用方已在世界矩阵下"）；1.20.1 顺手四件：机器线层包进 `pushWorld`（m488 半搬：屏幕层传 zoom）/线色改 m198 配置色/`render()` 开画布配色作用域（m214）/死 `line()`+`Axis` 拔除；对照表补两口五行；PAT 401 先 patch 交付→作者补 PAT 后已推，**CI 八 job 全绿（m511b）**
- **m510** 修构建失败：主线 `probeChainWants` 七参传五参（m481 起主线编译恒红 28 刀）→补 `probeOutT()`；1.20.1 九条判官恒失败（m473 起）全是判官写错→`add(c,s)` 取回图里那份等九处；**第 21 闸 arity_check**（冒烟第六类盲区：含不可解析类型实参的调用 javac 不报适用性错）；流程加"推完读 CI 三分支"第 11 步
- **m509** A5c 补刀：主线 m148 3A 右键菜单机制整段搬成 xplat CanvasMenu 两代共用（四段归一 diff=0，唯一世代差=贴图 id 走新增 SciSkin.gfxTex），主线屏机制退役为同签名转发壳；m508 那份 1.20.1 最小菜单件整段退役换共用件，组菜单/画布菜单改主线原文形态；第 19 闸并入菜单贴图路径普查（两路自证）；对照表补七行；**协作教训：并行会话取活 push 前再 fetch**
- **m508** A5c：1.20.1 分组客户端交互照主线 m192 事件段逐句落地（框选/G 键/组菜单/拖组/重命名窗），选中描边/框选矩形进 GroupFrameRenderer 共用；**冒烟第五类盲区**（javac 类体归因看源文件顺序，78 文件曾不归因）→ `docs/tools_smoke.py` 两代全量排序+改动文件逐文件单编
- **m507** A5b：组框渲染+几何整段搬成 xplat GroupFrameRenderer 两代共用（diff=0，唯一参数化世代差=cardHeight），1.20.1 第一次画出组框（只读）；顺手修 1.20.1 nodeAt 命中框 24×24 残留（卡已 100×52）
- **m506** A5a：分组六方法整段搬进 CanvasGraphState 两代共用（归一后 diff=0，onChange 构造器注入）；1.20.1 NodeGroup/NodeGroupMove 两包+接收器+可测操作核；第四张跨代契约 CanvasGroupAssertions（八条）；顺手修第 19 闸两处坏尺子（dict 重复键覆盖 / 声明正则把行首裸调用当声明——m504 血案形状恒不红），三步自证含血案重放
- **m505** A4/A5 小普查（零代码笔）：厘清依赖方向（A5 先于 A4，作业表原表述是笔误）；发现服务端分组业务（六方法）零 MC 1.21 专属 API 可真移植下沉，快照通道已能带分组数据；A5 拆三子刀（服务端/客户端读/客户端交互），产出 `docs/画布分组归并1201_A4A5小普查_m505.md`
- **m504** B5 收官：FTA 事务包装两代共用（`StorageLedger.ftaInsert/ftaExtract/ftaAmount`），两代 `FabricLedger` 退回薄壳（业务双写 309→0，**B 线业务代码整体收官**）；顺手修潜伏自 m485 的真编译错（`exactIndexOf` 等六处裸调用漏 `ledger.` 前缀）；返工一次（xplat 零加载器符号，`layer_gate` 硬闸）
- **m503** B4b：方块层下沉（CableEndCore+CableEnd 退役 CableEnd120），DataCableBlock120 降世代壳（业务双写 512→309；B 线数据面板/数据线两大块全部收官，只剩 StorageCore120）
- **m502** B4a：抽取口业务下沉（ExtractPort+RetroXfer），DataCable120 降世代壳（业务双写 801→512）
- **m501** B3 收官：取物回账下沉，DataPanel120 降世代壳（业务双写 978→801）
- **m500** B3 前半：面板聚合两代共用（PanelAggregator）+ 修主线真编译错（`g` 跨包不可见，m481 判官带进来的）
- **m499** B2 收官：六分支下沉，StructureCore120 降为世代壳（业务双写 1829→991）
- **m498** B2 收尾：拉料循环下沉 + 第二批对表闸退役（对表闸萎缩速度=真移植进度条）
- **m497** 修 m494 构建失败 + import 完整性普查（第四类盲区：两把尺子的交界处）
- **m496** B2 续：均分分发下沉（同一形状第二次兑现）
- **m495** B2 前置：产物分发下沉（「执行面取舍不同」原来只差三行）
- **m494** ⑤d2 熔炉族 tick 落地（万能熔炉可玩）+ 1.20.1 配方域熔炼一域实装
- **m493** 建作业表 + 节点悬停详情下沉（**A 线主体收尾**）
- **m492** 连线锚点照主线原文重写（m184 选缘/卡底接口/m352 柱心分高/pxScale）
- **m491** 冒烟报告筛选器（漏筛类型错的教训）+ 修 m488 构建失败
- **m490** 世代 API 对照表 + 第 20 闸 + 小地图整段搬
- **m489** 数据面板屏工艺 + 搜索框挪位 + 补共用件资源
- **m488** 连线缎带整段搬 + 括号配平并入第 19 闸
- **m487** 修 m485/m486 构建失败 + 第 19 闸（悬空引用）
- **m486** 路由脑判定层两代共用 + 第一批对表闸退役
- **m485** 存储账本核心两代共用（B 阶段主体）
- **m484** 节点卡整张下沉共用
- **m483** SciSkin 上挂（绞杀者第六刀）
- **m477~m482** 真移植 A/D 阶段（图状态/身份键/两域契约/双写闸）
- **m471~m476** C2-⑤c 分片（在途缓存/逻辑节点/链式拉料）+ ⑤d 普查
