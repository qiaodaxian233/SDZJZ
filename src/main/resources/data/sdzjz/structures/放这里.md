# 结构 .nbt 放这里

ObjToSchematic 导出的 **.nbt（Structure blocks 格式）** 放进本目录。
- 单个 ≤ 48×48×48（原版结构方块上限）；更大请在 ObjToSchematic 把 Size 调到每轴≤48，或切块。
- 文件名用英文小写，例如 `factory_machine.nbt`。
- 放好告诉 Claude 文件名，会加载并做成"建造蓝图"物品：够料→一键摆出。
